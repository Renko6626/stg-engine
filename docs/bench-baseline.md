# 性能基线（M0-18，2026-07-18）

> 数据源：`cargo run --release -p stg-harness -- bench`。
> 机器：Intel i7-8750H @ 2.20GHz（笔记本，**热漂移显著：两轮实测均值差可达 ±50%**，
> 绝对值仅看量级、趋势可信）。每档预热 120 帧 + 实测 600 帧；step 计时含导演补弹。
> 更新纪律：大改运动/碰撞/校验和后重跑一轮，把新表贴在旧表上方留史。

## 内存账（扁平无堆，size_of 即全部）

- **World 总计 0.92 MB**（设计预算 ≈1.3MB 内，D10 达标）；16 帧快照环 ≈ **14.7 MB**。
- 弹池 0.42 MB · 其余（变换段/globals/players/缓冲）0.44 MB · 三小池合计 ~0.06 MB。

## step 曲线（2026-07-18 第二轮，release）

| 场景 | 稳态弹 | step 均 µs | p50 | p99 | 快照 µs | 校验和 µs |
|---|---|---|---|---|---|---|
| 哑弹 1024 | 1024 | 40 | 29 | 156 | 463 | 2147 |
| 哑弹 2048 | 2048 | 51 | 38 | 267 | 339 | 1536 |
| 哑弹 4096 | 4096 | 69 | 49 | 241 | 318 | 1410 |
| 哑弹 8192 | 8192 | 110 | 79 | 464 | 173 | 1267 |
| xform 512 | 512 | 61 | 42 | 259 | 206 | 1263 |
| xform 1024 | 1024 | 114 | 76 | 619 | 198 | 1256 |
| xform 2048 | 2048 | 302 | 206 | 1273 | 288 | 1525 |
| 全混合（真实规模） | ~189 | 20 | 13 | 99 | 262 | 1414 |

（第一轮同机数值约为本表 0.5-0.7×——热态差异；量级与斜率一致。）

## 结论（帧预算 16667 µs @60Hz）

1. **模拟本体极其便宜**：满池 8192 哑弹 ≈ 110µs = **0.7% 帧预算**；变换弹逐帧 sincos
   回填约为哑弹 4-5× 单价，段池满载 2048 发也只 ~300µs；真实游戏规模（~200 弹全机制
   混合）≈ 20µs。**ECL 指令预算可以定得很宽裕**——以 ~10ns/指令估算，每帧 1 万条指令
   ≈ 100µs 量级，完全可负担（M1 grill 时以此定防死循环上限）。
2. **校验和是最大单项成本**（1.3-2.1ms/帧，为 step 本体的 10-20×）：FNV-1a 逐字节扫
   0.92MB 所致。金向量/CI 逐帧算无碍（离线）；**联机随包 K=20 采样的既定设计被实测
   证实必要**（摊薄后 ~70µs/帧）；将来若在意可议分块/增量哈希，现无必要。
3. **rollback 完全可行**（M3 定心丸）：快照 memcpy 170-460µs、16 帧环 14.7MB；
   最坏 16 帧重演 ≈ 16×300µs ≈ 5ms，一帧预算内吃得下。
4. p99 尾部（数百 µs 级）主要是 Windows 调度噪声与批量补弹帧，非模拟本身的病态路径。
