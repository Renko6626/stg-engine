# M0-4/5: World 拼装 + step 骨架 + 纯弹幕金向量 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans（分组检查点执行）。

**Goal:** 把数学核 + 校验和 + 池焊成一个**可推进的确定性世界**：`World` 拼装、`step` 宪法顺序（11 相位 + PhaseGuard + 导演槽）、vendored PCG32、`create_bullet` 写 API、安全 `copy_into` 快照；`stg-harness golden` 换成**真实 step 演化的纯弹幕金向量**，让 CI 的 `determinism-gate` 从对拍占位序列变成**对拍真实世界演化**。

**Architecture:** 最小垂直切片（无 ECL/玩家/碰撞）。`stg_core::world`（WorldBody + `pub(crate)` 相位函数 + create_bullet + PhaseGuard）；`stg_core::step`（组装层：`World{body}`、`step_with_director`、宪法顺序）。导演槽 = Rust 闭包（`FnMut(&mut WorldBody)`，step-3 跑一次）。快照 = 安全逐字段 `copy_into`（`define_pool!` 生成每池 copy_into）。RNG = vendored PCG32。金向量场景（harness）：导演每帧用 `polar_to_vec`(压 sincos) + `rng`(压 PCG32) 铺环、弹积分、越界/寿命 → cleanup churn，600 帧逐帧 World checksum。

**Tech Stack:** Rust 1.92；stg-core（+ rng/world/step 模块）、stg-derive（扩 copy_into）、stg-harness（真实金向量）。

## Global Constraints（grill 决策 + 不变量）

- **最小切片**：World 仅 `frame/rng/bullets/diag/last_status/phase_guard`（3 输出缓冲无生产者，随 collide/settle/reqs 再加）。
- **11 相位宪法全写 + PhaseGuard 押运全 11**；实现 begin/导演槽/integrate/cleanup/advance，其余 no-op stub（都调 `phase_enter` 保序）。
- **快照 copy_into 安全逐字段**（`copy_from_slice` = memcpy），**stg-core 零 unsafe**。
- **create_bullet 走 P1 写 API + P4-a**：池满 → NULL + `diag.pool_full[BULLET]++` + `last_status=POOL_FULL`（**计数入校验和**）。
- **RNG vendored PCG32**（state+inc 16B，XSH-RR），`rand_range(n)=next_u32()%n`（确定）。
- **PhaseGuard** `#[cfg(debug_assertions)]`，校验和 skip；乱序 debug panic。
- **World `Box` 堆分配**（`World::new(seed)->Box<World>`）；M5 并行 env 再上就地初始化。
- **integrate 仅 direct** `pos+=vel`+delay（+life 递减）；双表示 POLAR/CART + 极坐标 setter 推迟。
- I1/I4/I7、字段声明序校验和、小端；提交结尾附 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

## File Structure

- 新建 `crates/stg-core/src/rng.rs`（Pcg32）。
- 新建 `crates/stg-core/src/world.rs`（WorldBody + 相位 + create_bullet + PhaseGuard + DiagCounters + 常量）。
- 新建 `crates/stg-core/src/step.rs`（World + step_with_director + copy_into）。
- 改 `crates/stg-core/src/lib.rs`（`pub mod rng/world/step;` + re-export）。
- 改 `crates/stg-derive/src/lib.rs`（define_pool! 生成 `copy_into` + `free_index`）。
- 改 `crates/stg-harness/src/main.rs`（golden 真实场景）。
- 改 `stg-world-design.md`（A4 最小切片注记）、`CLAUDE.md`。

---

### Task 1: vendored PCG32（`rng.rs`）

- [ ] `crates/stg-core/src/rng.rs`：

```rust
//! Vendored PCG32（XSH-RR，state+inc = 16B）——I3 随机源，是 `World` 字段、随快照回滚。
//! 算法字节冻结（同 checksum 纪律）：外部 crate 悄改实现 = 全体回放作废。

/// PCG32 随机数发生器。
#[repr(C)]
#[derive(Clone, Copy, PartialEq, Eq, Debug, crate::checksum::Checksum)]
pub struct Pcg32 {
    state: u64,
    inc: u64,
}

impl Pcg32 {
    const MUL: u64 = 6364136223846793005;

    /// 播种（`seq` 选流；标准 pcg32_srandom_r）。
    pub fn new(seed: u64, seq: u64) -> Self {
        let mut r = Pcg32 { state: 0, inc: (seq << 1) | 1 };
        r.next_u32();
        r.state = r.state.wrapping_add(seed);
        r.next_u32();
        r
    }

    /// 下一个 u32（推进 state，XSH-RR 输出）。
    #[inline]
    pub fn next_u32(&mut self) -> u32 {
        let old = self.state;
        self.state = old.wrapping_mul(Self::MUL).wrapping_add(self.inc);
        let xorshifted = (((old >> 18) ^ old) >> 27) as u32;
        let rot = (old >> 59) as u32;
        xorshifted.rotate_right(rot)
    }

    /// `[0, n)` 上的有界随机（`% n`，确定；偏差处处相同，对 danmaku 可接受）。`n==0` 返回 0。
    #[inline]
    pub fn rand_range(&mut self, n: u32) -> u32 {
        if n == 0 { 0 } else { self.next_u32() % n }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    // PCG 官方 demo 向量：pcg32_srandom_r(seed=42, seq=54) 后 6 次输出。
    #[test]
    fn pcg_reference_vector() {
        let mut r = Pcg32::new(42, 54);
        let want = [
            0xa15c02b7u32, 0x7b47f409, 0xba1d3330, 0x83d2f293, 0xbfa4784b, 0xcbed606e,
        ];
        for (k, &w) in want.iter().enumerate() {
            assert_eq!(r.next_u32(), w, "PCG32 第 {k} 个输出");
        }
    }

    #[test]
    fn deterministic_and_seq_independent_streams() {
        let mut a = Pcg32::new(1, 1);
        let mut b = Pcg32::new(1, 1);
        assert_eq!(a.next_u32(), b.next_u32()); // 同种子同流 → 同序列
        let mut c = Pcg32::new(1, 2);
        assert_ne!(Pcg32::new(1, 1).next_u32(), c.next_u32()); // 不同流不同序列（极大概率）
    }

    #[test]
    fn rand_range_bounds() {
        let mut r = Pcg32::new(7, 7);
        for _ in 0..1000 {
            assert!(r.rand_range(10) < 10);
        }
        assert_eq!(r.rand_range(0), 0);
    }
}
```

- [ ] `lib.rs` 加 `pub mod rng;`。
- [ ] `cargo test -p stg-core rng` 绿；commit `feat(rng): vendored PCG32`。

### Task 2: `define_pool!` 生成 `copy_into` + `free_index`

- [ ] `stg-derive/src/lib.rs` 的 `define_pool` 生成的 `impl #pool` 里追加两个方法（在 `iter_alive` 后）：

```rust
            /// 安全逐字段快照拷贝（每条 SoA 数组 copy_from_slice = memcpy，原地无临时量）。
            pub fn copy_into(&self, dst: &mut Self) {
                #( dst.#fnames.copy_from_slice(&self.#fnames); )*
                dst.generation.copy_from_slice(&self.generation);
                dst.alive.copy_from_slice(&self.alive);
            }

            /// 按索引释放（清 alive 位，无句柄校验）——供相位循环 cleanup 用。
            pub(crate) fn free_index(&mut self, idx: usize) {
                self.alive[idx / 64] &= !(1u64 << (idx % 64));
            }
```

- [ ] `bullets.rs` tests 加快照往返测试：

```rust
    #[test]
    fn copy_into_roundtrip() {
        use crate::checksum::Checksum;
        let mut a = BulletPool::new();
        let h = a.alloc(dumb_bullet(5, 6)).unwrap();
        let snap = a.checksum();
        let mut b = BulletPool::new();
        a.copy_into(&mut b);
        assert_eq!(b.checksum(), snap); // 拷贝后指纹相同
        // 改 a 不影响 b
        let i = a.get(h).unwrap();
        a.x[i] = crate::math::Fx::from_int(999);
        assert_ne!(a.checksum(), snap);
        assert_eq!(b.checksum(), snap);
    }
```

- [ ] `cargo test -p stg-core bullets` 绿；`cargo clippy` 干净；commit `feat(pool): copy_into 快照 + free_index`。

### Task 3: `WorldBody` + `create_bullet`(P4-a) + PhaseGuard

- [ ] `crates/stg-core/src/world.rs`：

```rust
//! 世界本体（stg_core::world）—— WorldBody 字段 + `pub(crate)` 相位函数 + 写 API + PhaseGuard。
//! 最小切片（M0-4）：无 ECL/玩家/碰撞；3 输出缓冲随各自生产者再加。

use crate::bullets::{BulletHandle, BulletInit, BulletPool};
use crate::math::Fx;
use crate::rng::Pcg32;

// ── 常量：池 id / 错误码 / 场界（D7 中轴原点，384×448 + 越界边距）──────────
pub const POOL_BULLET: usize = 0;
pub const STATUS_OK: u16 = 0;
pub const STATUS_POOL_FULL: u16 = 1;

const FIELD_HALF_W: i32 = 192; // x ∈ [-192, 192]
const FIELD_HEIGHT: i32 = 448; // y ∈ [0, 448]
const OOB_MARGIN: i32 = 64; // 越界回收边距

// ── 相位索引（A4 v2，0-based；PhaseGuard 押运）───────────────────────────
pub(crate) const NUM_PHASES: u8 = 11;
pub(crate) const PH_BEGIN: u8 = 0;
pub(crate) const PH_DECODE: u8 = 1;
pub(crate) const PH_DIRECTOR: u8 = 2;
pub(crate) const PH_PLAYERS: u8 = 3;
pub(crate) const PH_XFORM: u8 = 4;
pub(crate) const PH_INTEGRATE: u8 = 5;
pub(crate) const PH_COLLIDE: u8 = 6;
pub(crate) const PH_SETTLE: u8 = 7;
pub(crate) const PH_ECL_HOOK: u8 = 8;
pub(crate) const PH_CLEANUP: u8 = 9;
pub(crate) const PH_ADVANCE: u8 = 10;

/// 诊断计数器（P4；**参与校验和**——两机必须丢得一样多）。
#[repr(C)]
#[derive(Clone, Copy, Default, crate::checksum::Checksum)]
pub struct DiagCounters {
    pub pool_full: [u32; 8], // 按池 id
    pub contract_viol: u32,
}

/// 世界本体。
#[repr(C)]
#[derive(crate::checksum::Checksum)]
pub struct WorldBody {
    pub frame: u32,
    pub rng: Pcg32,
    pub bullets: BulletPool,
    pub diag: DiagCounters,
    pub last_status: u16,
    #[cfg(debug_assertions)]
    #[checksum(skip = "debug-only 时序护栏")]
    pub(crate) phase_guard: u8,
}

impl WorldBody {
    pub fn new(seed: u64) -> Self {
        WorldBody {
            frame: 0,
            rng: Pcg32::new(seed, 0xda3e_39cb_94b9_5bdb),
            bullets: BulletPool::new(),
            diag: DiagCounters::default(),
            last_status: STATUS_OK,
            #[cfg(debug_assertions)]
            phase_guard: PH_BEGIN,
        }
    }

    /// PhaseGuard：debug 断言相位保序，乱序 panic；release 零成本。
    #[inline]
    pub(crate) fn phase_enter(&mut self, p: u8) {
        #[cfg(debug_assertions)]
        {
            debug_assert_eq!(self.phase_guard, p, "step 相位乱序：期望 {p}，实到");
            self.phase_guard = (p + 1) % NUM_PHASES;
        }
        let _ = p;
    }

    // ── 写 API（P1：调用方只走这里，不摸池内存）──────────────────────────
    /// 创建一颗弹（P4-a：池满 → NULL + 诊断计数 + last_status）。
    pub fn create_bullet(&mut self, init: BulletInit) -> BulletHandle {
        match self.bullets.alloc(init) {
            Some(h) => h,
            None => {
                self.diag.pool_full[POOL_BULLET] =
                    self.diag.pool_full[POOL_BULLET].wrapping_add(1);
                self.last_status = STATUS_POOL_FULL;
                BulletHandle::NULL
            }
        }
    }

    fn out_of_bounds(x: Fx, y: Fx) -> bool {
        let xi = x.to_int_floor();
        let yi = y.to_int_floor();
        xi < -FIELD_HALF_W - OOB_MARGIN
            || xi > FIELD_HALF_W + OOB_MARGIN
            || yi < -OOB_MARGIN
            || yi > FIELD_HEIGHT + OOB_MARGIN
    }
}
```

- [ ] `lib.rs` 加 `pub mod world;`。
- [ ] 测试（`world.rs` tests）：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::bullets::BulletInit;
    use crate::checksum::Checksum;
    use crate::math::{Angle, Fx};

    fn init0() -> BulletInit {
        BulletInit {
            x: Fx::ZERO, y: Fx::ZERO, vx: Fx::ZERO, vy: Fx::ZERO,
            speed: Fx::ZERO, angle: Angle::ZERO, ang_vel: 0, accel: Fx::ZERO,
            ax: Fx::ZERO, ay: Fx::ZERO, sprite: 0, radius: Fx::from_int(2),
            delay: 0, life: 0xFFFF, flags: 0, grazed_by: 0,
            transform_head: 0xFFFF, xform_wait: 0, xform_next: 0,
        }
    }

    #[test]
    fn create_bullet_ok_and_pool_full() {
        let mut w = WorldBody::new(1);
        let h = w.create_bullet(init0());
        assert_ne!(h, BulletHandle::NULL);
        // 填满 8192 → 下一个 NULL + 计数
        for _ in 0..(BulletPool::CAP - 1) {
            w.create_bullet(init0());
        }
        let over = w.create_bullet(init0());
        assert_eq!(over, BulletHandle::NULL);
        assert_eq!(w.diag.pool_full[POOL_BULLET], 1);
        assert_eq!(w.last_status, STATUS_POOL_FULL);
        assert_ne!(w.checksum(), WorldBody::new(1).checksum()); // 状态变了
    }

    #[test]
    fn new_is_deterministic() {
        assert_eq!(WorldBody::new(42).checksum(), WorldBody::new(42).checksum());
        assert_ne!(WorldBody::new(1).checksum(), WorldBody::new(2).checksum()); // 种子入 rng 入校验和
    }
}
```

- [ ] `cargo test -p stg-core world` 绿；commit `feat(world): WorldBody + create_bullet(P4-a) + PhaseGuard`。

### Task 4: 相位函数 + step 组装（World + step_with_director + 快照）

- [ ] `world.rs` 追加相位函数（`impl WorldBody`）：

```rust
    // ── 相位函数（pub(crate)，每个先 phase_enter 保序）────────────────────
    pub(crate) fn begin(&mut self) {
        self.phase_enter(PH_BEGIN);
        // 最小切片无输出缓冲可清；仅护栏复位（phase_enter 已推进）。
    }
    pub(crate) fn decode_input(&mut self) {
        self.phase_enter(PH_DECODE); // stub：无玩家
    }
    pub(crate) fn update_players(&mut self) {
        self.phase_enter(PH_PLAYERS); // stub
    }
    pub(crate) fn run_transforms(&mut self) {
        self.phase_enter(PH_XFORM); // stub：无变换段池
    }
    pub(crate) fn integrate(&mut self) {
        self.phase_enter(PH_INTEGRATE);
        let nw = self.bullets.alive.len();
        for w in 0..nw {
            let mut bits = self.bullets.alive[w];
            while bits != 0 {
                let i = w * 64 + bits.trailing_zeros() as usize;
                bits &= bits - 1;
                if self.bullets.delay[i] > 0 {
                    self.bullets.delay[i] -= 1; // delay 期不动
                    continue;
                }
                self.bullets.x[i] = self.bullets.x[i] + self.bullets.vx[i];
                self.bullets.y[i] = self.bullets.y[i] + self.bullets.vy[i];
                if self.bullets.life[i] != 0xFFFF && self.bullets.life[i] > 0 {
                    self.bullets.life[i] -= 1;
                }
            }
        }
    }
    pub(crate) fn collide(&mut self) {
        self.phase_enter(PH_COLLIDE); // stub：碰撞 D8 后续
    }
    pub(crate) fn settle(&mut self) {
        self.phase_enter(PH_SETTLE); // stub
    }
    pub(crate) fn cleanup(&mut self) {
        self.phase_enter(PH_CLEANUP);
        let nw = self.bullets.alive.len();
        for w in 0..nw {
            let mut bits = self.bullets.alive[w];
            while bits != 0 {
                let i = w * 64 + bits.trailing_zeros() as usize;
                bits &= bits - 1;
                let dead = (self.bullets.life[i] != 0xFFFF && self.bullets.life[i] == 0)
                    || Self::out_of_bounds(self.bullets.x[i], self.bullets.y[i]);
                if dead {
                    self.bullets.free_index(i);
                }
            }
        }
    }
    pub(crate) fn advance(&mut self) {
        self.phase_enter(PH_ADVANCE);
        self.frame += 1;
    }
```

- [ ] `crates/stg-core/src/step.rs`（组装层）：

```rust
//! 组装层（stg_core::step）—— §3.5/A4 宪法顺序的唯一持有者（P2）。World 定义 + step。

use crate::world::{PH_DIRECTOR, PH_ECL_HOOK, WorldBody};

/// 权威可变状态。M1 起加 `pub tasks: TaskPool`。
#[repr(C)]
#[derive(crate::checksum::Checksum)]
pub struct World {
    pub body: WorldBody,
}

impl World {
    /// 堆分配的新世界（~450KB；M5 并行 env 再上就地初始化）。
    pub fn new(seed: u64) -> Box<World> {
        Box::new(World {
            body: WorldBody::new(seed),
        })
    }

    /// 整块快照（安全逐字段，I7/D11）。
    pub fn copy_into(&self, dst: &mut World) {
        let s = &self.body;
        let d = &mut dst.body;
        d.frame = s.frame;
        d.rng = s.rng;
        s.bullets.copy_into(&mut d.bullets);
        d.diag = s.diag;
        d.last_status = s.last_status;
        #[cfg(debug_assertions)]
        {
            d.phase_guard = s.phase_guard;
        }
    }

    #[inline]
    pub fn checksum(&self) -> u64 {
        crate::checksum::Checksum::checksum(self)
    }
}

/// 空导演 = 纯世界模拟（P2：空租户零次循环）。
pub fn step(world: &mut World) {
    step_with_director(world, |_| {});
}

/// §3.5 宪法顺序（导演槽在 step-3 跑一次）。相位函数由 world 出，顺序由此焊死，PhaseGuard 押运。
pub fn step_with_director<F: FnMut(&mut WorldBody)>(world: &mut World, mut director: F) {
    let b = &mut world.body;
    b.begin(); // 0
    b.decode_input(); // 1
    b.phase_enter(PH_DIRECTOR); // 2：导演槽（护栏在组装层押）
    director(b);
    b.update_players(); // 3
    b.run_transforms(); // 4
    b.integrate(); // 5
    b.collide(); // 6
    b.settle(); // 7
    b.phase_enter(PH_ECL_HOOK); // 8：ECL 事件挂钩槽（M0-4 空）
    b.cleanup(); // 9
    b.advance(); // 10
}
```

- [ ] `lib.rs` 加 `pub mod step;` + `pub use step::{World, step, step_with_director};`。
   注：`phase_enter` 需对组装层可见——把它从 `pub(crate)` 保持即可（step 在同 crate）。相位常量 `PH_*` 已 `pub(crate)`。
- [ ] 测试（`step.rs` tests）：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::bullets::BulletInit;
    use crate::math::{Angle, Fx};

    fn straight(x: i32, y: i32, vx: i32, vy: i32, life: u16) -> BulletInit {
        BulletInit {
            x: Fx::from_int(x), y: Fx::from_int(y),
            vx: Fx::from_int(vx), vy: Fx::from_int(vy),
            speed: Fx::ZERO, angle: Angle::ZERO, ang_vel: 0, accel: Fx::ZERO,
            ax: Fx::ZERO, ay: Fx::ZERO, sprite: 0, radius: Fx::from_int(2),
            delay: 0, life, flags: 0, grazed_by: 0,
            transform_head: 0xFFFF, xform_wait: 0, xform_next: 0,
        }
    }

    #[test]
    fn integrate_moves_and_advances_frame() {
        let mut w = World::new(1);
        let h = w.body.create_bullet(straight(0, 0, 1, 2, 0xFFFF));
        step(&mut w);
        let i = w.body.bullets.get(h).unwrap();
        assert_eq!(w.body.bullets.x[i], Fx::from_int(1));
        assert_eq!(w.body.bullets.y[i], Fx::from_int(2));
        assert_eq!(w.body.frame, 1);
    }

    #[test]
    fn cleanup_frees_out_of_bounds_and_expired() {
        let mut w = World::new(1);
        let h_far = w.body.create_bullet(straight(1000, 0, 0, 0, 0xFFFF)); // 越界
        let h_life = w.body.create_bullet(straight(0, 0, 0, 0, 1)); // 寿命 1
        step(&mut w); // life:1→0(integrate)，cleanup 释放两者
        assert_eq!(w.body.bullets.get(h_far), None);
        assert_eq!(w.body.bullets.get(h_life), None);
    }

    #[test]
    fn deterministic_replay() {
        // 同种子 + 同导演 → 逐帧 checksum 全等
        let run = || {
            let mut w = World::new(7);
            let mut cks = Vec::new();
            for f in 0..50u32 {
                step_with_director(&mut w, |b| {
                    let vx = b.rng.rand_range(5) as i32 - 2;
                    b.create_bullet(straight(0, 0, vx, 3, 100));
                    let _ = f;
                });
                cks.push(w.checksum());
            }
            cks
        };
        assert_eq!(run(), run());
    }

    #[test]
    fn snapshot_restore_roundtrip() {
        let mut w = World::new(3);
        for _ in 0..10 {
            step_with_director(&mut w, |b| {
                b.create_bullet(straight(0, 0, 1, 1, 200));
            });
        }
        let snap_ck = w.checksum();
        let mut snap = World::new(3);
        w.copy_into(&mut snap);
        assert_eq!(snap.checksum(), snap_ck);
        step(&mut w); // 推进 w
        assert_ne!(w.checksum(), snap_ck);
        snap.copy_into(&mut w); // 恢复
        assert_eq!(w.checksum(), snap_ck);
    }

    #[test]
    #[cfg(debug_assertions)]
    #[should_panic(expected = "相位乱序")]
    fn phase_guard_catches_out_of_order() {
        let mut w = World::new(1);
        w.body.advance(); // 不经 begin 直接 advance → 护栏 panic
    }
}
```

- [ ] `cargo test -p stg-core step` 绿；`cargo clippy` 干净；commit `feat(step): World 拼装 + step_with_director + 相位 + 快照`。

### Task 5: harness 纯弹幕金向量（M0-5）+ 设计回写

- [ ] `crates/stg-harness/src/main.rs` 的 `cmd_golden` 换成真实场景：

```rust
fn cmd_golden(rest: &[String]) -> ExitCode {
    use stg_core::bullets::BulletInit;
    use stg_core::math::{Angle, Fx, polar_to_vec};
    use stg_core::step::{World, step_with_director};

    const FRAMES: u32 = 600;
    const SEED: u64 = 0x5147_4f4c_4445_4e00; // "GOLDEN"
    let mut world = World::new(SEED);
    let mut lines = String::new();

    for frame in 0..FRAMES {
        step_with_director(&mut world, |b| {
            // 每帧从中心铺一圈 12 发，基角随帧旋转 + rng 抖动（压 sincos + PCG32）
            let base = (frame.wrapping_mul(797) & 0xFFFF) as u16;
            let n = 12u16;
            for k in 0..n {
                let spread = b.rng.rand_range(512) as u16; // 抖动，消耗 RNG
                let a = Angle(base.wrapping_add(k.wrapping_mul(65536 / n)).wrapping_add(spread));
                let (vx, vy) = polar_to_vec(Fx::from_int(3), a);
                let bi = BulletInit {
                    x: Fx::ZERO, y: Fx::from_int(100), vx, vy,
                    speed: Fx::from_int(3), angle: a, ang_vel: 0, accel: Fx::ZERO,
                    ax: Fx::ZERO, ay: Fx::ZERO, sprite: 0, radius: Fx::from_int(2),
                    delay: 0, life: 200, flags: 0, grazed_by: 0,
                    transform_head: 0xFFFF, xform_wait: 0, xform_next: 0,
                };
                b.create_bullet(bi); // 池满时 P4-a 静默降级（计数入校验和）
            }
        });
        lines.push_str(&format!("{frame} {:016x}\n", world.checksum()));
    }

    match parse_out(rest) {
        Some(path) => {
            if let Err(e) = std::fs::write(&path, lines) {
                eprintln!("error: 写入 {path} 失败: {e}");
                return ExitCode::FAILURE;
            }
            eprintln!("golden: {FRAMES} 帧真实 step 演化校验和已写入 {path}");
        }
        None => print!("{lines}"),
    }
    ExitCode::SUCCESS
}
```

移除 `main.rs` 顶部不再需要的 `use stg_core::checksum::Fnv1a64;`（若无其他用处）。

- [ ] 本地跑：`cargo run -p stg-harness -- golden --out c.txt`，`head/tail` 看逐帧 checksum 稳定输出；重复跑两次 `diff` 应完全相同（本机确定性）。
- [ ] 设计回写：`stg-world-design.md` A4 表后加一行"M0-4 最小切片：实现 begin/导演/integrate/cleanup/advance，余相位 stub；3 输出缓冲随生产者再加"；`CLAUDE.md` M0 条目标注"M0-4/5 已落 World+step+纯弹幕金向量"。
- [ ] commit `feat(harness): 纯弹幕金向量（真实 step 演化）+ 设计回写`。

### Task 6: 收口 + CI（determinism-gate 现测真实演化）+ 合并

- [ ] `cargo test --workspace` / `fmt --check` / `clippy -D warnings` / `verify-tables` 全绿。
- [ ] 推分支 → PR → 三平台 CI 绿——**重点看 `determinism-gate`**：它现在对拍的是**真实 step 演化**（sincos/PCG32/池 churn/integrate/cleanup 全跨平台逐帧一致），是 DoD 机器第一次跑真东西。
- [ ] ff 合并 main、清理分支。

## Self-Review

- P2 覆盖：宪法顺序在组装层 ✓、相位 pub(crate) ✓、PhaseGuard 押运全 11（含乱序 panic 测试）✓、导演槽=闭包 ✓。
- A4：11 相位齐全（5 实现 + 6 stub 均保序）✓；integrate 双表示推迟（注明）；3 输出缓冲推迟（注明）。
- 快照 I7/D11：安全 copy_into（零 unsafe）+ 往返恢复测试 ✓。
- P4-a：create_bullet 池满降级 + 诊断计数入校验和 ✓。
- I3：PCG32 随 World 快照、入校验和、参考向量锁死 ✓。
- 金向量：真实 step 演化压 sincos+PCG32+池 churn+integrate+cleanup，三平台 determinism-gate 对拍 ✓。
- 留后：POLAR/CART 双表示 + 极坐标 setter、玩家/碰撞/结算、3 输出缓冲、XformSegPool、input 参数、World 就地初始化（M5）、快照环（M3）。
