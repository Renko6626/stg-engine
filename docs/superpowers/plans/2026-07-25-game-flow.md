# 整局流程刀 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 spec `docs/superpowers/specs/2026-07-25-game-flow-midstart-design.md`:多文件编译
`compile_units` + 表现锚点四字段与四 builtin + `mark` 中段启动 + `Loadout`/`new_game_at` + 桥面扩展 + 文档收口。

**Architecture:** 编译器多文件 = 双 parse 预检撞名 + 拼接源文本单管线 + 行号边界映射回译
(CompileError 无 file 字段,不动它);`mark` = 语句位前瞻(wait_spell 同款)+ typeck 三规则 +
codegen 落点垫片 + EclImage 内存态标记表(镜像无字节格式,零版本问题);锚点 = WorldBody 四
受检字段 + 5x 族 syscall(写字段+发 req);`new_game_at` = 开机查标记表直接覆写根任务 `pc`
(全局绝对字索引)。

**Tech Stack:** Rust 1.94.0 workspace(stg-core / stg-ecl-compiler / stg-harness / stg-godot),gdext 0.5.4。

## Global Constraints

- **I1-I7 全持有**;新增 WorldBody 字段默认进校验和(P6 无例外,不加 skip);禁 `HashMap`(用 `BTreeMap`)。
- **P4-b**:脚本坏参数 → 确定性安全结果(钳/no-op+`contract_viol` 计数),不 panic;宿主期(开机)错误 → 响亮返回 `Err`。
- **金向量预期**:Task 2 落地后校验和流**整体平移**(新受检字段,A1 同款性质;spec §4 明写)。
  开工前在分支点跑 `cargo run -p stg-harness -- golden --out .superpowers/gameflow/golden-base.txt`
  留档;Task 2 之后各任务**不得再引起平移**(锚字段无脚本写入时恒 0)。行为不变由 Task 2/3 的判别式测试证明。
- **改 `builtins.rs` 必跑** `cargo run -p stg-harness -- gen-ecl-meta`(漂移闸 `committed_meta_matches_generated`
  /`committed_doc_segment_matches_generated` 会抓手改/漏跑)。
- **新语句 `mark` 必须登记 `typeck::checker::RESERVED_SUGAR_NAMES`**(checker.rs:17,漏登记 = 同名 sub/const 被静默架空,C16 踩过)。
- **尺寸哨兵**:WorldBody 加字段后 `step.rs:2023` `world_size_sentinel_guards_copy_into_field_list` 必红——
  按其注释四件套核对(copy_into/checksum/D10/SaveBytes)后以**实测值**更新 EXPECTED,不许手算。
- **机械调整许可**:计划中引用的既有类型/枚举变体名以仓库实际代码为准(如 typed AST 的字面量变体名),
  实现者按实名机械调整并在报告中披露;**语义决策(号位/字段名/规则)不在许可内**。
- commit 尾附:`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- 每任务收尾:`cargo fmt --all` + `cargo clippy --workspace --all-targets -- -D warnings` 干净。

## 文件地图

```
crates/stg-ecl-compiler/src/lang/units.rs   【新】compile_units:预检+拼接+行号回译
crates/stg-ecl-compiler/src/lang/{parse,typeck*,codegen,builtins}.rs + lib.rs   mark 语句/补偿/新 builtin
crates/stg-core/src/ecl/image.rs            marks 标记表(ImageParts+EclImage+校验)
crates/stg-core/src/ecl/{binding,syscall}.rs  TaskStartError 新变体 / 5x 族 syscall
crates/stg-core/src/{world.rs,world/view.rs,player.rs,step.rs,consts.rs,save.rs}  锚点字段/Loadout/new_game_at
crates/stg-harness/src/main.rs              collect_units + check 目录支持 + 端到端测试
crates/stg-godot/src/{boot,bridge}.rs + smoke/  boot_at/new_game_at/anchors 读口
docs/{ecl-lang.md,ecl-ops.md,follow-ups.md} + PROGRESS.md   文档收口
```

---

### Task 1: 多文件编译 `compile_units` + harness 收集

**Files:**
- Create: `crates/stg-ecl-compiler/src/lang/units.rs`
- Modify: `crates/stg-ecl-compiler/src/lang/mod.rs`(挂模块+re-export)
- Modify: `crates/stg-harness/src/main.rs`(`collect_units` + `cmd_check` 目录支持)

**Interfaces:**
- Produces: `lang::compile_units(units: &[(String, String)]) -> Result<EclImage, Vec<(String, CompileError)>>`
  (元组 `.0` = 该错误所属文件名);harness `collect_units(path: &str) -> std::io::Result<Vec<(String, String)>>`。
- 设计事实(探查钉死):`CompileError{line,col,msg,src_line}` **无 file 字段**(ast.rs:231),不改它;
  `attach_src_lines` 假设单一 src(mod.rs:158)。故走**拼接+回译**:预检双 parse 抓跨文件撞名(两处位置),
  通过后拼接文本走既有 `compile()` 单管线,错误行号经边界表回译成 `(文件名, 局部行号)`。

- [ ] **Step 1: 写失败测试**(`units.rs` 尾部 `#[cfg(test)]`):

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn u(name: &str, src: &str) -> (String, String) {
        (name.to_string(), src.to_string())
    }

    #[test]
    fn cross_file_call_compiles_and_entry_resolves() {
        let img = compile_units(&[
            u("main.ecl", "sub main() { helper(); }\n"),
            u("lib.ecl", "sub helper() { wait(1); }\n"),
        ])
        .expect("跨文件同步调用必须编译通过");
        assert!(img.root().is_some(), "main 入口存在");
    }

    #[test]
    fn duplicate_sub_across_files_reports_both_positions() {
        let errs = compile_units(&[
            u("a.ecl", "sub main() { }\nsub foo() { }\n"),
            u("b.ecl", "sub foo() { }\n"),
        ])
        .expect_err("跨文件撞名必须报错");
        let (file, e) = &errs[0];
        assert_eq!(file, "b.ecl", "错误落在后出现的文件");
        assert!(e.msg.contains("foo") && e.msg.contains("a.ecl"), "消息含另一处位置:{}", e.msg);
    }

    #[test]
    fn parse_error_carries_local_line_and_file() {
        let errs = compile_units(&[
            u("ok.ecl", "sub main() { }\n"),
            u("bad.ecl", "sub broken() {\n    wait(1)\n}\n"), // 缺分号,错误在 bad.ecl 第 2 行
        ])
        .expect_err("语法错必须报");
        let (file, e) = &errs[0];
        assert_eq!(file, "bad.ecl");
        assert_eq!(e.line, 2, "行号必须是文件内局部行号,不是拼接后全局行号");
    }

    #[test]
    fn typeck_error_line_translates_back_to_unit() {
        // 类型错在第二个文件:拼接后全局行号必须回译成 lib.ecl 局部行号。
        let errs = compile_units(&[
            u("main.ecl", "sub main() { helper(); }\n"),
            u("lib.ecl", "sub helper() {\n    var x: fx = 1;\n}\n"), // int→fx 需 cast,第 2 行
        ])
        .expect_err("判型错必须报");
        let (file, e) = &errs[0];
        assert_eq!(file, "lib.ecl");
        assert_eq!(e.line, 2);
        assert!(!e.src_line.is_empty(), "src_line 已由管线回填且属于正确文件的文本");
    }

    #[test]
    fn unit_order_does_not_change_image_bytes() {
        let a = [
            u("main.ecl", "sub main() { helper(); }\n"),
            u("lib.ecl", "sub helper() { wait(1); }\n"),
        ];
        let b = [a[1].clone(), a[0].clone()];
        let ia = compile_units(&a).expect("a 序");
        let ib = compile_units(&b).expect("b 序");
        assert_eq!(ia.code(), ib.code(), "canonical 按名排序 ⇒ 收集顺序不改产物");
    }

    #[test]
    fn empty_units_rejected() {
        assert!(compile_units(&[]).is_err(), "空清单必须报错而非 panic");
    }
}
```

- [ ] **Step 2: 跑测试确认红**:`cargo test -p stg-ecl-compiler compile_units` → 编译失败(函数不存在)。

- [ ] **Step 3: 实现 `units.rs`**:

```rust
//! 多文件编译单元(整局流程刀 spec §1)。
//!
//! `CompileError` 没有 file 字段(ast.rs 契约出入,单文件世界的既定形态),本模块
//! 不改它,改"包装":① 每单元独立 parse 预检——语法错天然带局部行号、跨文件撞名
//! 在此层报(两处位置都知道);② 预检通过后按传入序拼接源文本走既有 `compile()`
//! 单管线(typeck/slots/codegen 零改动);③ 出错时把全局行号经边界表回译成
//! (文件名, 局部行号),src_line 已由管线从拼接文本回填、内容正确无需再动。
//! 产物不依赖收集顺序:codegen `build()` 按 sub 名排序出 canonical id(lib.rs)。

use super::{CompileError, compile, parse};
use stg_core::ecl::image::EclImage;

pub fn compile_units(
    units: &[(String, String)],
) -> Result<EclImage, Vec<(String, CompileError)>> {
    if units.is_empty() {
        return Err(vec![(
            String::new(),
            CompileError { line: 0, col: 0, msg: "编译单元清单为空".to_string(), src_line: String::new() },
        )]);
    }
    // ① 每单元独立 parse:语法错直接局部化;收集顶层名字做跨文件撞名预检。
    let mut errs: Vec<(String, CompileError)> = Vec::new();
    let mut seen: std::collections::BTreeMap<String, (usize, u32)> = std::collections::BTreeMap::new();
    for (i, (name, src)) in units.iter().enumerate() {
        match parse(src, name) {
            Ok(prog) => {
                let tops = prog
                    .subs.iter().map(|s| (format!("sub '{}'", s.name), s.span.line))
                    .chain(prog.consts.iter().map(|c| (format!("const '{}'", c.name), c.span.line)))
                    .chain(prog.xformdefs.iter().map(|x| (format!("xformdef '{}'", x.name), x.span.line)));
                for (key, line) in tops {
                    if let Some((pi, pline)) = seen.get(&key) {
                        if *pi != i {
                            errs.push((name.clone(), CompileError {
                                line, col: 1,
                                msg: format!("{key} 跨文件重复定义(另见 {}:{})", units[*pi].0, pline),
                                src_line: src.lines().nth(line.saturating_sub(1) as usize)
                                    .unwrap_or("").to_string(),
                            }));
                        } // 同文件重复留给既有 typeck/entryck,规则不双轨。
                    } else {
                        seen.insert(key, (i, line));
                    }
                }
            }
            Err(pe) => errs.extend(pe.into_iter().map(|e| (name.clone(), e))),
        }
    }
    if !errs.is_empty() {
        return Err(errs);
    }
    // ② 拼接(每单元保证换行收尾)+ 行号边界:bases[i] = 单元 i 之前累计的全局行数,
    //    即单元 i 内局部行 L 对应全局行 bases[i] + L。lines().count() 对 \n 收尾文本
    //    = 真实行数;非 \n 收尾拼接时补一个换行,补的换行不新增行。
    let mut combined = String::new();
    let mut bases: Vec<u32> = Vec::with_capacity(units.len());
    let mut acc: u32 = 0;
    for (_, src) in units {
        bases.push(acc);
        combined.push_str(src);
        if !src.ends_with('\n') {
            combined.push('\n');
        }
        acc += src.lines().count() as u32;
    }
    // ③ 单管线编译 + 行号回译:全局行 L(1-based)属于满足 bases[i] < L <= bases[i]+行数
    //    的单元 i,即 bases 中最后一个 < L 的元素;局部行 = L - bases[i]。
    compile(&combined, "<multi>").map_err(|ce| {
        ce.into_iter()
            .map(|mut e| {
                let idx = bases.partition_point(|&b| b < e.line).saturating_sub(1);
                e.line -= bases[idx];
                (units[idx].0.clone(), e)
            })
            .collect()
    })
}
```

> CompileError 字段全 pub(ast.rs:231-237),可直接结构体字面量构造与改写 `e.line`。
> 边界自检:单元 0 首行 L=1,`partition_point(b < 1)` 对 bases[0]=0 得 1,减 1 → idx=0,
> 局部行 = 1 - 0 = 1 ✔;`e.line == 0`(空清单错误那类)不会走本回译路径。

在 `lang/mod.rs` 挂模块并导出:

```rust
mod units;
pub use units::compile_units;
```

- [ ] **Step 4: 跑测试确认绿**:`cargo test -p stg-ecl-compiler` 全绿(含既有测试无回归)。

- [ ] **Step 5: harness `collect_units` + `cmd_check` 目录支持**。`main.rs` 中 `cmd_check` 附近加:

```rust
/// 目录 → 全部 *.ecl 按文件名字节序;单文件 → 单元素。整局流程刀 spec §1 收集约定。
fn collect_units(path: &str) -> std::io::Result<Vec<(String, String)>> {
    let meta = std::fs::metadata(path)?;
    if meta.is_dir() {
        let mut names: Vec<std::path::PathBuf> = std::fs::read_dir(path)?
            .filter_map(|e| e.ok().map(|e| e.path()))
            .filter(|p| p.extension().is_some_and(|x| x == "ecl"))
            .collect();
        names.sort(); // 路径字节序 = 文件名字节序(同目录)
        names
            .into_iter()
            .map(|p| {
                let name = p.file_name().unwrap().to_string_lossy().into_owned();
                std::fs::read_to_string(&p).map(|s| (name, s))
            })
            .collect()
    } else {
        std::fs::read_to_string(path).map(|s| vec![(path.to_string(), s)])
    }
}
```

`cmd_check` 改为(保持三路退码语义,usage 提示目录):

```rust
fn cmd_check(rest: &[String]) -> ExitCode {
    let Some(path) = rest.first() else {
        eprintln!("usage: stg-harness check <file.ecl|目录>");
        return ExitCode::from(2);
    };
    let units = match collect_units(path) {
        Ok(u) if !u.is_empty() => u,
        Ok(_) => {
            eprintln!("error: {path} 目录下没有 .ecl 文件");
            return ExitCode::from(2);
        }
        Err(e) => {
            eprintln!("error: 读 {path} 失败: {e}");
            return ExitCode::from(2);
        }
    };
    match stg_ecl_compiler::lang::compile_units(&units) {
        Ok(_) => {
            println!("OK");
            ExitCode::SUCCESS
        }
        Err(errors) => {
            for (file, e) in &errors {
                eprintln!("{}", e.render(file));
            }
            ExitCode::from(1)
        }
    }
}
```

- [ ] **Step 6: harness 测试**——仿既有 `check_exit_codes_three_ways`(main.rs:1284)加:

```rust
#[test]
fn check_accepts_directory_of_units() {
    let dir = std::env::temp_dir().join(format!("gameflow_units_{}", std::process::id()));
    std::fs::create_dir_all(&dir).unwrap();
    std::fs::write(dir.join("a_main.ecl"), "sub main() { helper(); }\n").unwrap();
    std::fs::write(dir.join("b_lib.ecl"), "sub helper() { wait(1); }\n").unwrap();
    let units = collect_units(dir.to_str().unwrap()).unwrap();
    assert_eq!(units.len(), 2);
    assert_eq!(units[0].0, "a_main.ecl", "按名排序");
    assert!(stg_ecl_compiler::lang::compile_units(&units).is_ok());
    std::fs::remove_dir_all(&dir).ok();
}
```

- [ ] **Step 7: 全绿 + fmt/clippy + 提交**:

```bash
cargo test -p stg-ecl-compiler -p stg-harness && cargo fmt --all && cargo clippy --workspace --all-targets -- -D warnings
git add crates/stg-ecl-compiler crates/stg-harness
git commit -m "feat(ecl): compile_units 多文件编译——预检撞名两处位置+拼接单管线+行号回译;check 支持目录"
```

---

### Task 2: 表现锚点四字段 + 5x 族 syscall + REQ 常量(stg-core)

**Files:**
- Modify: `crates/stg-core/src/world.rs`(四字段 + `set_bgm/set_bg/set_bg_phase` 写口 + 读口)
- Modify: `crates/stg-core/src/world/view.rs`(四读口)
- Modify: `crates/stg-core/src/step.rs`(copy_into 四行 + 尺寸哨兵实测更新 + `add_score` 归属见 syscall)
- Modify: `crates/stg-core/src/ecl/syscall.rs`(SYS_ADD_SCORE=50/SYS_BGM=51/SYS_BG=52/SYS_BG_PHASE=53)
- Modify: `crates/stg-core/src/consts.rs`(REQ_STAGE_CLEAR=4/REQ_BGM=5/REQ_BG=6/REQ_BG_PHASE=7)
- Modify: `crates/stg-core/src/save.rs`(SAVE_FILE_VER 1→2)

**Interfaces:**
- Produces(Task 3/6/7 依赖):WorldBody 字段 `bgm_id:u16 / bg_id:u16 / bg_phase:u16 / bg_phase_frame:u32`
  (pub(crate) + 读口 `pub fn bgm_id(&self)->u16` 等四个);`WorldView::{bgm_id,bg_id,bg_phase,bg_phase_frame}`;
  syscall 号 50-53(5x = 账面/表现声明族);consts `REQ_STAGE_CLEAR=4, REQ_BGM=5, REQ_BG=6, REQ_BG_PHASE=7`。
- add_score 钳位语义(spec §10-7 拍板):`delta: i32` 允许负值(扣分),`score` 以 i128 中间量加后
  钳 `[0, u64::MAX]`——实现用 `saturating_add`/`saturating_sub` 两分支,无 panic 无回绕。

- [ ] **Step 1: 失败测试先行**(syscall.rs 测试区仿既有 sys 测试写;world.rs/step.rs 测试区):

```rust
// syscall.rs tests —— 通过手工构造 task 栈调 dispatch 的既有模式写四条:
#[test]
fn sys_bgm_writes_field_and_emits_req() {
    // 构造 ctx(仿本文件既有测试基建),压 5,派发 SYS_BGM:
    // 断言 body.bgm_id == 5;take_requests() 末条 id == consts::REQ_BGM 且 args[0] == 5。
}
#[test]
fn sys_bg_phase_stamps_current_frame() {
    // body.frame 预置 42;压 3,派发 SYS_BG_PHASE:
    // 断言 bg_phase == 3 && bg_phase_frame == 42;req id == REQ_BG_PHASE。
}
#[test]
fn sys_anchor_out_of_range_is_noop_with_viol() {
    // 压 -1 派发 SYS_BGM:字段不动、无 req、diag.contract_viol +1、last_status == STATUS_BAD_ARGS
    // (与 sys_emit_req 的 id 收窄同款 P4-b 口径,syscall.rs:622 参照)。
}
#[test]
fn sys_add_score_saturates_at_zero() {
    // players[0].score = 10;压 -100 派发 SYS_ADD_SCORE:score == 0(不回绕);
    // 再压 7:score == 7;正向饱和:score = u64::MAX - 1,压 100 → u64::MAX。
}
```

```rust
// step.rs tests —— 判别式(spec §10-8 拍板形态):
#[test]
fn anchor_fields_are_write_only_for_simulation() {
    // 两个同 seed World,手动把 wa.body 的四锚字段写成 (7,8,9,100),wb 保持 0;
    // 各跑 120 帧空输入 step:逐帧断言除四锚字段外 wa/wb 的 rng、players、bullets 校验和意义上
    // 等价——实现:每帧把 wa 的四锚字段临时抄给 wb 后 assert_eq!(wa.checksum(), wb.checksum()),
    // 再把 wb 的抄回 0。红 = 有相位读了锚字段(它们必须是"仅 builtin 写、仅表现读")。
}
#[test]
fn anchor_fields_default_zero_keeps_golden_quiet() {
    // 新 World 四锚字段全 0;跑 60 帧无脚本 step 后仍全 0(无相位写它们)。
}
```

- [ ] **Step 2: 确认红**(字段/常量不存在 → 编译失败即红)。

- [ ] **Step 3: 实现**。world.rs 字段(插在 `spell_seq`(world.rs:183)之后、`reqs` 缓冲之前):

```rust
    /// 表现锚点(整局流程刀 spec §4):存读档/回滚/中段启动后表现层重同步的世界侧真相。
    /// 仅 5x 族 syscall 写、任何相位不读(判别式测试押运);P6 全量入校验和,facing 先例。
    pub(crate) bgm_id: u16,
    pub(crate) bg_id: u16,
    pub(crate) bg_phase: u16,
    pub(crate) bg_phase_frame: u32,
```

world.rs 写口(挨着 `emit_req`,world.rs:922 附近)+ 读口:

```rust
    /// 5x 族锚点写口(syscall 专用;写字段+发 req 一体,保证"字段可 seek、req 可边沿"双通道)。
    pub(crate) fn set_bgm(&mut self, id: u16) {
        self.bgm_id = id;
        self.emit_req(crate::consts::REQ_BGM, [id as i32, 0, 0, 0, 0, 0]);
    }
    pub(crate) fn set_bg(&mut self, id: u16) {
        self.bg_id = id;
        self.emit_req(crate::consts::REQ_BG, [id as i32, 0, 0, 0, 0, 0]);
    }
    pub(crate) fn set_bg_phase(&mut self, n: u16) {
        self.bg_phase = n;
        self.bg_phase_frame = self.frame;
        self.emit_req(crate::consts::REQ_BG_PHASE, [n as i32, 0, 0, 0, 0, 0]);
    }
    pub fn bgm_id(&self) -> u16 { self.bgm_id }
    pub fn bg_id(&self) -> u16 { self.bg_id }
    pub fn bg_phase(&self) -> u16 { self.bg_phase }
    pub fn bg_phase_frame(&self) -> u32 { self.bg_phase_frame }
```

view.rs 四读口(既有单行模式):

```rust
    pub fn bgm_id(self) -> u16 { self.body.bgm_id }
    pub fn bg_id(self) -> u16 { self.body.bg_id }
    pub fn bg_phase(self) -> u16 { self.body.bg_phase }
    pub fn bg_phase_frame(self) -> u32 { self.body.bg_phase_frame }
```

consts.rs `structural` 段追加(1..=63 引擎保留段,现占 1/2/3):

```rust
    REQ_STAGE_CLEAR: u16 = 4;
    REQ_BGM:         u16 = 5;
    REQ_BG:          u16 = 6;
    REQ_BG_PHASE:    u16 = 7;
```

syscall.rs:号常量(新开 5x 族,段注释"5x 账面/表现声明族")+ dispatch 四臂 + 实现:

```rust
/// 1 参:`delta`(允许负,饱和钳 [0, u64::MAX],P4-b)。
pub const SYS_ADD_SCORE: u16 = 50;
/// 1 参:`id`(0..=65535 收窄,越界 no-op+viol)。写 bgm_id + 发 REQ_BGM。
pub const SYS_BGM: u16 = 51;
/// 同上,写 bg_id + REQ_BG。
pub const SYS_BG: u16 = 52;
/// 1 参:`n`。写 bg_phase + 自动盖 bg_phase_frame=当前帧 + REQ_BG_PHASE。
pub const SYS_BG_PHASE: u16 = 53;
```

```rust
        SYS_ADD_SCORE => {
            let d = pop(task)?;
            let s = &mut ctx.body.players[0].score;
            *s = if d >= 0 { s.saturating_add(d as u64) } else { s.saturating_sub(d.unsigned_abs() as u64) };
            Ok(())
        }
        SYS_BGM => sys_anchor_u16(task, ctx, AnchorKind::Bgm),
        SYS_BG => sys_anchor_u16(task, ctx, AnchorKind::Bg),
        SYS_BG_PHASE => sys_anchor_u16(task, ctx, AnchorKind::BgPhase),
```

```rust
enum AnchorKind { Bgm, Bg, BgPhase }

/// 三锚共用:弹 1 参收窄 u16(越界 = P4-b no-op+viol,sys_emit_req 同口径),再走 world 写口。
fn sys_anchor_u16(task: &mut Task, ctx: &mut VmCtx, kind: AnchorKind) -> Result<(), u8> {
    let v = pop(task)?;
    if !(0..=u16::MAX as i32).contains(&v) {
        ctx.body.diag.contract_viol = ctx.body.diag.contract_viol.wrapping_add(1);
        ctx.body.last_status = crate::world::STATUS_BAD_ARGS;
        return Ok(());
    }
    match kind {
        AnchorKind::Bgm => ctx.body.set_bgm(v as u16),
        AnchorKind::Bg => ctx.body.set_bg(v as u16),
        AnchorKind::BgPhase => ctx.body.set_bg_phase(v as u16),
    }
    Ok(())
}
```

save.rs:`pub(crate) const SAVE_FILE_VER: u8 = 2;`(load 侧版本不匹配已有拒绝路径,确认其测试跟着改字面量——
仿 tables.rs 前置刀经验,拒绝测试用 `SAVE_FILE_VER + 1` 相对化,若现值是硬编码字面量一并修)。

step.rs `copy_into`(step.rs:95-130)加四行:

```rust
        d.bgm_id = s.bgm_id;
        d.bg_id = s.bg_id;
        d.bg_phase = s.bg_phase;
        d.bg_phase_frame = s.bg_phase_frame;
```

- [ ] **Step 4: 尺寸哨兵**:跑 `cargo test -p stg-core world_size_sentinel` → 红,按注释四件套核对
  (copy_into 已加/checksum 默认入/SaveBytes 自动/D10 文档账),用报错输出的**实测值**更新 EXPECTED 两元组。
- [ ] **Step 5: 全绿**:`cargo test -p stg-core`;跑 `cargo run -p stg-harness -- golden --out .superpowers/gameflow/golden-task2.txt`,
  与 golden-base.txt diff:**每帧都变(平移)、行数与帧号列不变**——留档进 `.superpowers/gameflow/`。
- [ ] **Step 6: 提交**:`git add crates/stg-core .superpowers/gameflow && git commit -m "feat(core): 表现锚点四字段+5x族syscall(add_score/bgm/bg/bg_phase)+REQ常量4-7;SAVE_FILE_VER=2"`

---

### Task 3: 四 builtin + gen-ecl-meta 重跑 + 端到端锚点测试

**Files:**
- Modify: `crates/stg-ecl-compiler/src/lang/builtins.rs`(4 条目)
- Regenerate: `editors/vscode/stg-ecl/ecl-meta.json` + `docs/ecl-lang.md` 生成段(命令产出,勿手改)
- Modify: `crates/stg-harness/src/main.rs`(端到端测试)

**Interfaces:**
- Consumes: Task 2 的 SYS_ADD_SCORE/SYS_BGM/SYS_BG/SYS_BG_PHASE。
- Produces: `.ecl` 可写 `add_score(d)` / `bgm(id)` / `bg(id)` / `bg_phase(n)`(全 void 裸语句);
  Task 5 的补偿扫描按名字 `"bgm"/"bg"/"bg_phase"` 识别这三个 builtin。

- [ ] **Step 1: builtins.rs 加 4 条**(插在 `spell_timer` 之后,穷举测试 `lookup_finds_every_documented_builtin_by_name` 会自动覆盖):

```rust
    Builtin {
        name: "add_score",
        syscall: syscall::SYS_ADD_SCORE,
        is_op: false,
        params: &[Val(Int)],
        ret: None,
        doc: "给自机记分:delta 允许负(扣分),饱和钳 [0,u64::MAX] 不回绕;关底 bonus/结算记账用",
        param_names: &["delta"],
    },
    Builtin {
        name: "bgm",
        syscall: syscall::SYS_BGM,
        is_op: false,
        params: &[Val(Int)],
        ret: None,
        doc: "声明当前 BGM:写世界锚点字段 bgm_id 并发 REQ_BGM;mark 跳入自动补偿最近声明(常量参)",
        param_names: &["id"],
    },
    Builtin {
        name: "bg",
        syscall: syscall::SYS_BG,
        is_op: false,
        params: &[Val(Int)],
        ret: None,
        doc: "声明当前背景:写锚点 bg_id 并发 REQ_BG;换背景隐含新的 phase 纪元(补偿细则见 ecl-lang)",
        param_names: &["id"],
    },
    Builtin {
        name: "bg_phase",
        syscall: syscall::SYS_BG_PHASE,
        is_op: false,
        params: &[Val(Int)],
        ret: None,
        doc: "声明背景演出段号:写 bg_phase 并自动盖 bg_phase_frame=当前帧,发 REQ_BG_PHASE;表现层按段内局部时间 seek",
        param_names: &["phase"],
    },
```

- [ ] **Step 2: 重跑生成链**:`cargo run -p stg-harness -- gen-ecl-meta`;`git diff` 确认 ecl-meta.json 与
  ecl-lang.md 生成段出现四条新签名。
- [ ] **Step 3: 端到端测试**(harness main.rs tests,仿 item_sprite 骨架):

```rust
#[test]
fn anchor_builtins_write_fields_and_reqs_via_script() {
    const SRC: &str = r#"
sub main() {
    bgm(5);
    bg(2);
    bg_phase(1);
    add_score(1000);
    add_score(-2000);
    loop { wait(60); }
}
"#;
    let image = stg_ecl_compiler::lang::compile(SRC, "anchors.ecl").expect("编译");
    let mut w = stg_core::step::World::new_game(7, 2, &image).expect("boot");
    let t = &stg_core::tables::TABLES_V0;
    let input = stg_core::input::InputFrame::default();
    stg_core::step::step(&mut w, t, &image, &input); // 出生帧不跑
    stg_core::step::step(&mut w, t, &image, &input); // 第 2 步 main 执行
    let v = w.body.view();
    assert_eq!(v.bgm_id(), 5);
    assert_eq!(v.bg_id(), 2);
    assert_eq!(v.bg_phase(), 1);
    assert_eq!(v.bg_phase_frame(), 1, "phase 帧戳 = 声明发生帧");
    assert_eq!(v.players()[0].score, 0, "1000-2000 饱和钳 0");
}
```

> `w.body.view()` 若无此口,用 WorldView 的实际构造口(view 模块的既有入口,机械调整许可);
> 断言值若因 born_frame 语义帧号差 1,以打印实测为准修正**注释**而非放宽断言。

- [ ] **Step 4: 全绿 + 漂移闸自证**:`cargo test --workspace`(含 committed_meta/doc 两闸)。
- [ ] **Step 5: 提交**:`git add -A && git commit -m "feat(ecl): add_score/bgm/bg/bg_phase 四builtin+元数据重生成+端到端锚点判别测试"`

---

### Task 4: `mark` 语句(语言层)+ EclImage 标记表

**Files:**
- Modify: `crates/stg-core/src/ecl/image.rs`(ImageParts.marks + EclImage.marks + resolve_mark + 校验)
- Modify: `crates/stg-ecl-compiler/src/lang/{ast.rs,parse.rs}`(Stmt::Mark + 语句位前瞻)
- Modify: `crates/stg-ecl-compiler/src/lang/typeck*`(RESERVED_SUGAR_NAMES + 三规则 + TypedStmt::Mark)
- Modify: `crates/stg-ecl-compiler/src/{lib.rs,lang/codegen.rs}`(SubBuilder.marks + 垫片降低 + build 汇总)

**Interfaces:**
- Produces: `.ecl` 语法 `mark(<编译期常量int,>0>);` 与 `mark(C) { <补偿块> }`,仅 `sub main` 顶层;
  `EclImage::resolve_mark(id: i32) -> Option<u32>`(全局绝对字索引,Task 6 消费);
  `ImageParts.marks: Vec<(i32, u32)>`(按 id 严格升序,builder 排好)。
- 降低形态(spec §2.1):`JMP after; landing: <Task5 注入><作者块>; after:`——正常流一跳跨过。

- [ ] **Step 1: image.rs 失败测试**:

```rust
#[test]
fn resolve_mark_finds_registered_ip() {
    // 手工 ImageParts(仿本文件既有构造测试)加 marks: vec![(3, ip)],ip 指向合法指令边界;
    // try_from_parts 后 resolve_mark(3) == Some(ip);resolve_mark(4) == None;resolve_mark(0) == None。
}
#[test]
fn marks_must_be_sorted_and_in_bounds() {
    // marks: vec![(5,0),(3,0)] → Err(MarksNotStrictlySorted);
    // marks: vec![(3, code_len as u32 + 10)] → Err(MarkIpOutOfBounds)。
}
```

- [ ] **Step 2: image.rs 实现**:`ImageParts` 加 `pub marks: Vec<(i32, u32)>`(既有构造处全部补 `marks: vec![]`);
  `EclImage` 加 `marks: Box<[(i32, u32)]>`;`try_from_parts` 校验(严格升序、id>0、ip < code.len())并存;
  `ImageBuildError` 加 `MarksNotStrictlySorted` / `MarkIpOutOfBounds` / `MarkIdNonPositive`;

```rust
    /// 中段启动标记表:id → main 内落点(全局绝对字索引)。整局流程刀 spec §2。
    pub fn resolve_mark(&self, id: i32) -> Option<u32> {
        self.marks.binary_search_by_key(&id, |m| m.0).ok().map(|i| self.marks[i].1)
    }
```

- [ ] **Step 3: 编译器失败测试**(mod.rs 测试区,消息子串断言仿 `expect_compile_err` 模式):

```rust
#[test]
fn mark_compiles_in_main_top_level() {
    let img = compile("const M: int = 3;\nsub main() { wait(1); mark(M); wait(1); }", "m.ecl").expect("合法 mark");
    assert!(img.resolve_mark(3).is_some());
}
#[test]
fn mark_with_block_compiles() {
    compile("sub main() { mark(1) { bgm(2); } wait(1); }", "m.ecl").expect("带补偿块");
}
#[test]
fn mark_outside_main_rejected() {
    let e = expect_compile_err("sub main() { s(); }\nsub s() { mark(1); }", "m.ecl");
    assert!(e.iter().any(|e| e.msg.contains("mark") && e.msg.contains("main")));
}
#[test]
fn mark_nested_in_block_rejected() {
    let e = expect_compile_err("sub main() { if (1) { mark(1); } }", "m.ecl");
    assert!(e.iter().any(|e| e.msg.contains("顶层")));
}
#[test]
fn mark_id_zero_or_dup_or_nonconst_rejected() {
    assert!(expect_compile_err("sub main() { mark(0); }", "m.ecl").iter().any(|e| e.msg.contains("正整数")));
    assert!(expect_compile_err("sub main() { mark(1); wait(1); mark(1); }", "m.ecl").iter().any(|e| e.msg.contains("重复")));
    assert!(expect_compile_err("sub main() { var x: int = 1; mark(x); }", "m.ecl").iter().any(|e| e.msg.contains("常量")));
}
#[test]
fn var_before_mark_rejected() {
    let e = expect_compile_err("sub main() { var x: int = 1; mark(2); }", "m.ecl");
    assert!(e.iter().any(|e| e.msg.contains("var") && e.msg.contains("mark")));
}
#[test]
fn normal_flow_skips_landing_pad() {
    // mark 块里 bgm(9),正常 new_game(start=0)跑 2 步:bgm_id 仍 0(垫片被 JMP 跨过)。
    // 本测试需要 stg-core World——放 harness(Task 6 的端到端组里也可,若放这里则用
    // 手工断言 code 序列:resolve_mark 落点前一条指令是 OP_JMP 且目标 > 落点)。
}
```

- [ ] **Step 4: 语言层实现**。
  ① ast.rs:`Stmt::Mark { id: Expr, block: Option<Block>, span: Span }`;
  ② parse.rs 语句位前瞻(仿 wait_spell,parse.rs:411-419):

```rust
            TokenKind::Ident(ref name)
                if name == "mark" && self.peek_kind_at(1) == Some(&TokenKind::LParen) =>
            {
                self.parse_mark_stmt()
            }
```

  `parse_mark_stmt`:吃 `mark ( <expr> )`,后随 `{` 则 `parse_block` 收补偿块,否则期待 `;`。
  ③ checker.rs `RESERVED_SUGAR_NAMES` 加 `"mark"`;
  ④ typeck:`validate_marks(prog, consts)` 独立函数(typeck::check 入口调用,错误并入):遍历全部 sub——
  非 main 任意位置出现 Mark → "mark 只能出现在 main 顶层";main 顶层线性扫:嵌套块内 Mark(递归查)→
  "mark 只能在 main 顶层语句位(不进 if/while/for/loop 块)";顶层 `var` 出现后再遇 Mark →
  "main 顶层 var 声明不得先于 mark(任务帧零初始化,跳入后局部为 0)";id 经 `const_eval::evaluate`
  求值(失败 → "mark 编号必须是编译期常量");值 <=0 → "mark 编号必须是正整数";BTreeMap 查重 →
  "mark 编号 N 重复"。`check_stmt` 遇 `Stmt::Mark`:求值 id、typecheck 块语句,产出
  `TypedStmt::Mark { id: i32, body: Vec<TypedStmt> }`(typed_ast.rs 加变体)。
  ⑤ lib.rs `SubBuilder` 加 `pub(crate) marks: Vec<(i32, usize)>` 与
  `pub(crate) fn mark_here(&mut self, id: i32) { let h = self.here(); self.marks.push((id, h)); }`;
  `ImageBuilder::build` 在 bases 定型后汇总 `base + local` → 全局 ip,按 id 排序,塞 `parts.marks`
  (id 重复在编译期已拒,这里 debug_assert);
  ⑥ codegen `gen_block` 的 `TypedStmt::Mark` 臂:

```rust
            TypedStmt::Mark { id, body } => {
                let skip = b.raw_jmp();          // 正常流跨过垫片
                b.mark_here(*id);                 // 落点 = 垫片首指令
                // Task 5 在此前注入补偿;本任务先只生成作者块:
                gen_block(body, b, ...)?;         // 沿用当前 gen_block 的参数形态
                let after = b.here();
                b.patch(skip, after);
            }
```

- [ ] **Step 5: 全绿**:`cargo test -p stg-core -p stg-ecl-compiler`;确认金向量无平移
  (`golden --out` 与 golden-task2.txt 逐字节同——rainbow 无 mark、锚字段无写)。
- [ ] **Step 6: 提交**:`git commit -m "feat(ecl): mark 语句——三编译规则+落点垫片+EclImage 标记表 resolve_mark"`

---

### Task 5: mark 自动补偿(最近声明注入)

**Files:**
- Modify: `crates/stg-ecl-compiler/src/lang/codegen.rs`(扫描 + 注入)

**Interfaces:**
- Consumes: Task 3 builtin 名 `"bgm"/"bg"/"bg_phase"`;Task 4 的 `TypedStmt::Mark` 与垫片降低点。
- 规则(spec §5):main 同步调用链**顶层线性**展开(visited 防环;不进 if/while/for/loop 块、
  不进 spawn/fire);每 mark 取其前最近 bgm/bg/bg_phase 声明各一;**只捕获常量参**声明
  (ConstRef/整型字面量;变量参跳过——垫片处不可重求值);`bg_phase` 声明位置早于最近 `bg` 者不注入
  (旧背景的段号);作者块顶层已手写同名 builtin 则该类不注入;注入在作者块**之前**,
  形态 = `push_i(值); sys(SYS_*)` 直发。

- [ ] **Step 1: 失败测试**(harness main.rs,端到端最有说服力;编译器侧再补 code 形状断言可选):

```rust
#[test]
fn mark_injects_nearest_anchor_declarations() {
    const SRC: &str = r#"
sub stage1() { bgm(11); bg(21); bg_phase(1); wait(1); }
sub main() {
    stage1();
    mark(7);
    loop { wait(60); }
}
"#;
    let image = stg_ecl_compiler::lang::compile(SRC, "comp.ecl").expect("编译");
    // 正常流(start=0):跳过垫片,锚点由 stage1 正常写。
    // 中段启动(start=7):stage1 整个被跳过,垫片自动补 bgm/bg/bg_phase。
    let mut w = boot_at_for_test(&image, 7); // Task 6 提供 new_game_at;本测试在 Task 6 后解锁,
    // —— 若 Task 5 先行,退而断言字节码:resolve_mark(7) 落点起始序列含 PUSH 11/SYS_BGM 等
    // (用 image.code() 与 ops 常量直接比对,三组 push+sys 共 6 条指令后接作者块/空)。
    let t = &stg_core::tables::TABLES_V0;
    let input = stg_core::input::InputFrame::default();
    stg_core::step::step(&mut w, t, &image, &input);
    stg_core::step::step(&mut w, t, &image, &input);
    let v = w.body.view();
    assert_eq!((v.bgm_id(), v.bg_id(), v.bg_phase()), (11, 21, 1));
}
#[test]
fn mark_manual_override_suppresses_injection_per_kind() {
    // stage1 声明 bgm(11)/bg(21);mark 块手写 bgm(99) → 跳入后 bgm==99(不被注入覆盖)、bg==21(仍注入)。
}
#[test]
fn stale_bg_phase_older_than_bg_not_injected() {
    // 顺序:bg_phase(5); bg(30); mark(1);——跳入后 bg==30、bg_phase==0(旧背景段号不注入)。
}
```

> 依赖顺序说明:本任务的端到端断言需要 Task 6 的 `new_game_at`。**执行顺序保持 4→5→6**,
> Task 5 先用"字节码形状断言"(image.code() 里垫片区含三组 `PUSH v / OP_SYS SYS_*`)落住红绿环,
> Task 6 完成后在其步骤里把上述端到端三条补齐激活(Task 6 Step 4 明确包含此项)。

- [ ] **Step 2: 实现扫描**(codegen.rs,`generate` 第二遍前算一次):

```rust
/// mark 补偿扫描(spec §5):沿 main 同步调用链顶层线性走,记录三类锚点声明的
/// (值, 单调位置);遇 Mark 快照"其前最近"。只认常量参;bg_phase 位置早于 bg 者弃。
struct AnchorComp { bgm: Option<i32>, bg: Option<i32>, bg_phase: Option<i32> }

fn scan_mark_compensation(ti: &TypedInfo) -> std::collections::BTreeMap<i32, AnchorComp> {
    let by_name: std::collections::BTreeMap<&str, &TypedSub> =
        ti.subs.iter().map(|s| (s.name.as_str(), s)).collect();
    let mut out = std::collections::BTreeMap::new();
    let mut pos = 0u32;
    let mut latest: [(Option<(i32, u32)>); 3] = [None, None, None]; // bgm/bg/bg_phase → (值,位置)
    let mut visited = std::collections::BTreeSet::new();
    let Some(main) = by_name.get("main") else { return out };
    walk(main, &by_name, &mut visited, &mut pos, &mut latest, &mut out, true);
    return out;

    fn walk(/* sub, by_name, visited, pos, latest, out, is_main */) {
        // 对 sub.body 顶层语句逐条:
        //  - 锚点 builtin 裸调用(名字 ∈ {bgm,bg,bg_phase})且 arg0 是常量
        //    (TypedExprKind 的 ConstRef/整型字面量变体,实名机械调整) → *pos+=1,latest[k]=(v,*pos);
        //  - 同步 sub 调用(callee 非 builtin、非 spawn/fire) → visited 防环后递归 walk(callee, is_main=false);
        //  - TypedStmt::Mark{id,body}(仅 main 顶层会出现) → 快照:
        //      bgm/bg 直取 latest;bg_phase 仅当 latest[phase].pos > latest[bg].pos(bg 缺省视 0)才取;
        //      再按 body 顶层手写的锚点调用名逐类抑制;out.insert(id, comp);
        //  - 其它语句(if/while/for/loop/wait/spawn/…) → 不下潜,跳过。
    }
}
```

注入(Task 4 的 Mark 臂里、`gen_block(body)` 之前):

```rust
                if let Some(c) = comp.get(id) {
                    for (v, sysno) in [
                        (c.bgm, syscall::SYS_BGM),
                        (c.bg, syscall::SYS_BG),
                        (c.bg_phase, syscall::SYS_BG_PHASE),
                    ] {
                        if let Some(v) = v {
                            b.push_i(v);
                            b.sys(sysno);
                        }
                    }
                }
```

(`push_i`/`sys` 为 lib.rs 既有 builder 口;`comp` 由 `generate` 算好后穿参进 `gen_block` 所在调用链,
按现有参数传递形态加一个 `&BTreeMap<i32, AnchorComp>`。)

- [ ] **Step 3: 绿 + 全套无回归**:`cargo test --workspace`。
- [ ] **Step 4: 提交**:`git commit -m "feat(ecl): mark 垫片自动补偿——同步链最近 bgm/bg/bg_phase 常量声明注入+逐类手写抑制"`

---

### Task 6: `Loadout` + `new_game_at`(核心开机面)

**Files:**
- Modify: `crates/stg-core/src/player.rs`(Loadout 结构 + spawn 默认收编)
- Modify: `crates/stg-core/src/step.rs`(new_game_at + new_game 委托)
- Modify: `crates/stg-core/src/ecl/binding.rs`(TaskStartError 两新变体)
- Modify: `crates/stg-harness/src/main.rs`(端到端中段启动测试 + 激活 Task 5 三条端到端)

**Interfaces:**
- Produces:

```rust
// player.rs
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct Loadout { pub character: u8, pub power: u16, pub lives: u8, pub bombs: u8 }
impl Default for Loadout {
    /// 正典默认 = 机体0/0火力/3残/3雷——PlayerState::spawn 的硬编码收编为此单一来源。
    fn default() -> Self { Loadout { character: 0, power: 0, lives: 3, bombs: 3 } }
}
// step.rs
pub fn new_game_at(seed: u64, rank: i32, start: i32, loadout: Loadout, image: &EclImage)
    -> Result<Box<World>, TaskStartError>
// binding.rs
TaskStartError::UnknownMark(i32) | TaskStartError::InvalidCharacter(u8)
```

- 钳位拍板:`power` → `min(crate::items::POWER_MAX)`;`lives/bombs` u8 全域即合法域(引擎无上限
  常量,不造;文档注明);`character >= tables.characters.len()` → `Err(InvalidCharacter)`(宿主期响亮);
  `start != 0` 且 `resolve_mark(start).is_none()` → `Err(UnknownMark(start))`;`start` 负值天然 UnknownMark
  (标记表 id 恒正)。回放/握手身份自此 = `(seed, rank, start, loadout, image_hash)`(spec §0)。

- [ ] **Step 1: 失败测试**(step.rs 测试区;镜像用**手工 ImageParts**——stg-core 不依赖编译器,
  仿 binding.rs 既有测试的 image 构造基建,marks 表手填):

```rust
#[test]
fn new_game_delegates_bitwise_to_default_path() {
    // 同 seed/rank/image:new_game(...) 与 new_game_at(..., 0, Loadout::default(), ...)
    // 逐位一致(checksum 相等)——委托改造零行为差。
}
#[test]
fn new_game_at_applies_loadout_with_clamp() {
    // Loadout{character:0, power: 9999, lives: 8, bombs: 1}:
    // players[0].power == POWER_MAX(钳)、lives == 8、bombs == 1;score/graze == 0。
}
#[test]
fn new_game_at_unknown_mark_and_bad_character_fail_loud() {
    // start=42 无此标记 → Err(UnknownMark(42));character=9 → Err(InvalidCharacter(9));
    // 两者都必须发生在任何任务落池之前(失败后世界不可半初始化——直接返回 Err 丢弃)。
}
#[test]
fn new_game_at_moves_root_pc_to_mark_landing() {
    // 手工镜像:main 的 code_entry=E,marks=[(7, L)](L 为镜像内合法指令);
    // new_game_at(start=7):根任务(tasks 池 0 号)pc == L 而非 E。
}
```

- [ ] **Step 2: 实现**。player.rs:`Loadout` 如上;`PlayerState::spawn` 的 70-72 行改为取
  `Loadout::default()`(`let ld = Loadout::default(); … power: ld.power, lives: ld.lives, bombs: ld.bombs,`)。
  binding.rs:`TaskStartError` 加两变体。step.rs:

```rust
    /// 正典开机完全体(整局流程刀 spec §2.2/§3):回放/握手身份 = (seed, rank, start, loadout, image_hash)。
    /// start=0 从头;非 0 查标记表把根任务 pc 直接搁到 main 内落点(landing pad,编译器保证合法指令边界)。
    /// 中段启动是"规范态"开局(符卡练习语义):被跳过流程的世界效果由脚本 mark 块+自动补偿承担。
    pub fn new_game_at(
        seed: u64,
        rank: i32,
        start: i32,
        loadout: Loadout,
        image: &crate::ecl::image::EclImage,
    ) -> Result<Box<World>, crate::ecl::binding::TaskStartError> {
        use crate::ecl::binding::TaskStartError;
        let tables = &crate::tables::TABLES_V0;
        if loadout.character as usize >= tables.characters.len() {
            return Err(TaskStartError::InvalidCharacter(loadout.character));
        }
        let landing = if start != 0 {
            Some(image.resolve_mark(start).ok_or(TaskStartError::UnknownMark(start))?)
        } else {
            None
        };
        let mut w = World::new(seed);
        w.body.players[0] =
            crate::player::PlayerState::spawn(loadout.character, &tables.characters[loadout.character as usize]);
        let p = &mut w.body.players[0];
        p.power = loadout.power.min(crate::items::POWER_MAX);
        p.lives = loadout.lives;
        p.bombs = loadout.bombs;
        w.body.set_var(crate::consts::GVAR_RANK, rank);
        let root_idx = w.start_main(image)?;
        if let Some(ip) = landing {
            w.tasks.slots[root_idx as usize].pc = ip;
        }
        Ok(w)
    }

    pub fn new_game(
        seed: u64,
        rank: i32,
        image: &crate::ecl::image::EclImage,
    ) -> Result<Box<World>, crate::ecl::binding::TaskStartError> {
        Self::new_game_at(seed, rank, 0, crate::player::Loadout::default(), image)
    }
```

  (`set_var(GVAR_RANK, rank)` 注意既有 new_game 里 GVAR_RANK 是 `u16` 常量、set_var 收 `u16` 槽号——
  与现实现保持同形;rank 值照旧直写。)

- [ ] **Step 3: 端到端**(harness):

```rust
#[test]
fn midstart_skips_earlier_stages_deterministically() {
    const SRC: &str = r#"
const M2: int = 2;
sub stage1() { bgm(1); wait(120); }
sub stage2() { bgm(2); wait(120); }
sub main() {
    stage1();
    mark(M2);
    stage2();
    loop { wait(60); }
}
"#;
    let image = stg_ecl_compiler::lang::compile(SRC, "mid.ecl").expect("编译");
    let t = &stg_core::tables::TABLES_V0;
    let input = stg_core::input::InputFrame::default();
    // 从头:第 2 步后 bgm==1(stage1 正常声明,垫片被跨过)。
    let mut w0 = stg_core::step::World::new_game(7, 2, &image).expect("boot0");
    stg_core::step::step(&mut w0, t, &image, &input);
    stg_core::step::step(&mut w0, t, &image, &input);
    assert_eq!(w0.body.view().bgm_id(), 1);
    // 中段:start=2 → stage1 整跳,垫片补偿 bgm(1) 后 stage2 立即执行覆写为 2。
    let mut w2 = stg_core::step::World::new_game_at(7, 2, 2, Default::default(), &image).expect("boot2");
    stg_core::step::step(&mut w2, t, &image, &input);
    stg_core::step::step(&mut w2, t, &image, &input);
    assert_eq!(w2.body.view().bgm_id(), 2, "跳入后 stage2 的声明生效(同帧覆写补偿值)");
    // 确定性:同参中段启动两次,120 帧逐帧校验和一致。
    let mut wa = stg_core::step::World::new_game_at(7, 2, 2, Default::default(), &image).unwrap();
    let mut wb = stg_core::step::World::new_game_at(7, 2, 2, Default::default(), &image).unwrap();
    for f in 0..120 {
        stg_core::step::step(&mut wa, t, &image, &input);
        stg_core::step::step(&mut wb, t, &image, &input);
        assert_eq!(wa.checksum(), wb.checksum(), "frame {f}");
    }
}
```

- [ ] **Step 4: 激活 Task 5 的三条端到端断言**(把字节码形状断言升级/补齐为 new_game_at 驱动的
  `mark_injects_nearest_anchor_declarations` / `mark_manual_override…` / `stale_bg_phase…`,代码见 Task 5 Step 1)。
- [ ] **Step 5: 全绿 + golden 不再平移**(与 golden-task2.txt 逐字节同)。
- [ ] **Step 6: 提交**:`git commit -m "feat(core): Loadout+new_game_at——中段启动查标记表搁pc+装备钳位+UnknownMark/InvalidCharacter"`

---

### Task 7: 桥面扩展(stg-godot)

**Files:**
- Modify: `crates/stg-godot/src/boot.rs`(`boot_at` 多单元完全体,`boot` 委托)
- Modify: `crates/stg-godot/src/bridge.rs`(`new_game_at` #[func] + `anchors` 读口)
- Modify: `crates/stg-godot/smoke/smoke.gd` + `smoke/godot_smoke.ecl`(冒烟覆盖新面)

**Interfaces:**
- Produces:

```rust
// boot.rs(纯 Rust,可测)
pub fn boot_at(
    units: &[(String, String)],
    seed: u64, rank: i32, start: i32,
    loadout: stg_core::player::Loadout,
) -> Result<Game, BootError>   // compile_units + new_game_at;错误 render 带各自文件名
pub fn boot(ecl_source: &str, seed: u64, rank: i32) -> Result<Game, BootError>
// = boot_at(&[("bridge.ecl", src)], seed, rank, 0, Loadout::default()) 委托,现有调用方零改
```

```gdscript
# bridge #[func](gdext 标量一律 i64 惯例;多文件从 GDScript 传两平行数组)
new_game_at(names: PackedStringArray, sources: PackedStringArray,
            seed: i64, rank: i64, start: i64,
            character: i64, power: i64, lives: i64, bombs: i64) -> bool
anchors() -> Dictionary   # {"bgm": int, "bg": int, "bg_phase": int, "bg_phase_frame": int}
```

- 面变更记账口径(spec §3):v1 13 方法冻结面**扩两口**(new_game_at/anchors),C17 打磨账不动;
  `names.len() != sources.len()` → godot_error + false(壳层护栏,P4 口径)。
- 标量收窄:`character/power/lives/bombs` 从 i64 收窄用 `clamp` 到各自域(`0..=u8::MAX` 等)再 as——
  壳层钳位,核内还有 POWER_MAX 钳(双保险,无 panic)。

- [ ] **Step 1: boot.rs 失败测试**(纯 Rust,仿既有两条):

```rust
#[test]
fn boot_at_multi_unit_midstart() {
    let units = vec![
        ("main.ecl".to_string(), "sub main() { stage1(); mark(2); loop { wait(60); } }\n".to_string()),
        ("s1.ecl".to_string(), "sub stage1() { bgm(1); wait(120); }\n".to_string()),
    ];
    let g = boot_at(&units, 7, 2, 2, stg_core::player::Loadout { power: 400, ..Default::default() })
        .expect("多单元中段 boot");
    assert_eq!(g.world.body.view().players()[0].power, 400);
}
#[test]
fn boot_at_compile_error_names_offending_file() {
    let units = vec![
        ("main.ecl".to_string(), "sub main() { }\n".to_string()),
        ("bad.ecl".to_string(), "sub b() {\n  wait(1)\n}\n".to_string()),
    ];
    match boot_at(&units, 7, 2, 0, Default::default()) {
        Err(BootError::Compile(msg)) => assert!(msg.contains("bad.ecl"), "{msg}"),
        other => panic!("期望 Compile 错:{other:?}"),
    }
}
```

- [ ] **Step 2: 实现 boot_at**(compile_units → 错误 `e.render(file)` 逐条拼接;new_game_at 转 Start 错),
  `boot` 改委托。bridge.rs 加两 #[func](gdext 形态照 new_game/hud_player 既有款):
  `new_game_at` 组装 `Vec<(String,String)>` + `Loadout` 后调 `boot_at`;`anchors()` 经 `view()` 四读口填 Dictionary。
- [ ] **Step 3: 冒烟扩展**:`godot_smoke.ecl` 加一行 `mark(9);`(main 顶层,wait 之前)与 `bgm(3);`;
  smoke.gd 追加:

```gdscript
# 中段启动 + 锚点读口冒烟
var names := PackedStringArray(["smoke.ecl"]); var srcs := PackedStringArray([src])
if not b.new_game_at(names, srcs, 7, 2, 9, 0, 400, 3, 3): fail("new_game_at"); return
b.step_frame(0); b.step_frame(0)
var a: Dictionary = b.anchors()
if a["bgm"] != 3: fail("anchors bgm"); return
```

(具体 mark/bgm 摆位保证"跳过 9 号标记前的 bgm 声明→垫片补偿可见 3"或"正常声明可见 3"二择一,
以 godot_smoke.ecl 实排为准,断言与脚本同步写死。)
- [ ] **Step 4: 跑冒烟**:`bash crates/stg-godot/smoke/run-smoke.sh` → `SMOKE OK`。
- [ ] **Step 5: 全绿 + 提交**:`git commit -m "feat(godot): boot_at 多单元中段开机+new_game_at/anchors 桥口+冒烟覆盖"`

---

### Task 8: 文档收口 + follow-ups + PROGRESS

**Files:**
- Modify: `docs/ecl-lang.md`(手写节)、`docs/ecl-ops.md`(syscall 号表 50-53 + REQ 段)
- Modify: `docs/follow-ups.md`(新账)、`PROGRESS.md`(收口)

**Interfaces:** Consumes 全部前任务的落地事实;生成段已由 Task 3 的 gen-ecl-meta 刷新,本任务只写手写节。

- [ ] **Step 1: ecl-lang.md**:
  ① 坑清单追加三条:「`mark` 只能在 `sub main` 顶层语句位,编号是正整数编译期常量且全镜像唯一,
  顶层 `var` 不得先于任何 mark;跳入后 mark 之前的全局变量/局部全是 0,需要的在 mark 块补」;
  「锚点自动补偿只认**顶层线性位**的常量参声明——if/难度分支里、spawn/async 任务里的 bgm/bg/bg_phase
  扫不到,mark 块手写覆盖」;「`add_score` 负值扣分饱和钳 0」。
  ② `## 语句` 节后新增 `## mark(中段启动标记)` 小节:语法两形态、landing pad 语义
  (正常流跨过/跳入执行)、补偿规则、`new_game_at(seed, rank, start, loadout)` 宿主侧对应关系。
  ③ `## 渲染请求(通道 B)` 节补 req 引擎段 4-7 注册表(REQ_STAGE_CLEAR 转场挂牌协议三行:
  账在世界内记完→挂牌→宿主停 step,spec §6 摘编)。
  ④ 新增 `## 多文件(整局脚本布局)` 小节:目录=编译单元集按名排序合并、扁平命名空间撞名报两处、
  `check <目录>` 用法。
  ⑤ **新增的 ```ecl 例子全部可编译**(`every_ecl_fenced_example_in_doc_compiles` 押运;
  多文件示例用文字描述目录结构,单 fence 内保持独立可编译)。
- [ ] **Step 2: ecl-ops.md**:syscall 号表加 5x 族四行(号/参/语义/Fault 行为=P4-b no-op);
  `mark` 的降低形态一行(JMP 跨垫);标记表在镜像的位置一句。
- [ ] **Step 3: follow-ups.md**:
  - **A4**:背景 STD 式 mini-VM(表现层解释器+文本格式;phase 分段寻位契约已在 spec §7 钉死;
    触发 = Godot 场景刀做背景演出时);
  - **B19**:清弹 builtin(语义空间:直接消/转点/护盾帧;触发 = 真实整局脚本关底转场需求);
  - **B20**:`set_power/set_lives/set_bombs` 账面 setter 三件(practice 装备已上行 Loadout,
    脚本侧暂无场景;触发 = 道中事件奖命类需求);
  - C17 追一小项:`new_game_at` 桥口的 loadout 参数是四标量平铺(将来若长出更多装备维度考虑
    Dictionary 化,与 C17 面打磨同批)。
- [ ] **Step 4: PROGRESS.md**:里程碑史加一行「2026-07-25 | **整局流程刀** | 方案A拍板(整局一World一镜像)
  +compile_units 多文件+mark 中段启动(垫片/标记表/自动补偿)+Loadout/new_game_at+表现锚点四字段
  (bgm/bg/bg_phase 声明式)+转场挂牌协议;金向量因锚点字段整体平移(判别式护行为)」;
  重写「现在」段(位置=M2 桥+整局地基齐,在飞=无,候选=Godot 场景刀/RL 线/背景刀)。
- [ ] **Step 5: 终检**:`cargo test --workspace`(文档例子闸+漂移闸)全绿;
  `cargo run -p stg-harness -- golden` 输出与 golden-task2.txt 逐字节同(Task 3 起零平移);
  fmt/clippy 净。
- [ ] **Step 6: 提交**:`git commit -m "docs: 整局流程刀收口——ecl-lang mark/锚点/多文件节+ops 5x族+follow-ups A4/B19/B20+PROGRESS"`

---

## Self-Review 备忘(计划作者已核)

- spec 覆盖:§1→T1,§2→T4+T6,§3→T6(+缩编账 T8/B20),§4→T2+T3,§5→T5,§6→T8(纯文档,引擎零改,
  REQ_STAGE_CLEAR 常量在 T2),§7→T8/A4 记账,§8→T8/B19,§9 各测试分布见各任务,§10 八条全部
  已在本计划正文钉死(1→T1 设计事实;2→T4 image 无字节格式+两新变体;3→T4 前瞻+垫片序列;
  4→T6 spawn 收编+T7 桥形态;5→T2 字段位/req 载荷 args[0]/号 4-7;6→T5 codegen 落点+新旧背景细则;
  7→T2 饱和语义;8→T2 判别式形态)。
- 类型一致:`Loadout` 定义(T6)与 T7 消费同形;`resolve_mark`(T4)与 T6 消费同名;
  `compile_units` 返回元组形态 T1/T7 一致;SYS_*/REQ_* 号 T2 定义、T3/T5 引用一致。
- 顺序依赖:T3 需 T2(SYS 常量);T5 需 T3(builtin 名)+T4(Mark 臂);T6 需 T4(标记表);
  T7 需 T1+T6;T5 端到端三条在 T6 Step 4 激活(计划内明写,不悬空)。
