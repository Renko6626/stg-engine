# stg-rl env 刀 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 合并前全分支大审阅由控制者亲自做（用户规矩）。

**Goal:** 交付 `stg-rl`（Rust 批量 env 核心）+ `stg-py`（PyO3 abi3 wheel `stg_rl`）+ wheel 分发 workflow：接收 ECL、机体 1、proto 表观测写入调用方缓冲、rayon 单进程批量 step。

**Architecture:** `stg-rl` 是断层线以上的纯 Rust crate：`layout`（proto 表布局 + HELLO）→ `encode`（World → 行字节）→ `env`（单 env 生命周期：镜像池、起点采样、开局快照、reseed、随机预热、done/events）→ `vec_env`（专属 rayon 池、CSR 压实）。`stg-py` 是独立 cargo workspace 的 PyO3 薄壳，不进主 workspace（避免主 CI 链接 libpython）。core 只加 `World::reseed` 与公开 `enemy::pack_handle`。

**Tech Stack:** Rust 1.94（rayon 1.12、pyo3 0.29.2 abi3-py310、numpy 0.29、serde_json 仅 dev）、maturin 1.15、Python ≥3.10、numpy、pytest；可选 torch / gymnasium。

**Spec:** `docs/superpowers/specs/2026-09-15-stg-rl-env-design.md`

## Global Constraints

- stg-core 断层线不变：core 只新增 `World::reseed(&mut self, seed: u64)` 与 `pub fn enemy::pack_handle(h: EnemyHandle) -> i32`；**不 bump `ENGINE_VER`**；不引入任何依赖。
- proto 布局逐字照 `stg-agent-proto/c/sa_layout.h`：stride player 36 / bullets 30 / enemies 38 / lasers 48 / items 18；表 id 1–5；类型名 `fx`/`angle`/`u8`/`u16`/`u32`/`i32`；全小端。
- 表 cap：bullets = `bullets_cap`（默认 1024，范围 `1..=8192`）；enemies 256；lasers 64（恒 0 行）；items 1024。
- HELLO：`proto 1`、`backend "stg-engine@<ENGINE_VER>"`、`policy "rl"`、`obs_timing "prev-frame-final"`、`tick_hz 60`、`move_area {-192,192,0,448}`、actions 位 0–6（UP DOWN LEFT RIGHT SHOT BOMB SLOW）。
- `ACTION_MASK = 0x7F`；机体恒 1（`Loadout { character: 1, ..Loadout::default() }`）。
- done 码：0 继续 / 1 死亡（`EVT_PLAYER_DIED`）/ 2 段落结束（`end_on`）/ 3 超时；同帧 1 优先；同 step 内自动 reset。
- events 列序：`died, graze, score, enemies_killed, shot_hits, bombs_used, items_picked, segment_end`；segment_end 码 1 phase_ended / 2 spell_captured / 3 spell_failed / 4 stage_cleared。
- 预热：`k ∈ [0, warmup_max]`，随机游走 9 方向每段 `[8,32]` 帧、不按 SHOT/BOMB；进决死窗口或出现段落结束事件 ⇒ 重试，上限 8，耗尽则 k=0。
- 种子：`splitmix64`（常量 `0x9E3779B97F4A7C15` / `0xBF58476D1CE4E5B9` / `0x94D049BB133111EB`），`episode_seed = splitmix64(base ^ (env_index as u64).wrapping_mul(K1) ^ counter.wrapping_mul(K2))`，K1 = `0xD1B54A32D192ED03`，K2 = `0xABC98388FB8FAC03`。
- 确定性：同 base_seed + 同动作序列 ⇒ 全部输出逐字节相同，与线程数无关。
- 清理临时/变异代码**只用编辑撤回，禁止 `git checkout <file>` / `git stash`**。
- commit 结尾：`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`。
- 主 workspace 每个 Task 结束 `cargo test --workspace` + `cargo clippy --workspace --all-targets -- -D warnings` + `cargo fmt --all -- --check` 全绿再提交；stg-py 用其自己目录下的命令（Task 7）。

## Rulings（写计划时裁定，偏离 spec 处）

1. **内置内容用 `include_str!` 嵌进 `stg-rl`**（build.rs 按文件名排序生成列表），Python 暴露 `bundled_sources("game") -> list[(name, src)]` 与 `compile_bundled("game")`，替代 spec §3/§10 的 `bundled_content()` 返回路径——免去 wheel 包数据拷贝，离线可用性相同。Task 7 同步改 spec。
2. **`stg-py` 不进主 workspace**（根 `Cargo.toml` `exclude`，自带 `[workspace]` 与 `Cargo.lock`）：PyO3 cdylib 进 `cargo test --workspace` 会要求 CI 有 libpython，破坏现有三平台闸门。
3. **`pack_enemy_handle` 搬到 `stg_core::enemy::pack_handle` 并公开**，syscall 调用它：编码 enemies `id` 需要同一打包，复制一份公式会漂移。

## File Map

| 文件 | 职责 |
|---|---|
| `crates/stg-core/src/step.rs` | + `World::reseed` 与等价测试 |
| `crates/stg-core/src/enemy.rs` / `ecl/syscall.rs` | `pack_handle` 公开 / syscall 改调用 |
| `../stg-agent-proto/{c/world.h,src/stgagent/consts.py,SPEC.md,tests/test_consts.py}` | 碎片枚举 8/9、cap 措辞 |
| `crates/stg-rl/Cargo.toml`, `build.rs`, `src/lib.rs` | crate 骨架、内置内容嵌入 |
| `crates/stg-rl/src/layout.rs` | 表常量、字段表、`hello_json(bullets_cap)`、`ACTION_MASK`、`EVENT_COLUMNS` |
| `crates/stg-rl/src/encode.rs` | player/phase/bullets/enemies/items 行编码 |
| `crates/stg-rl/src/env.rs` | `Image`/`Start`/`EnvConfig`/`Env`：reset、step、预热、events、done |
| `crates/stg-rl/src/vec_env.rs` | `VecEnv`、`BufferSet`、rayon 池、CSR 压实 |
| `crates/stg-rl/tests/fixtures/proto_v1_hello.json` | proto C 生成的 HELLO 夹具 |
| `crates/stg-harness/src/rlbench.rs` + `main.rs` | `rl-bench` 子命令 |
| `crates/stg-py/**` | PyO3 包、Python 模块、pytest |
| `.github/workflows/wheels.yml` | tag 出 wheel → Release |
| 文档 | spec 偏差、PROGRESS、CLAUDE.md、architecture、follow-ups、bench-baseline |

---

### Task 1: core —— `World::reseed` + `enemy::pack_handle`

**Files:**
- Modify: `crates/stg-core/src/step.rs`（`impl World` 内 `seed()` 附近；`mod tests` 末尾）
- Modify: `crates/stg-core/src/enemy.rs`（新增 fn + 测试）
- Modify: `crates/stg-core/src/ecl/syscall.rs:596-605`（删私有 fn，调用点改 `crate::enemy::pack_handle`）

**Interfaces:**
- Produces: `pub fn World::reseed(&mut self, seed: u64)`；`pub fn stg_core::enemy::pack_handle(h: EnemyHandle) -> i32`

- [ ] **Step 1: 写失败测试**（`step.rs` `mod tests` 末尾）

```rust
    /// reseed 等价（stg-rl env 刀 spec §8）：seed=0 开机模板 + reseed(s) 与直接 new_game_at(s)
    /// 逐帧校验和相同。若红 ⇒ new_game_at 在开机期消费了 RNG / 写了 seed 相关状态，stg-rl 须放弃模板缓存。
    #[test]
    fn reseed_after_template_equals_direct_boot() {
        let src = "sub main() { loop { let b = rand(100); wait(1); } }";
        let image = stg_image_for_test(src);
        for &(seed, rank) in &[(1u64, 0i32), (0xDEAD_BEEF, 2), (u64::MAX, 3)] {
            let ld = crate::player::Loadout { character: 1, ..crate::player::Loadout::default() };
            let mut a = World::new_game_at(0, rank, 0, ld, &image).unwrap();
            a.reseed(seed);
            let mut b = World::new_game_at(seed, rank, 0, ld, &image).unwrap();
            assert_eq!(a.seed(), seed);
            for f in 0..600 {
                assert_eq!(a.checksum(), b.checksum(), "seed {seed:#x} frame {f}");
                let input = crate::input::InputFrame::empty(a.frame());
                crate::step::step(&mut a, &crate::tables::TABLES_V0, &image, &input);
                crate::step::step(&mut b, &crate::tables::TABLES_V0, &image, &input);
            }
        }
    }
```

`stg_image_for_test`：stg-core 不能依赖编译器（见 `crates/stg-core/Cargo.toml` 踩坑记录）。先 `grep -n "fn .*image\|EclImage::" crates/stg-core/src/step.rs | head` 找本文件已有的测试镜像构造手法（手拼字节码的 helper）；若本文件没有「消费 RNG 的 main」镜像，改为把本测试放到 `crates/stg-ecl-compiler/tests/`（新建 `reseed.rs`，用 `stg_ecl_compiler::lang::compile(src, "t.ecl").unwrap()`，并把 `rand(100)` 换成手册 `docs/ecl-lang/7-reference.md` 里实际的随机数内建名），保持断言不变。报告里写明落在哪。

`enemy.rs` `mod tests` 末尾：

```rust
    /// 打包约定钉死：只押 gen 低 15 位，恒非负。
    #[test]
    fn pack_handle_masks_generation_to_15_bits() {
        assert_eq!(pack_handle(EnemyHandle { index: 3, generation: 1 }), (1 << 16) | 3);
        assert_eq!(pack_handle(EnemyHandle { index: 255, generation: 0xF00D }), (0x700D << 16) | 255);
        assert!(pack_handle(EnemyHandle { index: 255, generation: 0xFFFF }) >= 0);
    }
```

- [ ] **Step 2: 跑确认失败**

Run: `cargo test -p stg-core --lib pack_handle 2>&1 | tail -5`（及 reseed 测试所在 crate）
Expected: 编译错误 `cannot find function pack_handle` / `no method named reseed`。

- [ ] **Step 3: 实现**

`step.rs`（`seed()` 之后）：

```rust
    /// 训练用重设种子（stg-rl env 刀 spec §8）：只写 RNG 与 `seed` 字段。配合开机模板缓存：
    /// `new_game_at(0,…)` 快照 + `reseed(s)` ≡ `new_game_at(s,…)`（等价测试押运）。回放 / 握手身份仍以开机
    /// seed 为准，`Timeline` 不调用本函数。不改布局、不改演化。
    pub fn reseed(&mut self, seed: u64) {
        self.body.rng = Pcg32::new(seed, RNG_SEQ);
        self.seed = seed;
    }
```

`enemy.rs`：把 `syscall.rs` 里 `pack_enemy_handle` 的文档注释与函数体整段搬过来，改名 `pub fn pack_handle(h: EnemyHandle) -> i32`；`syscall.rs` 删原函数，所有调用点改 `crate::enemy::pack_handle(...)`（`grep -n pack_enemy_handle crates/stg-core/src` 逐个改，注释里提到的旧名同步改）。

- [ ] **Step 4: 跑测试**

Run: `cargo test --workspace 2>&1 | grep -E "^test result|FAILED|panicked"` + clippy + fmt
Expected: 全绿；金向量不变（`cargo run -p stg-harness -- golden --out /tmp/g.txt && md5sum /tmp/g.txt` = `54b5c5a3db4d78934c942e735d190070`）。

- [ ] **Step 5: Commit**

```bash
git add -A crates/stg-core crates/stg-ecl-compiler
git commit -m "feat(core): stg-rl 刀 T1——World::reseed（模板+reseed ≡ new_game_at 等价测试）+ enemy::pack_handle 公开"
```

---

### Task 2: proto 仓 —— 碎片枚举 + cap 措辞 + HELLO 夹具

**Files:**
- Modify: `/data/sunyunbo/www/stg-agent-proto/c/world.h:112-119`、`src/stgagent/consts.py`、`SPEC.md`（items kind 表 + §3 tables 说明 + §1 容量注释）、`tests/test_consts.py`
- Create: `/tmp/sunyunbo/proto_v1_hello.json`（Task 3 拷入 crate）

**Interfaces:**
- Produces: `AP_ITEM_LIFE_PIECE 8`、`AP_ITEM_BOMB_PIECE 9`；`/tmp/sunyunbo/proto_v1_hello.json`（proto C `sa_hello_json` 真实输出，Task 3 消费）

- [ ] **Step 1: 失败测试**（`tests/test_consts.py` 追加；先 `grep -n ITEM src/stgagent/consts.py` 看现有命名风格并照用）

```python
def test_item_piece_kinds():
    from stgagent import consts
    assert consts.ITEM_LIFE_PIECE == 8
    assert consts.ITEM_BOMB_PIECE == 9
```

Run: `cd /data/sunyunbo/www/stg-agent-proto && python3 -m pytest tests/test_consts.py -q` → FAIL（AttributeError）。

- [ ] **Step 2: 实现**
- `c/world.h`：`#define AP_ITEM_LIFE_PIECE  8   /* 残机碎片 */`、`#define AP_ITEM_BOMB_PIECE  9   /* 炸弹碎片 */`。
- `consts.py` 同名常量。
- SPEC items `kind` 表加两行 `8 | 残机碎片`、`9 | 炸弹碎片`。
- SPEC §3 `tables` 说明末尾加：「**cap 由各后端在 HELLO 声明**：`AP_MAX_*`（640/256/64/1024）只是 `c/world.h` 结构体的默认容量（可 `#define` 覆盖），不是契约上限；OBS `count` 为 u16。stg-engine 后端 bullets cap 可配（默认 1024）。」
- §1 `ap_world_t` 代码块里 `// AP_MAX_BULLETS = 640` 注释后补「（th06nc 默认，可覆盖）」。

Run: `python3 -m pytest -q && make -C c test-host` → 全绿。

- [ ] **Step 3: 生成 HELLO 夹具**

```bash
cd /data/sunyunbo/www/stg-agent-proto
cat > /tmp/sunyunbo/hello_fixture.c <<'EOF'
#include <stdio.h>
#include "stgagent.h"
int main(void) {
    sa_desc_t d = { "fixture", "builtin", "prev-frame-final", 60, 0x7f, { -192, 192, 0, 448 } };
    char buf[8192]; int n = sa_hello_json(&d, buf, sizeof buf);
    if (n < 0) return 1; fwrite(buf, 1, (size_t)n, stdout); return 0;
}
EOF
gcc -std=gnu99 -O2 -Ic -o /tmp/sunyunbo/hello_fixture /tmp/sunyunbo/hello_fixture.c c/sa_encode.c -lm
/tmp/sunyunbo/hello_fixture | python3 -m json.tool > /tmp/sunyunbo/proto_v1_hello.json
python3 -c "import json;d=json.load(open('/tmp/sunyunbo/proto_v1_hello.json'));print([(t['name'],t['stride'],len(t['fields'])) for t in d['tables']])"
```

Expected: `[('player', 36, 14), ('bullets', 30, 10), ('enemies', 38, 10), ('lasers', 48, 14), ('items', 18, 6)]`。

夹具先落 `/tmp/sunyunbo/proto_v1_hello.json`：`crates/` 下不能出现没有 `Cargo.toml` 的目录（workspace `members = ["crates/*"]` 会加载失败），Task 3 建 crate 时再拷进去。

- [ ] **Step 4: Commit（只 proto 仓；它无 remote，推 GitHub 由用户做）**

```bash
cd /data/sunyunbo/www/stg-agent-proto && git add -A && git commit -m "feat(contract): items kind 新增 8 残机碎片 / 9 炸弹碎片；SPEC 写明 cap 由后端 HELLO 声明"
```

---

### Task 3: stg-rl 骨架 —— `layout` + HELLO + 内置内容

**Files:**
- Create: `crates/stg-rl/Cargo.toml`、`crates/stg-rl/build.rs`、`crates/stg-rl/src/lib.rs`、`crates/stg-rl/src/layout.rs`
- Create: `crates/stg-rl/tests/fixtures/proto_v1_hello.json`（`cp /tmp/sunyunbo/proto_v1_hello.json`）、`crates/stg-rl/tests/layout.rs`
- Modify: 根 `Cargo.toml`（`[workspace.dependencies]` 加 `stg-rl = { path = "crates/stg-rl" }`；`[workspace]` 加 `exclude = ["crates/stg-py"]`，Task 7 用）

**Interfaces:**
- Produces（`stg_rl::layout`）：
  - `pub enum FieldType { Fx, Angle, U8, U16, U32, I32 }`，`impl FieldType { pub fn name(self) -> &'static str; pub fn size(self) -> usize }`
  - `pub struct FieldDef { pub name: &'static str, pub ty: FieldType, pub off: usize }`
  - `pub struct TableDef { pub id: u8, pub name: &'static str, pub stride: usize, pub fields: &'static [FieldDef] }`
  - `pub const PLAYER / BULLETS / ENEMIES / LASERS / ITEMS: TableDef`；`pub const TABLES: [&TableDef; 5]`（id 序）
  - 偏移常量模块 `pub mod off { pub mod player { pub const X: usize = 0; … } pub mod bullet {…} pub mod enemy {…} pub mod item {…} }`，名字与 `sa_layout.h` 的 `P_*/B_*/E_*/I_*` 一一对应（小写去前缀，如 `player::HIT_R`、`bullet::SPEED`）
  - `pub const ENEMIES_CAP: usize = 256; pub const LASERS_CAP: usize = 64; pub const ITEMS_CAP: usize = 1024; pub const BULLETS_CAP_DEFAULT: usize = 1024; pub const BULLETS_CAP_MAX: usize = 8192;`
  - `pub const ACTION_MASK: u32 = 0x7F;`
  - `pub const EVENT_COLUMNS: [&str; 8] = ["died","graze","score","enemies_killed","shot_hits","bombs_used","items_picked","segment_end"];`
  - `pub fn hello_json(bullets_cap: usize) -> String`
  - `pub fn bundled_sources(pack: &str) -> Option<&'static [(&'static str, &'static str)]>`（`stg_rl::bundled`；v1 仅 `"game"`）

- [ ] **Step 1: 建 crate 与夹具，写失败测试**

`crates/stg-rl/Cargo.toml`：

```toml
[package]
name         = "stg-rl"
version      = "0.1.0"
edition.workspace      = true
rust-version = "1.94"
authors.workspace      = true
publish      = false
description  = "强化学习批量 env 核心（断层线以上）：接收 ECL、机体 1、proto 表观测、rayon 批量 step"

[dependencies]
stg-core.workspace = true
stg-ecl-compiler.workspace = true
rayon = "1.12"

[dev-dependencies]
serde_json = "1"
```

`crates/stg-rl/tests/layout.rs`：

```rust
//! HELLO 布局对拍 proto C 夹具（spec §11）：除 backend/policy/cap/actions 之外逐项相等。
use serde_json::Value;

fn fixture() -> Value {
    serde_json::from_str(include_str!("fixtures/proto_v1_hello.json")).unwrap()
}

#[test]
fn tables_match_proto_fixture_except_cap() {
    let ours: Value = serde_json::from_str(&stg_rl::layout::hello_json(1024)).unwrap();
    let theirs = fixture();
    let (a, b) = (ours["tables"].as_array().unwrap(), theirs["tables"].as_array().unwrap());
    assert_eq!(a.len(), b.len());
    for (x, y) in a.iter().zip(b) {
        for k in ["id", "name", "stride", "fields"] {
            assert_eq!(x[k], y[k], "table {} key {k}", y["name"]);
        }
    }
}

#[test]
fn header_fields() {
    let v: Value = serde_json::from_str(&stg_rl::layout::hello_json(777)).unwrap();
    assert_eq!(v["proto"], 1);
    assert_eq!(v["backend"], format!("stg-engine@{}", stg_core::ENGINE_VER));
    assert_eq!(v["policy"], "rl");
    assert_eq!(v["obs_timing"], "prev-frame-final");
    assert_eq!(v["tick_hz"], 60);
    assert_eq!(v["field"], fixture()["field"]);
    assert_eq!(v["number"], fixture()["number"]);
    assert_eq!(v["actions"], fixture()["actions"], "位 0-6，与夹具 action_bits=0x7f 相同");
    let caps: Vec<u64> = v["tables"].as_array().unwrap().iter().map(|t| t["cap"].as_u64().unwrap()).collect();
    assert_eq!(caps, vec![1, 777, 256, 64, 1024]);
}

#[test]
fn offset_consts_agree_with_field_tables() {
    use stg_rl::layout::*;
    let find = |t: &TableDef, n: &str| t.fields.iter().find(|f| f.name == n).unwrap().off;
    assert_eq!(find(&PLAYER, "score"), off::player::SCORE);
    assert_eq!(find(&BULLETS, "type"), off::bullet::TYPE);
    assert_eq!(find(&ENEMIES, "id"), off::enemy::ID);
    assert_eq!(find(&ITEMS, "flags"), off::item::FLAGS);
    for t in TABLES {
        for f in t.fields {
            assert!(f.off + f.ty.size() <= t.stride, "{}.{}", t.name, f.name);
        }
    }
}

#[test]
fn bundled_game_pack_compiles() {
    let units: Vec<(String, String)> = stg_rl::bundled::bundled_sources("game")
        .unwrap()
        .iter()
        .map(|(n, s)| (n.to_string(), s.to_string()))
        .collect();
    assert_eq!(units.len(), 5);
    assert!(units.windows(2).all(|w| w[0].0 < w[1].0), "按文件名排序");
    stg_ecl_compiler::lang::compile_units(&units).expect("内置 game 包必须能编译");
    assert!(stg_rl::bundled::bundled_sources("nope").is_none());
}
```

Run: `cargo test -p stg-rl 2>&1 | tail -5` → Expected: 编译失败（`layout` / `bundled` 不存在）。

- [ ] **Step 2: 实现 `layout.rs`**

字段表**逐字照抄** `sa_layout.h` 与 SPEC §3 表（名字、类型、偏移、顺序）：

```rust
//! proto v1 表布局（逐字照 stg-agent-proto `c/sa_layout.h`；夹具测试 `tests/layout.rs` 押运）。

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum FieldType { Fx, Angle, U8, U16, U32, I32 }

impl FieldType {
    pub fn name(self) -> &'static str {
        match self { Self::Fx => "fx", Self::Angle => "angle", Self::U8 => "u8", Self::U16 => "u16", Self::U32 => "u32", Self::I32 => "i32" }
    }
    pub fn size(self) -> usize {
        match self { Self::U8 => 1, Self::Angle | Self::U16 => 2, Self::Fx | Self::U32 | Self::I32 => 4 }
    }
}

pub struct FieldDef { pub name: &'static str, pub ty: FieldType, pub off: usize }
pub struct TableDef { pub id: u8, pub name: &'static str, pub stride: usize, pub fields: &'static [FieldDef] }

macro_rules! f { ($n:literal, $t:ident, $o:expr) => { FieldDef { name: $n, ty: FieldType::$t, off: $o } } }

pub mod off {
    pub mod player { pub const X: usize = 0; pub const Y: usize = 4; pub const HIT_R: usize = 8; pub const SPEED: usize = 12; pub const SPEED_F: usize = 16; pub const FOCUS: usize = 20; pub const STATE: usize = 21; pub const LIVES: usize = 22; pub const BOMBS: usize = 23; pub const LFRAG: usize = 24; pub const BFRAG: usize = 25; pub const POWER: usize = 26; pub const SCORE: usize = 28; pub const GRAZE: usize = 32; }
    pub mod bullet { pub const X: usize = 0; pub const Y: usize = 4; pub const VX: usize = 8; pub const VY: usize = 12; pub const SPEED: usize = 16; pub const ANGLE: usize = 20; pub const RADIUS: usize = 22; pub const FLAGS: usize = 26; pub const STATE: usize = 27; pub const TYPE: usize = 28; }
    pub mod enemy { pub const X: usize = 0; pub const Y: usize = 4; pub const HURT_W: usize = 8; pub const HURT_H: usize = 12; pub const HIT_W: usize = 16; pub const HIT_H: usize = 20; pub const HP: usize = 24; pub const HP_MAX: usize = 28; pub const FLAGS: usize = 32; pub const ID: usize = 34; }
    pub mod item { pub const X: usize = 0; pub const Y: usize = 4; pub const VX: usize = 8; pub const VY: usize = 12; pub const KIND: usize = 16; pub const FLAGS: usize = 17; }
}

pub const PLAYER: TableDef = TableDef { id: 1, name: "player", stride: 36, fields: &[
    f!("x", Fx, 0), f!("y", Fx, 4), f!("hit_radius", Fx, 8), f!("speed", Fx, 12), f!("speed_focus", Fx, 16),
    f!("focus", U8, 20), f!("state", U8, 21), f!("lives", U8, 22), f!("bombs", U8, 23), f!("life_frags", U8, 24),
    f!("bomb_frags", U8, 25), f!("power", U16, 26), f!("score", U32, 28), f!("graze", U32, 32),
] };
pub const BULLETS: TableDef = TableDef { id: 2, name: "bullets", stride: 30, fields: &[
    f!("x", Fx, 0), f!("y", Fx, 4), f!("vx", Fx, 8), f!("vy", Fx, 12), f!("speed", Fx, 16), f!("angle", Angle, 20),
    f!("radius", Fx, 22), f!("flags", U8, 26), f!("state", U8, 27), f!("type", U16, 28),
] };
pub const ENEMIES: TableDef = TableDef { id: 3, name: "enemies", stride: 38, fields: &[
    f!("x", Fx, 0), f!("y", Fx, 4), f!("hurt_w", Fx, 8), f!("hurt_h", Fx, 12), f!("hit_w", Fx, 16), f!("hit_h", Fx, 20),
    f!("hp", I32, 24), f!("hp_max", I32, 28), f!("flags", U16, 32), f!("id", U32, 34),
] };
pub const LASERS: TableDef = TableDef { id: 4, name: "lasers", stride: 48, fields: &[
    f!("x", Fx, 0), f!("y", Fx, 4), f!("angle", Angle, 8), f!("start", Fx, 10), f!("end", Fx, 14), f!("start_len", Fx, 18),
    f!("speed", Fx, 22), f!("half_h", Fx, 26), f!("omega", Fx, 30), f!("vx", Fx, 34), f!("vy", Fx, 38),
    f!("t_active", I32, 42), f!("state", U8, 46), f!("type", U8, 47),
] };
pub const ITEMS: TableDef = TableDef { id: 5, name: "items", stride: 18, fields: &[
    f!("x", Fx, 0), f!("y", Fx, 4), f!("vx", Fx, 8), f!("vy", Fx, 12), f!("kind", U8, 16), f!("flags", U8, 17),
] };
pub const TABLES: [&TableDef; 5] = [&PLAYER, &BULLETS, &ENEMIES, &LASERS, &ITEMS];

pub const ENEMIES_CAP: usize = 256;
pub const LASERS_CAP: usize = 64;
pub const ITEMS_CAP: usize = 1024;
pub const BULLETS_CAP_DEFAULT: usize = 1024;
pub const BULLETS_CAP_MAX: usize = 8192;
pub const ACTION_MASK: u32 = 0x7F;
pub const EVENT_COLUMNS: [&str; 8] = ["died", "graze", "score", "enemies_killed", "shot_hits", "bombs_used", "items_picked", "segment_end"];
const ACTION_NAMES: [&str; 7] = ["UP", "DOWN", "LEFT", "RIGHT", "SHOT", "BOMB", "SLOW"];

/// proto HELLO JSON（键序同 C `sa_hello_json`）。`bullets_cap` 须已由调用方校验在 `1..=BULLETS_CAP_MAX`。
pub fn hello_json(bullets_cap: usize) -> String {
    use std::fmt::Write;
    let mut s = String::new();
    write!(s, "{{\"proto\":1,\"backend\":\"stg-engine@{}\",\"policy\":\"rl\",\"obs_timing\":\"prev-frame-final\",\"tick_hz\":60,", stg_core::ENGINE_VER).unwrap();
    s.push_str("\"field\":{\"half_w\":192,\"height\":448,\"origin\":\"center-top\",\"move_area\":{\"xmin\":-192,\"xmax\":192,\"ymin\":0,\"ymax\":448}},");
    s.push_str("\"number\":{\"fx\":\"q16.16-i32-le\",\"angle\":\"bam-u16-le\"},\"actions\":[");
    for (i, n) in ACTION_NAMES.iter().enumerate() {
        if i > 0 { s.push(','); }
        write!(s, "{{\"bit\":{i},\"name\":\"{n}\"}}").unwrap();
    }
    s.push_str("],\"tables\":[");
    let caps = [1, bullets_cap, ENEMIES_CAP, LASERS_CAP, ITEMS_CAP];
    for (i, t) in TABLES.iter().enumerate() {
        if i > 0 { s.push(','); }
        write!(s, "{{\"id\":{},\"name\":\"{}\",\"cap\":{},\"stride\":{},\"fields\":[", t.id, t.name, caps[i], t.stride).unwrap();
        for (j, f) in t.fields.iter().enumerate() {
            if j > 0 { s.push(','); }
            write!(s, "{{\"name\":\"{}\",\"type\":\"{}\",\"off\":{}}}", f.name, f.ty.name(), f.off).unwrap();
        }
        s.push_str("]}");
    }
    s.push_str("]}");
    s
}
```

注意：夹具 `move_area` 是 `-192/192/0/448`（Task 2 生成时就用了这组值），若 `header_fields` 的 `field` 比较红，以夹具为准核对。

- [ ] **Step 3: 实现内置内容**

`crates/stg-rl/build.rs`：

```rust
//! 把 `godot/ecl/game/*.ecl` 按文件名排序嵌进 crate（spec 偏差 Ruling 1）。
use std::{env, fs, path::PathBuf};

fn main() {
    let root = PathBuf::from(env::var("CARGO_MANIFEST_DIR").unwrap()).join("../../godot/ecl/game");
    println!("cargo:rerun-if-changed={}", root.display());
    let mut files: Vec<_> = fs::read_dir(&root).unwrap().filter_map(|e| e.ok()).map(|e| e.path())
        .filter(|p| p.extension().is_some_and(|x| x == "ecl")).collect();
    files.sort();
    let mut out = String::from("pub static GAME: &[(&str, &str)] = &[\n");
    for p in &files {
        println!("cargo:rerun-if-changed={}", p.display());
        let name = p.file_name().unwrap().to_str().unwrap();
        out.push_str(&format!("    ({name:?}, include_str!({:?})),\n", p.canonicalize().unwrap()));
    }
    out.push_str("];\n");
    fs::write(PathBuf::from(env::var("OUT_DIR").unwrap()).join("bundled.rs"), out).unwrap();
}
```

`src/lib.rs`：

```rust
//! stg-rl —— 强化学习批量 env 核心（spec `docs/superpowers/specs/2026-09-15-stg-rl-env-design.md`）。
//! 断层线以上：允许线程 / rayon；stg-core 语义不变。

pub mod layout;

pub mod bundled {
    include!(concat!(env!("OUT_DIR"), "/bundled.rs"));

    /// 内置 ECL 内容包（按文件名排序）。v1 只有 `"game"`（`godot/ecl/game`）。
    pub fn bundled_sources(pack: &str) -> Option<&'static [(&'static str, &'static str)]> {
        match pack { "game" => Some(GAME), _ => None }
    }
}
```

wheel 构建（Task 7）时源码树完整，`../../godot` 可达；`cargo package` 不在范围内。

- [ ] **Step 4: 跑测试**

Run: `cargo test -p stg-rl` + `cargo test --workspace 2>&1 | grep -E "^test result|FAILED|panicked"` + clippy + fmt
Expected: 4 条 layout 测试绿，其余不变。

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml Cargo.lock crates/stg-rl
git commit -m "feat(stg-rl): T3 crate 骨架——proto v1 表布局 + hello_json（对拍 C 夹具）+ 内置 game 包嵌入"
```

---

### Task 4: `encode` —— World → proto 行字节

**Files:**
- Create: `crates/stg-rl/src/encode.rs`、`crates/stg-rl/tests/encode.rs`
- Modify: `crates/stg-rl/src/lib.rs`（`pub mod encode;`）

**Interfaces:**
- Consumes: `stg_rl::layout::{off, ENEMIES_CAP, ITEMS_CAP, BULLETS, ENEMIES, ITEMS, PLAYER}`；`stg_core::enemy::pack_handle`（Task 1）
- Produces（`stg_rl::encode`）：
  - `pub fn phase_bits(w: &World) -> u32`
  - `pub fn write_player(w: &World, tables: &WorldTables, row: &mut [u8])`（`row.len() == 36`）
  - `pub fn write_enemies(w: &World, rows: &mut [u8]) -> usize`（`rows.len() >= 256*38`，返回行数）
  - `pub fn write_items(w: &World, rows: &mut [u8]) -> usize`（`rows.len() >= 1024*18`）
  - `pub struct BulletStats { pub count: usize, pub total: usize, pub dropped: usize }`
  - `pub fn write_bullets(w: &World, cap: usize, rows: &mut [u8], scratch: &mut Vec<(i64, u16)>) -> BulletStats`（`rows.len() >= cap*30`；`scratch` 复用避免每步分配）
  - 常量 `pub const PHASE_IN_GAME: u32 = 1; PHASE_BOMB_ACTIVE = 1<<4; PHASE_SPELL_ACTIVE = 1<<5; PHASE_PLAYER_CONTROLLABLE = 1<<6;`
  - `pub const ITEM_KIND: [u8; 5] = [1, 2, 8, 9, 7];`（按 `ITEM_POWER, ITEM_POINT, ITEM_LIFE_PIECE, ITEM_BOMB_PIECE, ITEM_STAR` 下标）

每行**全部字节显式写满**（未用字段写 0），编码只读 `w.view()` 与 `w.body` 的 pub 读口。

- [ ] **Step 1: 写失败测试** `crates/stg-rl/tests/encode.rs`

```rust
//! 编码判别测试（spec §5 / §11）：手摆已知世界，逐字段断言字节；互异非零值防错位。
use stg_core::math::Fx;
use stg_core::step::World;
use stg_core::tables::TABLES_V0;
use stg_rl::encode::*;
use stg_rl::layout::off;

fn rd_i32(b: &[u8], o: usize) -> i32 { i32::from_le_bytes(b[o..o + 4].try_into().unwrap()) }
fn rd_u32(b: &[u8], o: usize) -> u32 { u32::from_le_bytes(b[o..o + 4].try_into().unwrap()) }
fn rd_u16(b: &[u8], o: usize) -> u16 { u16::from_le_bytes(b[o..o + 2].try_into().unwrap()) }

fn bullet(w: &mut World, x: i32, y: i32, vx: i32, vy: i32, delay: u8, grazed: u8, sprite: u16) {
    w.body.create_bullet(stg_core::bullets::BulletInit {
        x: Fx::from_int(x), y: Fx::from_int(y), vx: Fx::from_int(vx), vy: Fx::from_int(vy),
        speed: Fx::ZERO, angle: stg_core::math::Angle::ZERO, ang_vel: 0, accel: Fx::ZERO, ax: Fx::ZERO, ay: Fx::ZERO,
        sprite, radius: Fx::from_int(3), delay, life: 0xFFFF, flags: 0, grazed_by: grazed,
        transform_head: 0xFFFF, xform_wait: 0, xform_next: 0, born_frame: 0,
    });
}
```

敌人构造：新增 `fn enemy(w: &mut World, x: i32, y: i32, hp: i32) -> stg_core::enemy::EnemyHandle`，函数体**逐字段照抄** `crates/stg-core/src/world.rs` 中 `test_support::spawn_enemy`（约 1364 行起，`crate::` 改 `stg_core::`；`radius: Fx::from_int(12), hurtbox: Fx::from_int(16)` 保留——互异是判别点）。

```rust
#[test]
fn player_row_fields() {
    let w = World::new(1);
    let mut row = [0xAAu8; 36];
    write_player(&w, &TABLES_V0, &mut row);
    let p = w.view().players()[0];
    let cfg = &TABLES_V0.characters[p.character_id as usize];
    assert_eq!(rd_i32(&row, off::player::X), p.x.raw());
    assert_eq!(rd_i32(&row, off::player::Y), p.y.raw());
    assert_eq!(rd_i32(&row, off::player::HIT_R), p.hit_radius.raw());
    assert_eq!(rd_i32(&row, off::player::SPEED), cfg.high_speed.raw());
    assert_eq!(rd_i32(&row, off::player::SPEED_F), cfg.low_speed.raw());
    assert_eq!(row[off::player::STATE], p.life_state);
    assert_eq!(row[off::player::LIVES], p.lives);
    assert_eq!(row[off::player::BOMBS], p.bombs);
    assert_eq!(rd_u16(&row, off::player::POWER), p.power);
    assert!(row.iter().all(|&b| b != 0xAA), "每字节都被写过");
}

#[test]
fn bullets_rows_flags_state_type_and_derived() {
    let mut w = World::new(1);
    bullet(&mut w, 10, 100, 3, 4, 0, 1, 77); // 可碰撞、已擦
    bullet(&mut w, -20, 50, 0, -2, 5, 0, 88); // 延迟中、未擦
    let mut rows = vec![0u8; 8 * 30];
    let mut scratch = Vec::new();
    let st = write_bullets(&w, 8, &mut rows, &mut scratch);
    assert_eq!((st.count, st.total, st.dropped), (2, 2, 0));
    let r0 = &rows[0..30];
    assert_eq!(rd_i32(r0, off::bullet::X), Fx::from_int(10).raw());
    assert_eq!(rd_i32(r0, off::bullet::VY), Fx::from_int(4).raw());
    assert_eq!(rd_i32(r0, off::bullet::SPEED), Fx::from_int(5).raw(), "isqrt(3²+4²)");
    assert_eq!(rd_u16(r0, off::bullet::ANGLE), stg_core::math::atan2(Fx::from_int(4), Fx::from_int(3)).raw());
    assert_eq!(r0[off::bullet::FLAGS], 0b111);
    assert_eq!(r0[off::bullet::STATE], 1);
    assert_eq!(rd_u16(r0, off::bullet::TYPE), 77);
    let r1 = &rows[30..60];
    assert_eq!(r1[off::bullet::FLAGS], 0b010, "delay>0 不可碰撞、未擦");
    assert_eq!(r1[off::bullet::STATE], 0);
    assert_eq!(rd_u16(r1, off::bullet::TYPE), 88);
}

#[test]
fn bullets_overflow_keeps_nearest_in_pool_order() {
    let mut w = World::new(1);
    // 自机在 (0,384)。池索引 0..10 距离依次：远近交错
    let ys = [0, 380, 10, 370, 20, 360, 30, 350, 40, 340];
    for (i, y) in ys.iter().enumerate() { bullet(&mut w, 0, *y, 0, 0, 0, 0, i as u16); }
    let mut rows = vec![0u8; 4 * 30];
    let st = write_bullets(&w, 4, &mut rows, &mut Vec::new());
    assert_eq!((st.count, st.total, st.dropped), (4, 10, 6));
    let types: Vec<u16> = (0..4).map(|i| rd_u16(&rows[i * 30..], off::bullet::TYPE)).collect();
    assert_eq!(types, vec![1, 3, 5, 7], "最近 4 颗（y=380/370/360/350），按池索引序输出");
}

#[test]
fn bullets_overflow_tie_breaks_by_pool_index() {
    let mut w = World::new(1);
    for i in 0..3 { bullet(&mut w, 0, 300, 0, 0, 0, 0, i); } // 三颗等距
    let mut rows = vec![0u8; 2 * 30];
    write_bullets(&w, 2, &mut rows, &mut Vec::new());
    assert_eq!((rd_u16(&rows, off::bullet::TYPE), rd_u16(&rows[30..], off::bullet::TYPE)), (0, 1));
}

#[test]
fn enemies_rows_boss_collidable_id() {
    let mut w = World::new(1);
    let a = enemy(&mut w, 30, 60, 500);
    let b = enemy(&mut w, -30, 90, 800);
    w.body.boss_set(0, stg_core::boss::BossUiSlot { enemy: a, hp_ratio: Fx::ONE, spell_id: 0, timer_frames: 0, phase_left: 0, active: 1 });
    // b 标 NO_BODY：走公开写口（grep `pub fn set_enemy_flag` / syscall 441 对应的 WorldBody 写 API；若无 Rust 写口，
    // 改用 kill：`w.body.kill_enemy_by_handle(b, &TABLES_V0)` 置 DYING，同样应使 bit4 = 0）
    let mut rows = vec![0u8; 256 * 38];
    let n = write_enemies(&w, &mut rows);
    assert_eq!(n, 2);
    let (r0, r1) = (&rows[0..38], &rows[38..76]);
    assert_eq!(rd_i32(r0, off::enemy::HIT_W), Fx::from_int(12).raw());
    assert_eq!(rd_i32(r0, off::enemy::HURT_W), Fx::from_int(16).raw());
    assert_eq!(rd_i32(r0, off::enemy::HP), 500);
    assert_eq!(rd_u16(r0, off::enemy::FLAGS), 0x11, "boss + 可碰撞");
    assert_eq!(rd_u32(r0, off::enemy::ID), stg_core::enemy::pack_handle(a) as u32);
    assert_eq!(rd_u16(r1, off::enemy::FLAGS) & 0x01, 0, "非 boss");
    assert_eq!(rd_u16(r1, off::enemy::FLAGS) & 0x10, 0, "NO_BODY/DYING 不可碰撞");
    assert_eq!(rd_u32(r1, off::enemy::ID), stg_core::enemy::pack_handle(b) as u32);
}

#[test]
fn items_rows_kind_and_magnet() {
    use stg_core::items::*;
    let mut w = World::new(1);
    for t in [ITEM_POWER, ITEM_POINT, ITEM_LIFE_PIECE, ITEM_BOMB_PIECE] {
        w.body.drop_item(Fx::from_int(0), Fx::from_int(100), t, &TABLES_V0);
    }
    w.body.attract_all_items(0); // 前 4 个上锁
    w.body.drop_item(Fx::from_int(0), Fx::from_int(100), ITEM_STAR, &TABLES_V0); // 未锁
    let mut rows = vec![0u8; 1024 * 18];
    assert_eq!(write_items(&w, &mut rows), 5);
    let kinds: Vec<u8> = (0..5).map(|i| rows[i * 18 + off::item::KIND]).collect();
    assert_eq!(kinds, vec![1, 2, 8, 9, 7]);
    let flags: Vec<u8> = (0..5).map(|i| rows[i * 18 + off::item::FLAGS]).collect();
    assert_eq!(flags, vec![1, 1, 1, 1, 0]);
}

#[test]
fn phase_bits_in_game_and_controllable() {
    let w = World::new(1);
    assert_eq!(phase_bits(&w), PHASE_IN_GAME | PHASE_PLAYER_CONTROLLABLE);
}
```

（`enemies_rows_boss_collidable_id` 里 b 的不可碰撞手段二选一，写进报告；断言不变。）

Run: `cargo test -p stg-rl --test encode 2>&1 | tail -5` → 编译失败（`encode` 不存在）。

- [ ] **Step 2: 实现 `encode.rs`**

```rust
//! World → proto v1 行字节（spec §5）。只读 `w.view()` 与公开读口；每行全部字节显式写满。

use crate::layout::{off, ENEMIES_CAP, ITEMS_CAP};
use stg_core::enemy::{pack_handle, EnemyHandle, ENEMY_DYING, ENEMY_NO_BODY};
use stg_core::items::{MAGNET_NONE, MAGNET_PICKED};
use stg_core::math::{atan2, isqrt, len_sq, Fx};
use stg_core::player::LIFE_ALIVE;
use stg_core::step::World;
use stg_core::tables::WorldTables;

pub const PHASE_IN_GAME: u32 = 1;
pub const PHASE_BOMB_ACTIVE: u32 = 1 << 4;
pub const PHASE_SPELL_ACTIVE: u32 = 1 << 5;
pub const PHASE_PLAYER_CONTROLLABLE: u32 = 1 << 6;
/// 引擎 `ITEM_*` 下标 → proto `AP_ITEM_*`（POWER, POINT, LIFE_PIECE, BOMB_PIECE, STAR）。
pub const ITEM_KIND: [u8; 5] = [1, 2, 8, 9, 7];

#[inline] fn put_i32(b: &mut [u8], o: usize, v: i32) { b[o..o + 4].copy_from_slice(&v.to_le_bytes()); }
#[inline] fn put_u32(b: &mut [u8], o: usize, v: u32) { b[o..o + 4].copy_from_slice(&v.to_le_bytes()); }
#[inline] fn put_u16(b: &mut [u8], o: usize, v: u16) { b[o..o + 2].copy_from_slice(&v.to_le_bytes()); }

pub fn phase_bits(w: &World) -> u32 {
    let v = w.view();
    let p = &v.players()[0];
    let mut bits = PHASE_IN_GAME;
    if p.bomb_timer > 0 { bits |= PHASE_BOMB_ACTIVE; }
    if v.spells().iter().any(|s| s.active != 0) { bits |= PHASE_SPELL_ACTIVE; }
    if p.life_state == LIFE_ALIVE && v.freeze_left()[1] == 0 { bits |= PHASE_PLAYER_CONTROLLABLE; }
    bits
}

pub fn write_player(w: &World, tables: &WorldTables, row: &mut [u8]) {
    let p = &w.view().players()[0];
    let cfg = &tables.characters[p.character_id as usize];
    row.fill(0);
    put_i32(row, off::player::X, p.x.raw());
    put_i32(row, off::player::Y, p.y.raw());
    put_i32(row, off::player::HIT_R, p.hit_radius.raw());
    put_i32(row, off::player::SPEED, cfg.high_speed.raw());
    put_i32(row, off::player::SPEED_F, cfg.low_speed.raw());
    row[off::player::FOCUS] = u8::from(p.input & stg_core::input::BTN_SLOW != 0);
    row[off::player::STATE] = p.life_state;
    row[off::player::LIVES] = p.lives;
    row[off::player::BOMBS] = p.bombs;
    row[off::player::LFRAG] = p.life_pieces;
    row[off::player::BFRAG] = p.bomb_pieces;
    put_u16(row, off::player::POWER, p.power);
    put_u32(row, off::player::SCORE, p.score.min(u32::MAX as u64) as u32);
    put_u32(row, off::player::GRAZE, p.graze);
}

pub struct BulletStats { pub count: usize, pub total: usize, pub dropped: usize }

pub fn write_bullets(w: &World, cap: usize, rows: &mut [u8], scratch: &mut Vec<(i64, u16)>) -> BulletStats {
    let v = w.view();
    let b = v.bullets();
    let p = &v.players()[0];
    let total = b.iter_alive().count();
    scratch.clear();
    if total <= cap {
        scratch.extend(b.iter_alive().map(|i| (0, i as u16)));
    } else {
        scratch.extend(b.iter_alive().map(|i| (len_sq(b.x()[i] - p.x, b.y()[i] - p.y), i as u16)));
        scratch.select_nth_unstable(cap - 1); // 全序键 (距离², 池索引) ⇒ 结果确定
        scratch.truncate(cap);
        scratch.sort_unstable_by_key(|&(_, i)| i);
    }
    for (k, &(_, i)) in scratch.iter().enumerate() {
        let i = i as usize;
        let r = &mut rows[k * 30..(k + 1) * 30];
        r.fill(0);
        let (vx, vy) = (b.vx()[i], b.vy()[i]);
        put_i32(r, off::bullet::X, b.x()[i].raw());
        put_i32(r, off::bullet::Y, b.y()[i].raw());
        put_i32(r, off::bullet::VX, vx.raw());
        put_i32(r, off::bullet::VY, vy.raw());
        put_i32(r, off::bullet::SPEED, isqrt(len_sq(vx, vy) as u64).min(i32::MAX as u32) as i32);
        put_u16(r, off::bullet::ANGLE, atan2(vy, vx).raw());
        put_i32(r, off::bullet::RADIUS, b.radius()[i].raw());
        let delay = b.delay()[i];
        r[off::bullet::FLAGS] = u8::from(delay == 0) | 2 | (u8::from(b.grazed_by()[i] & 1 != 0) << 2);
        r[off::bullet::STATE] = u8::from(delay == 0);
        put_u16(r, off::bullet::TYPE, b.sprite()[i]);
    }
    let count = scratch.len();
    BulletStats { count, total, dropped: total - count }
}

pub fn write_enemies(w: &World, rows: &mut [u8]) -> usize {
    let v = w.view();
    let e = v.enemies();
    let bosses: Vec<EnemyHandle> = v.boss_ui().iter().filter(|s| s.active != 0).map(|s| s.enemy).collect();
    let mut k = 0;
    for i in e.iter_alive().take(ENEMIES_CAP) {
        let r = &mut rows[k * 38..(k + 1) * 38];
        r.fill(0);
        let h = EnemyHandle { index: i as u16, generation: e.generation_of(i) };
        put_i32(r, off::enemy::X, e.x()[i].raw());
        put_i32(r, off::enemy::Y, e.y()[i].raw());
        put_i32(r, off::enemy::HURT_W, e.hurtbox()[i].raw());
        put_i32(r, off::enemy::HURT_H, e.hurtbox()[i].raw());
        put_i32(r, off::enemy::HIT_W, e.radius()[i].raw());
        put_i32(r, off::enemy::HIT_H, e.radius()[i].raw());
        put_i32(r, off::enemy::HP, e.hp()[i]);
        put_i32(r, off::enemy::HP_MAX, e.hp_max()[i]);
        let boss = bosses.iter().any(|bh| *bh == h);
        let collidable = e.flags()[i] & (ENEMY_NO_BODY | ENEMY_DYING) == 0;
        put_u16(r, off::enemy::FLAGS, u16::from(boss) | (u16::from(collidable) << 4));
        put_u32(r, off::enemy::ID, pack_handle(h) as u32);
        k += 1;
    }
    k
}

pub fn write_items(w: &World, rows: &mut [u8]) -> usize {
    let it = w.view().items();
    let mut k = 0;
    for i in it.iter_alive() {
        if k == ITEMS_CAP || it.magnet_to()[i] == MAGNET_PICKED { continue; }
        let r = &mut rows[k * 18..(k + 1) * 18];
        r.fill(0);
        put_i32(r, off::item::X, it.x()[i].raw());
        put_i32(r, off::item::Y, it.y()[i].raw());
        put_i32(r, off::item::VX, it.vx()[i].raw());
        put_i32(r, off::item::VY, it.vy()[i].raw());
        r[off::item::KIND] = ITEM_KIND.get(it.item_type()[i] as usize).copied().unwrap_or(0);
        r[off::item::FLAGS] = u8::from(it.magnet_to()[i] != MAGNET_NONE);
        k += 1;
    }
    k
}
```

`Fx` 若无 `Sub` impl 或 `EnemyHandle` 无 `PartialEq`，按编译器提示用 `Fx::from_raw(a.raw().wrapping_sub(b.raw()))` / 比较 `index`+`generation` 两字段。`Fx` 未使用的 import 删掉。

- [ ] **Step 3: 跑测试** — `cargo test -p stg-rl` 全绿；workspace 闸门三件套。

- [ ] **Step 4: Commit**

```bash
git add crates/stg-rl
git commit -m "feat(stg-rl): T4 encode——player/phase/bullets(溢出按距离保最近、池序输出)/enemies/items 行编码 + 判别测试"
```

---

### Task 5: `env` —— 单 env 生命周期（起点、快照、reseed、预热、events、done）

**Files:**
- Create: `crates/stg-rl/src/env.rs`、`crates/stg-rl/tests/env.rs`
- Modify: `crates/stg-rl/src/lib.rs`（`pub mod env;`）

**Interfaces:**
- Consumes: Task 1 `World::reseed`；Task 3 `layout::{ACTION_MASK, BULLETS_CAP_MAX}`、`bundled::bundled_sources`；Task 4 全部 `encode::*`
- Produces（`stg_rl::env`）：
  - `#[derive(Clone)] pub struct Image(pub(crate) Arc<EclImage>)`（`EnvConfig: Clone` 需要）；`pub fn compile(units: &[(String, String)]) -> Result<Image, String>`（Err = 各文件 `render` 拼接）
  - `#[derive(Clone)] pub struct Start { pub image: usize, pub mark: i32, pub rank: i32, pub weight: f64 }`（`image` = `EnvConfig.images` 下标）
  - `#[derive(Clone, Copy, PartialEq, Eq)] pub enum EndOn { PhaseEnded, SpellCaptured, SpellFailed, StageCleared }`
  - `#[derive(Clone)] pub struct EnvConfig { pub images: Vec<Image>, pub starts: Vec<Start>, pub frame_skip: u32, pub max_frames: u32, pub warmup_max: u32, pub end_on: Vec<EndOn>, pub bullets_cap: usize, pub seed: u64 }`
  - `pub fn validate(cfg: &EnvConfig) -> Result<(), String>`（起点空、权重非正/非有限、image 下标越界、mark 查无、rank 越界、`frame_skip == 0`、`max_frames == 0`、`bullets_cap` 不在 `1..=8192`）
  - `pub const EVENTS: usize = 8;` `pub const DONE_NONE/DIED/SEGMENT/TIMEOUT: u8 = 0/1/2/3;` `pub const MAX_WARMUP_RETRIES: u32 = 8;`
  - `pub struct StepOut { pub events: [i32; EVENTS], pub done: u8, pub ep_frames: i32, pub warmup_retries: i32, pub start_index: i32 }`
  - `pub struct Env`：`pub fn new(cfg: Arc<EnvConfig>, env_index: u32, cache: Arc<BootCache>) -> Env`；`pub fn reset(&mut self)`；`pub fn step(&mut self, action: u32) -> StepOut`（done≠0 时内部已自动 reset）；`pub fn world(&self) -> &World`；`pub fn set_weights(&mut self, w: Arc<Vec<f64>>)`
  - `pub struct BootCache`（`Mutex<HashMap<(usize, i32, i32), Box<World>>>`；断层线以上允许 HashMap——只做查找，不参与遍历序）：`pub fn new() -> BootCache`；`fn template(&self, cfg: &EnvConfig, start: &Start) -> Box<World>`（命中则 `copy_into` 出新 Box，未命中则 `new_game_at(0, rank, mark, Loadout{character:1,..}, image)` 生成并插入）
  - `pub fn splitmix64(x: u64) -> u64`

- [ ] **Step 1: 写失败测试** `crates/stg-rl/tests/env.rs`

测试镜像：用内置 game 包（`stg_rl::bundled::bundled_sources("game")`，mark 10 = 道中、15 = boss 段）。另需一个「很快会死」和「很快段落结束」的最小脚本：先读 `docs/ecl-lang.md` 与 `docs/ecl-lang/1-hello-danmaku.md`、`6-spell-and-stage.md` 取**实际语法**，写：
- `SHOOTER`：`main` 每帧朝自机发高速自机狙（让不操作的自机数十帧内死）；
- `QUICK_END`：`main` 里 `stage_clear(1)` 在 30 帧后发出（产生 `EVT_STAGE_CLEARED`）；
- `IDLE`：`main` 只 `wait` 循环（不发弹，永不结束）。
每个脚本先 `cargo run -p stg-harness -- run <file> --frames 200` 确认行为（死/清关/无事）再写进测试常量。

```rust
use std::sync::Arc;
use stg_rl::env::*;

fn img(src: &str) -> Image { compile(&[("t.ecl".into(), src.into())]).unwrap() }
fn cfg(images: Vec<Image>, end_on: Vec<EndOn>) -> EnvConfig {
    EnvConfig { images, starts: vec![Start { image: 0, mark: 0, rank: 0, weight: 1.0 }], frame_skip: 1,
                max_frames: 600, warmup_max: 0, end_on, bullets_cap: 1024, seed: 7 }
}
fn run(env: &mut Env, frames: usize, action: u32) -> Vec<StepOut> { (0..frames).map(|_| env.step(action)).collect() }

#[test]
fn validate_rejects_bad_config() {
    let good = cfg(vec![img(IDLE)], vec![]);
    assert!(validate(&good).is_ok());
    let mut c = good.clone(); c.starts[0].mark = 999; assert!(validate(&c).is_err());
    let mut c = good.clone(); c.starts[0].image = 5; assert!(validate(&c).is_err());
    let mut c = good.clone(); c.starts[0].rank = 99; assert!(validate(&c).is_err());
    let mut c = good.clone(); c.bullets_cap = 0; assert!(validate(&c).is_err());
    let mut c = good.clone(); c.bullets_cap = 8193; assert!(validate(&c).is_err());
    let mut c = good.clone(); c.frame_skip = 0; assert!(validate(&c).is_err());
    let mut c = good.clone(); c.starts[0].weight = 0.0; assert!(validate(&c).is_err());
    assert!(compile(&[("bad.ecl".into(), "sub main( {".into())]).unwrap_err().contains("bad.ecl"));
}

#[test]
fn character_is_classic_kit() {
    let c = Arc::new(cfg(vec![img(IDLE)], vec![]));
    let env = Env::new(c, 0, Arc::new(BootCache::new()));
    assert_eq!(env.world().view().players()[0].character_id, 1);
}

#[test]
fn death_ends_with_done_1_and_autoresets() {
    let c = Arc::new(cfg(vec![img(SHOOTER)], vec![]));
    let mut env = Env::new(c, 0, Arc::new(BootCache::new()));
    let outs = run(&mut env, 400, 0);
    let k = outs.iter().position(|o| o.done != DONE_NONE).expect("不操作必死");
    assert_eq!(outs[k].done, DONE_DIED);
    assert_eq!(outs[k].events[0], 1, "died 列");
    assert_eq!(outs[k].ep_frames, k as i32 + 1);
    assert_eq!(env.world().view().players()[0].deaths, 0, "已自动 reset 成新局");
}

#[test]
fn segment_end_is_done_2_only_when_listed() {
    let c = Arc::new(cfg(vec![img(QUICK_END)], vec![EndOn::StageCleared]));
    let mut env = Env::new(c, 0, Arc::new(BootCache::new()));
    let o = run(&mut env, 100, 0).into_iter().find(|o| o.done != 0).unwrap();
    assert_eq!((o.done, o.events[7]), (DONE_SEGMENT, 4));

    let c = Arc::new(cfg(vec![img(QUICK_END)], vec![]));
    let mut env = Env::new(c, 0, Arc::new(BootCache::new()));
    let outs = run(&mut env, 100, 0);
    assert!(outs.iter().all(|o| o.done == 0), "未列入 end_on 不结束");
    assert!(outs.iter().any(|o| o.events[7] == 4), "但 segment_end 列照记");
}

#[test]
fn timeout_is_done_3() {
    let mut c = cfg(vec![img(IDLE)], vec![]); c.max_frames = 50;
    let mut env = Env::new(Arc::new(c), 0, Arc::new(BootCache::new()));
    let outs = run(&mut env, 50, 0);
    assert_eq!(outs[49].done, DONE_TIMEOUT);
    assert!(outs[..49].iter().all(|o| o.done == 0));
}

#[test]
fn frame_skip_accumulates_and_stops_at_done() {
    let mut c = cfg(vec![img(IDLE)], vec![]); c.frame_skip = 3; c.max_frames = 10;
    let mut env = Env::new(Arc::new(c), 0, Arc::new(BootCache::new()));
    let outs = run(&mut env, 4, 0);
    assert_eq!(outs[3].done, DONE_TIMEOUT);
    assert_eq!(outs[3].ep_frames, 10, "判到即停：第 4 步只跑了 1 帧");
}

#[test]
fn same_seed_same_actions_same_world_and_seed_matters() {
    let mk = |seed| { let mut c = cfg(vec![stg_rl_game()], vec![]); c.starts[0].mark = 15; c.warmup_max = 60; c.seed = seed;
                      Env::new(Arc::new(c), 3, Arc::new(BootCache::new())) };
    let (mut a, mut b, mut d) = (mk(1), mk(1), mk(2));
    for f in 0..300u32 {
        let act = (f * 2654435761) & 0x7F;
        let (oa, ob) = (a.step(act), b.step(act)); d.step(act);
        assert_eq!((oa.done, oa.events), (ob.done, ob.events));
    }
    assert_eq!(a.world().checksum(), b.world().checksum());
    assert_ne!(a.world().checksum(), d.world().checksum());
}

#[test]
fn warmup_retries_are_bounded_and_deterministic() {
    let mut c = cfg(vec![img(SHOOTER)], vec![]); c.warmup_max = 400; // 预热期间几乎必死 ⇒ 反复重试
    let c = Arc::new(c);
    let mut a = Env::new(c.clone(), 0, Arc::new(BootCache::new()));
    let mut b = Env::new(c, 0, Arc::new(BootCache::new()));
    let (oa, ob) = (run(&mut a, 400, 0), run(&mut b, 400, 0));
    let ra: Vec<i32> = oa.iter().filter(|o| o.done != 0).map(|o| o.warmup_retries).collect();
    let rb: Vec<i32> = ob.iter().filter(|o| o.done != 0).map(|o| o.warmup_retries).collect();
    assert_eq!(ra, rb);
    assert!(ra.iter().all(|&r| (0..=MAX_WARMUP_RETRIES as i32).contains(&r)));
}

#[test]
fn boot_cache_template_is_reused() {
    let c = Arc::new({ let mut c = cfg(vec![stg_rl_game()], vec![]); c.starts[0].mark = 10; c });
    let cache = Arc::new(BootCache::new());
    let mut e0 = Env::new(c.clone(), 0, cache.clone());
    let _e1 = Env::new(c, 1, cache.clone());
    e0.reset();
    assert_eq!(cache.len(), 1);
}

fn stg_rl_game() -> Image {
    let units: Vec<(String, String)> = stg_rl::bundled::bundled_sources("game").unwrap().iter()
        .map(|(n, s)| (n.to_string(), s.to_string())).collect();
    compile(&units).unwrap()
}
```

`BootCache` 另需 `pub fn len(&self) -> usize`。Run: `cargo test -p stg-rl --test env` → 编译失败。

- [ ] **Step 2: 实现 `env.rs`**（要点逐条，代码按此写全）

```rust
pub fn splitmix64(mut x: u64) -> u64 {
    x = x.wrapping_add(0x9E37_79B9_7F4A_7C15);
    x = (x ^ (x >> 30)).wrapping_mul(0xBF58_476D_1CE4_E5B9);
    x = (x ^ (x >> 27)).wrapping_mul(0x94D0_49BB_1331_11EB);
    x ^ (x >> 31)
}
const K1: u64 = 0xD1B5_4A32_D192_ED03;
const K2: u64 = 0xABC9_8388_FB8F_AC03;
```

`Env` 字段：`cfg: Arc<EnvConfig>`、`weights: Arc<Vec<f64>>`、`cache: Arc<BootCache>`、`env_index: u32`、`counter: u64`、`world: Box<World>`、`start_index: usize`、`ep_frames: u32`、`warmup_retries: u32`、`prev_graze: u32`、`prev_score: u64`、`prev_bombs: u8`。

`reset()`：
1. `let s = splitmix64(cfg.seed ^ (env_index as u64).wrapping_mul(K1) ^ counter.wrapping_mul(K2)); counter += 1;`
2. 起点：`u = (splitmix64(s ^ 1) >> 11) as f64 / (1u64 << 53) as f64`，按 `weights` 累积和选第一个 `u * sum < cum` 的下标（整数外的唯一浮点——断层线以上、确定性由相同位级运算保证）。
3. `for retry in 0..=MAX_WARMUP_RETRIES`：`seed = splitmix64(s ^ (retry as u64 + 2))`；`world = cache.template(..)`；`world.reseed(seed)`；`k = if retry == MAX_WARMUP_RETRIES { 0 } else { splitmix64(seed ^ 0xA5) % (warmup_max as u64 + 1) }`；随机游走跑 k 帧（方向表 `[0, UP, UP|RIGHT, RIGHT, DOWN|RIGHT, DOWN, DOWN|LEFT, LEFT, UP|LEFT]`，段长 `8 + r % 25`，`r` 由 `splitmix64` 链式推进）；每帧后若 `players[0].life_state != LIFE_ALIVE` 或本帧事件含 `EVT_PHASE_ENDED/EVT_SPELL_CAPTURED/EVT_SPELL_FAILED/EVT_STAGE_CLEARED` ⇒ `continue` 下一 retry；跑满 ⇒ `warmup_retries = retry; break`。
4. `ep_frames = 0`；`prev_graze/prev_score/prev_bombs` ← 当前自机值。

`step(action)`：`ev = [0; 8]`；`for _ in 0..frame_skip`：`step(world, &TABLES_V0, image, &InputFrame{frame, actions[0].buttons = action & ACTION_MASK})`；`ep_frames += 1`；扫 `world.frame_events()`：`EVT_PLAYER_DIED → ev[0]+=1`、`EVT_ENEMY_DIED → ev[3]+=1`、`EVT_SHOT_HIT_ENEMY → ev[4]+=1`、`EVT_ITEM_PICKED → ev[6]+=1`、四个段落事件 → `ev[7] = 1/2/3/4`（取本步最后一个）并记 `seg_hit` 若其 `EndOn` 在 `cfg.end_on` 中；增量：`graze − prev`（饱和到 i32）加进 `ev[1]`、`score − prev`（饱和到 i32）加进 `ev[2]`、`bombs` 下降量加进 `ev[5]`，并刷新 prev；判 done：`ev[0] > 0 ⇒ DIED`，否则 `seg_hit ⇒ SEGMENT`，否则 `ep_frames >= max_frames ⇒ TIMEOUT`；done≠0 ⇒ `break`。结束后若 done≠0：先装好 `StepOut{ep_frames, warmup_retries, start_index}`，再 `reset()`。

`Image` 须 `Send + Sync`（`Arc<EclImage>`）；`Env` 须 `Send`（`Box<World>` + `Arc`）。在 `lib.rs` 加编译期断言：`const _: fn() = || { fn s<T: Send>() {} s::<env::Env>(); };`

- [ ] **Step 3: 跑测试** — `cargo test -p stg-rl` 全绿（`reseed` 等价若在 Task 1 红过并改了方案，这里按报告的方案实现：每局 `new_game_at(seed, …)`，删 `BootCache` 缓存逻辑但保留类型与 `len`）；workspace 闸门三件套。

- [ ] **Step 4: Commit**

```bash
git add crates/stg-rl
git commit -m "feat(stg-rl): T5 env——起点加权采样/种子流/开局快照缓存+reseed/随机预热重试/events 8 列/done 0-3 自动 reset"
```

---

### Task 6: `vec_env` —— rayon 批量 step + 缓冲 + CSR；harness `rl-bench`

**Files:**
- Create: `crates/stg-rl/src/vec_env.rs`、`crates/stg-rl/tests/vec_env.rs`、`crates/stg-harness/src/rlbench.rs`
- Modify: `crates/stg-rl/src/lib.rs`（`pub mod vec_env;`）、`crates/stg-harness/Cargo.toml`（`stg-rl.workspace = true`）、`crates/stg-harness/src/main.rs`（`Some("rl-bench") => rlbench::cmd_rl_bench(&args[2..])` + usage 串）、`docs/bench-baseline.md`

**Interfaces:**
- Consumes: Task 5 `env::{Env, EnvConfig, BootCache, StepOut, validate, EVENTS}`；Task 4 `encode::*`；Task 3 `layout::{ENEMIES_CAP, ITEMS_CAP}`
- Produces（`stg_rl::vec_env`）：
  - `pub struct BufferSet<'a> { pub frame: &'a mut [u32], pub phase: &'a mut [u32], pub player: &'a mut [u8], pub enemies: &'a mut [u8], pub enemies_count: &'a mut [i32], pub bullets: &'a mut [u8], pub bullets_offsets: &'a mut [i32], pub items: &'a mut [u8], pub items_offsets: &'a mut [i32], pub lasers_count: &'a mut [i32], pub bullets_total: &'a mut [i32], pub bullets_dropped: &'a mut [i32], pub events: &'a mut [i32], pub done: &'a mut [u8], pub ep_frames: &'a mut [i32], pub warmup_retries: &'a mut [i32], pub start_index: &'a mut [i32] }`
  - `pub struct BufferSizes { … 同名字段: usize }`；`pub fn buffer_sizes(num_envs: usize, bullets_cap: usize) -> BufferSizes`（frame N、player N·36、enemies N·256·38、bullets N·cap·30、*_offsets N+1、events N·8，其余 N）
  - `pub struct VecEnv`：`pub fn new(cfg: EnvConfig, num_envs: usize, threads: usize) -> Result<VecEnv, String>`；`pub fn num_envs(&self) -> usize`；`pub fn bullets_cap(&self) -> usize`；`pub fn reset(&mut self, buf: &mut BufferSet) -> Result<(), String>`；`pub fn step(&mut self, actions: &[u32], buf: &mut BufferSet) -> Result<(), String>`；`pub fn set_start_weights(&mut self, w: Vec<f64>) -> Result<(), String>`

- [ ] **Step 1: 写失败测试** `crates/stg-rl/tests/vec_env.rs`

```rust
use stg_rl::env::*;
use stg_rl::vec_env::*;

struct Owned { frame: Vec<u32>, phase: Vec<u32>, player: Vec<u8>, enemies: Vec<u8>, enemies_count: Vec<i32>, bullets: Vec<u8>,
               bullets_offsets: Vec<i32>, items: Vec<u8>, items_offsets: Vec<i32>, lasers_count: Vec<i32>, bullets_total: Vec<i32>,
               bullets_dropped: Vec<i32>, events: Vec<i32>, done: Vec<u8>, ep_frames: Vec<i32>, warmup_retries: Vec<i32>, start_index: Vec<i32> }

impl Owned {
    fn new(n: usize, cap: usize) -> Owned {
        let s = buffer_sizes(n, cap);
        Owned { frame: vec![0; s.frame], phase: vec![0; s.phase], player: vec![0; s.player], enemies: vec![0; s.enemies],
                enemies_count: vec![0; s.enemies_count], bullets: vec![0; s.bullets], bullets_offsets: vec![0; s.bullets_offsets],
                items: vec![0; s.items], items_offsets: vec![0; s.items_offsets], lasers_count: vec![0; s.lasers_count],
                bullets_total: vec![0; s.bullets_total], bullets_dropped: vec![0; s.bullets_dropped], events: vec![0; s.events],
                done: vec![0; s.done], ep_frames: vec![0; s.ep_frames], warmup_retries: vec![0; s.warmup_retries], start_index: vec![0; s.start_index] }
    }
    fn view(&mut self) -> BufferSet<'_> {
        BufferSet { frame: &mut self.frame, phase: &mut self.phase, player: &mut self.player, enemies: &mut self.enemies,
                    enemies_count: &mut self.enemies_count, bullets: &mut self.bullets, bullets_offsets: &mut self.bullets_offsets,
                    items: &mut self.items, items_offsets: &mut self.items_offsets, lasers_count: &mut self.lasers_count,
                    bullets_total: &mut self.bullets_total, bullets_dropped: &mut self.bullets_dropped, events: &mut self.events,
                    done: &mut self.done, ep_frames: &mut self.ep_frames, warmup_retries: &mut self.warmup_retries, start_index: &mut self.start_index }
    }
    /// 有效数据的规范化快照：CSR 只比前缀（后缀是暂存区残留，不属于输出）。
    fn digest(&self) -> Vec<u8> {
        let nb = *self.bullets_offsets.last().unwrap() as usize * 30;
        let ni = *self.items_offsets.last().unwrap() as usize * 18;
        let mut v = Vec::new();
        for s in [&self.player[..], &self.bullets[..nb], &self.items[..ni], &self.done[..]] { v.extend_from_slice(s); }
        for s in [&self.enemies_count, &self.bullets_offsets, &self.items_offsets, &self.bullets_total, &self.bullets_dropped, &self.events, &self.ep_frames, &self.warmup_retries, &self.start_index] {
            for x in s.iter() { v.extend_from_slice(&x.to_le_bytes()); }
        }
        for (i, &c) in self.enemies_count.iter().enumerate() { v.extend_from_slice(&self.enemies[i * 256 * 38..][..c as usize * 38]); }
        for s in [&self.frame, &self.phase] { for x in s.iter() { v.extend_from_slice(&x.to_le_bytes()); } }
        v
    }
}

fn game_cfg(seed: u64) -> EnvConfig {
    let units: Vec<(String, String)> = stg_rl::bundled::bundled_sources("game").unwrap().iter().map(|(n, s)| (n.to_string(), s.to_string())).collect();
    EnvConfig { images: vec![compile(&units).unwrap()],
                starts: vec![Start { image: 0, mark: 10, rank: 2, weight: 1.0 }, Start { image: 0, mark: 15, rank: 2, weight: 2.0 }],
                frame_skip: 1, max_frames: 400, warmup_max: 60, end_on: vec![EndOn::PhaseEnded, EndOn::SpellCaptured, EndOn::SpellFailed, EndOn::StageCleared],
                bullets_cap: 256, seed }
}

fn actions(step: u32, n: usize) -> Vec<u32> { (0..n as u32).map(|i| (step.wrapping_mul(2654435761) ^ i.wrapping_mul(40503)) & 0x7F).collect() }

#[test]
fn deterministic_across_thread_counts() {
    let n = 16;
    let (mut a, mut b) = (VecEnv::new(game_cfg(5), n, 1).unwrap(), VecEnv::new(game_cfg(5), n, 8).unwrap());
    let (mut ba, mut bb) = (Owned::new(n, 256), Owned::new(n, 256));
    a.reset(&mut ba.view()).unwrap(); b.reset(&mut bb.view()).unwrap();
    assert_eq!(ba.digest(), bb.digest());
    let mut saw_done = false;
    for s in 0..300 {
        let act = actions(s, n);
        a.step(&act, &mut ba.view()).unwrap(); b.step(&act, &mut bb.view()).unwrap();
        assert_eq!(ba.digest(), bb.digest(), "step {s}");
        saw_done |= ba.done.iter().any(|&d| d != 0);
    }
    assert!(saw_done, "300 步随机动作应至少结束过一局（否则本测试没覆盖自动 reset）");
}

#[test]
fn different_seed_differs() {
    let n = 4;
    let (mut a, mut b) = (VecEnv::new(game_cfg(5), n, 2).unwrap(), VecEnv::new(game_cfg(6), n, 2).unwrap());
    let (mut ba, mut bb) = (Owned::new(n, 256), Owned::new(n, 256));
    a.reset(&mut ba.view()).unwrap(); b.reset(&mut bb.view()).unwrap();
    for s in 0..120 { let act = actions(s, n); a.step(&act, &mut ba.view()).unwrap(); b.step(&act, &mut bb.view()).unwrap(); }
    assert_ne!(ba.digest(), bb.digest());
}

#[test]
fn csr_offsets_are_consistent() {
    let n = 8;
    let mut e = VecEnv::new(game_cfg(9), n, 4).unwrap();
    let mut buf = Owned::new(n, 256);
    e.reset(&mut buf.view()).unwrap();
    for s in 0..200 {
        e.step(&actions(s, n), &mut buf.view()).unwrap();
        assert_eq!(buf.bullets_offsets[0], 0);
        for i in 0..n {
            let cnt = buf.bullets_offsets[i + 1] - buf.bullets_offsets[i];
            assert!(cnt >= 0 && cnt as usize <= 256);
            assert_eq!(cnt, buf.bullets_total[i] - buf.bullets_dropped[i]);
            assert!(buf.items_offsets[i + 1] >= buf.items_offsets[i]);
            assert_eq!(buf.lasers_count[i], 0);
        }
    }
}

#[test]
fn rejects_bad_buffers_actions_weights() {
    let n = 4;
    let mut e = VecEnv::new(game_cfg(1), n, 2).unwrap();
    let mut small = Owned::new(n, 128); // bullets 缓冲按 cap=128 分配，env cap 是 256
    assert!(e.reset(&mut small.view()).is_err());
    let mut buf = Owned::new(n, 256);
    e.reset(&mut buf.view()).unwrap();
    assert!(e.step(&[0; 3], &mut buf.view()).is_err());
    assert!(e.set_start_weights(vec![1.0]).is_err(), "长度须等于起点数");
    assert!(e.set_start_weights(vec![1.0, -1.0]).is_err());
    assert!(e.set_start_weights(vec![0.0, 3.0]).is_ok(), "允许单个为 0，总和须 > 0");
    assert!(VecEnv::new(game_cfg(1), 0, 2).is_err());
}
```

Run: `cargo test -p stg-rl --test vec_env` → 编译失败。

- [ ] **Step 2: 实现 `vec_env.rs`**

```rust
//! 批量 env（spec §3.1 / §7）：专属 rayon 池、不相交 &mut 切片并行、CSR 单线程压实。
use crate::encode::{self, BulletStats};
use crate::env::{validate, BootCache, Env, EnvConfig, EVENTS};
use crate::layout::{ENEMIES_CAP, ITEMS_CAP};
use rayon::prelude::*;
use std::sync::Arc;

pub struct BufferSizes { pub frame: usize, pub phase: usize, pub player: usize, pub enemies: usize, pub enemies_count: usize, pub bullets: usize,
                         pub bullets_offsets: usize, pub items: usize, pub items_offsets: usize, pub lasers_count: usize, pub bullets_total: usize,
                         pub bullets_dropped: usize, pub events: usize, pub done: usize, pub ep_frames: usize, pub warmup_retries: usize, pub start_index: usize }

pub fn buffer_sizes(n: usize, cap: usize) -> BufferSizes {
    BufferSizes { frame: n, phase: n, player: n * 36, enemies: n * ENEMIES_CAP * 38, enemies_count: n, bullets: n * cap * 30,
                  bullets_offsets: n + 1, items: n * ITEMS_CAP * 18, items_offsets: n + 1, lasers_count: n, bullets_total: n,
                  bullets_dropped: n, events: n * EVENTS, done: n, ep_frames: n, warmup_retries: n, start_index: n }
}

struct Slot { env: Env, bullets: Vec<u8>, items: Vec<u8>, sel: Vec<(i64, u16)>, nb: usize, ni: usize }

pub struct VecEnv { slots: Vec<Slot>, pool: rayon::ThreadPool, cap: usize, n_starts: usize, min_len: usize }
```

- `new`：`validate(&cfg)?`；`num_envs == 0 || threads == 0` ⇒ Err；`cfg = Arc::new(cfg)`；共享 `Arc<BootCache>`；`slots[i] = Slot { env: Env::new(cfg.clone(), i as u32, cache.clone()), bullets: vec![0; cap*30], items: vec![0; ITEMS_CAP*18], sel: Vec::with_capacity(cap), nb: 0, ni: 0 }`；`pool = ThreadPoolBuilder::new().num_threads(threads).thread_name(|i| format!("stg-rl-{i}")).build()`；`min_len = num_envs.div_ceil(threads)`。
- `check(buf)`：逐字段 `len == buffer_sizes(n, cap).<field>`，不等返回 `Err(format!("buffer {name}: len {got} != {want}"))`。
- 私有 `fn write_obs(slot, player_row, enemies_rows, enemies_count, frame, phase, total, dropped)`：调 `encode::write_player/write_enemies/phase_bits`、`write_bullets(world, cap, &mut slot.bullets, &mut slot.sel)`（记 `slot.nb`、total、dropped）、`write_items(world, &mut slot.items)`（记 `slot.ni`）。
- `reset`：`check`；并行 `env.reset()` + `write_obs`；events/done/ep_frames/warmup_retries/start_index 写 0；然后 `compact(buf)`。
- `step`：`check` + `actions.len() == n`；把 `buf` 的每个按-env 字段用 `chunks_mut` 切成 N 份，与 `slots.iter_mut()`、`actions` 一起 `zip` 成 `Vec<(…)>`（顺序构建），`self.pool.install(|| work.into_par_iter().with_min_len(min_len).for_each(|(slot, act, player, enemies, ec, fr, ph, tot, drp, ev, done, epf, wr, si)| { let o = slot.env.step(*act); ev.copy_from_slice(&o.events); done[0] = o.done; epf[0] = o.ep_frames; wr[0] = o.warmup_retries; si[0] = o.start_index; write_obs(…) }))`；然后 `compact(buf)`。
- `compact`：单线程 `off = 0; offsets[0] = 0; for (i, s) in slots: bullets[off*30..(off+s.nb)*30].copy_from_slice(&s.bullets[..s.nb*30]); off += s.nb; offsets[i+1] = off as i32;` items 同理；`lasers_count.fill(0)`。
- `set_start_weights`：长度 == `n_starts`、全部有限且 ≥ 0、总和 > 0，否则 Err；`Arc::new(w)` 分发给每个 `slot.env.set_weights`（下一次 reset 生效）。

zip 超过 rayon 元组嵌套不便时，定义 `struct Work<'a> { slot: &'a mut Slot, act: u32, player: &'a mut [u8], … }` 收集成 `Vec<Work>` 再 `into_par_iter()`。

- [ ] **Step 3: 跑测试** — `cargo test -p stg-rl` 全绿 + workspace 闸门。

- [ ] **Step 4: harness `rl-bench`** `crates/stg-harness/src/rlbench.rs`

```rust
//! `rl-bench [--envs N] [--steps S] [--threads T] [--mark M] [--rank R]`：stg-rl VecEnv 吞吐（spec §7）。
//! 不给 --threads ⇒ 依次 1,2,4,…（≤ available_parallelism、≤ envs）。输出每行：threads envs steps env_steps_per_s。
use std::time::Instant;
use stg_rl::env::{compile, EndOn, EnvConfig, Start};
use stg_rl::vec_env::{buffer_sizes, VecEnv};
```

实现：解析参数（默认 envs 512、steps 2000、mark 15、rank 2）；内置 game 包编译；`EnvConfig { frame_skip: 1, max_frames: 3600, warmup_max: 0, end_on: 全四个, bullets_cap: 1024, seed: 1, starts: [Start{image:0, mark, rank, weight:1.0}] }`；按 `buffer_sizes` 分配 `Vec` 缓冲（结构同测试 `Owned`，就地复制一份）；`reset` 后预跑 100 步，再计时 `steps` 步（动作：`(step*2654435761 ^ i*40503) & 0x53`，只含移动/射击/低速，不 bomb），打印 `env_steps_per_s = envs*steps/secs`。返回 `ExitCode::SUCCESS`。

- [ ] **Step 5: 跑基准并记账**

Run: `cargo run --release -p stg-harness -- rl-bench --envs 512 --steps 2000`
把输出表追加到 `docs/bench-baseline.md` 末尾新节「## stg-rl VecEnv 吞吐（2026-09-15，rl-bench，release）」：机器 `nproc` 值、表格（threads / envs / env_steps_per_s）、一句结论（加速比拐点在几线程）。按文件纪律只追加不改旧表。

- [ ] **Step 6: Commit**

```bash
git add crates/stg-rl crates/stg-harness Cargo.lock docs/bench-baseline.md
git commit -m "feat(stg-rl): T6 VecEnv——专属 rayon 池批量 step + BufferSet 校验 + CSR 压实 + 线程数无关确定性测试；harness rl-bench 与吞吐基线"
```

---

### Task 7: `stg-py` wheel + 分发 workflow + 文档收口

**Files:**
- Create: `crates/stg-py/Cargo.toml`、`crates/stg-py/build.rs`、`crates/stg-py/src/lib.rs`、`crates/stg-py/pyproject.toml`、`crates/stg-py/python/stg_rl/__init__.py`、`crates/stg-py/tests/test_smoke.py`、`crates/stg-py/README.md`、`.github/workflows/wheels.yml`
- Modify: 根 `Cargo.toml`（Task 3 已加 `exclude`，核对）、`.gitignore`（`crates/stg-py/target/`、`crates/stg-py/dist/`、`.venv/`）、spec（Ruling 1/4 偏差）、`PROGRESS.md`、`CLAUDE.md`、`docs/architecture.md`、`docs/follow-ups.md`

**Interfaces:**
- Consumes: Task 3 `layout::{TABLES, hello_json, EVENT_COLUMNS, BULLETS_CAP_DEFAULT}`、`bundled::bundled_sources`；Task 5 `env::{compile, Image, Start, EndOn, EnvConfig}`；Task 6 `vec_env::{VecEnv, BufferSet, buffer_sizes}`
- Produces（Python `stg_rl`）：`hello(bullets_cap=1024) -> str`、`OFFSETS: dict[str, dict[str, tuple[int, str]]]`、`STRIDES: dict[str, int]`、`EVENT_COLUMNS: tuple[str, ...]`、`build_info() -> dict`、`bundled_sources(pack) -> list[tuple[str, str]]`、`compile_sources(units) -> Image`、`compile_dir(path) -> Image`、`compile_bundled(pack="game") -> Image`、`CompileError`、`@dataclass Start(image: str, mark: int = 0, rank: int = 2, weight: float = 1.0)`、`alloc_buffers(num_envs, bullets_cap=1024, backend="torch", pin=True) -> dict`、`class VecEnv(num_envs, threads, images: dict[str, Image], starts: list[Start], frame_skip=1, max_frames=3600, warmup_max=120, end_on=("phase_ended","spell_captured","spell_failed","stage_cleared"), bullets_cap=1024, seed=0, buffers=None)`，属性 `.buffers`，方法 `.reset()`、`.step(actions)`、`.set_start_weights(weights)`

**Ruling 4（写计划时裁定）：** spec §3 的可选 gymnasium 适配 **v1 不做**——训练仓还没定框架，适配层按框架写才不白写；文档写明「禁止套 subprocess vector wrapper」即可。本 Task 同步改 spec。

- [ ] **Step 1: Rust 扩展**

`crates/stg-py/Cargo.toml`：

```toml
[package]
name         = "stg-py"
version      = "0.1.0"
edition      = "2024"
rust-version = "1.94"
publish      = false
description  = "stg_rl Python 扩展（PyO3 abi3，薄壳；不进主 workspace，见 stg-rl 计划 Ruling 2）"

[workspace]

[lib]
name = "_native"
crate-type = ["cdylib"]

[features]
extension-module = ["pyo3/extension-module"]

[dependencies]
stg-rl   = { path = "../stg-rl" }
stg-core = { path = "../stg-core" }
pyo3     = { version = "0.29.2", features = ["abi3-py310"] }
numpy    = "0.29"
```

`build.rs`：`git rev-parse --short HEAD`（失败则 `"unknown"`）→ `cargo:rustc-env=STG_GIT_SHA=…`；`cargo:rerun-if-changed=../../.git/HEAD`。

`src/lib.rs` 暴露（全部在 `#[pymodule] fn _native`）：
- `create_exception!(_native, CompileError, PyException)`
- `fn hello(bullets_cap: usize) -> PyResult<String>`：cap 不在 `1..=8192` ⇒ `ValueError`；否则 `layout::hello_json`。
- `fn layout_tables() -> Vec<(String, usize, Vec<(String, usize, String)>)>`：`(name, stride, [(field, off, type)])`，由 `TABLES` 生成。
- `fn event_columns() -> Vec<&'static str>`
- `fn build_info() -> HashMap<&'static str, String>`：`version = env!("CARGO_PKG_VERSION")`、`engine_ver`、`tables_hash = format!("{:016x}", TABLES_V0.content_hash)`、`git_sha = env!("STG_GIT_SHA")`。
- `fn bundled_sources(pack: &str) -> PyResult<Vec<(String, String)>>`：未知包 ⇒ `ValueError`。
- `#[pyclass] struct Image(env::Image)`；`fn compile_sources(units: Vec<(String, String)>) -> PyResult<Image>`：Err ⇒ `CompileError`。
- `#[pyclass] struct NativeVecEnv(vec_env::VecEnv)`（`VecEnv: Send`，rayon `ThreadPool` 满足），`#[new] fn new(num_envs, threads, images: Vec<PyRef<Image>>, starts: Vec<(usize, i32, i32, f64)>, frame_skip: u32, max_frames: u32, warmup_max: u32, end_on: Vec<String>, bullets_cap: usize, seed: u64) -> PyResult<Self>`（`end_on` 名字 → `EndOn`，未知名 ⇒ `ValueError`；`VecEnv::new` 的 Err ⇒ `ValueError`）。
- `fn reset(&mut self, py, bufs: &Bound<PyDict>) -> PyResult<()>` / `fn step(&mut self, py, actions: PyReadonlyArray1<u32>, bufs: &Bound<PyDict>) -> PyResult<()>` / `fn set_start_weights(&mut self, w: Vec<f64>) -> PyResult<()>`：
  从 dict 按 17 个固定键取 `PyReadwriteArray1<T>`（dtype 不符 / 非 C 连续 ⇒ `ValueError`，消息含键名），`as_slice_mut()` 组 `BufferSet`，`py.detach(|| self.0.step(actions, &mut set))`（PyO3 0.29 释放 GIL 的 API 名以 `cargo doc -p pyo3` 为准：`Python::detach` 或 `allow_threads`），Err ⇒ `ValueError`。
- 所有缓冲在 Python 侧都是**一维**数组（多维形状由 Python 包装层 `reshape` 出视图）：Rust 只见扁平切片。

- [ ] **Step 2: Python 包** `python/stg_rl/__init__.py`

```python
"""stg_rl —— stg-engine 强化学习批量 env（spec docs/superpowers/specs/2026-09-15-stg-rl-env-design.md）。

单进程：Rust 专属 rayon 池批量 step，写入本包分配的缓冲。**不要**再套 gymnasium AsyncVectorEnv /
SB3 SubprocVecEnv / make_vec_env —— 那会把整个 env 复制进子进程，吃掉全部性能。
训练侧请设置 torch.set_num_threads，避免与 rayon 抢核。
"""
from __future__ import annotations
from dataclasses import dataclass
from pathlib import Path
import numpy as np
from . import _native
from ._native import CompileError, Image, build_info, bundled_sources, event_columns

EVENT_COLUMNS = tuple(event_columns())
STRIDES = {name: stride for name, stride, _ in _native.layout_tables()}
OFFSETS = {name: {f: (off, ty) for f, off, ty in fields} for name, _, fields in _native.layout_tables()}
ENEMIES_CAP, ITEMS_CAP = 256, 1024
_END_ON = ("phase_ended", "spell_captured", "spell_failed", "stage_cleared")


def hello(bullets_cap: int = 1024) -> str:
    return _native.hello(bullets_cap)


def compile_sources(units) -> Image:
    return _native.compile_sources([(str(n), str(s)) for n, s in units])


def compile_dir(path) -> Image:
    files = sorted(Path(path).glob("*.ecl"), key=lambda p: p.name)
    if not files:
        raise ValueError(f"no .ecl files in {path}")
    return compile_sources([(p.name, p.read_text(encoding="utf-8")) for p in files])


def compile_bundled(pack: str = "game") -> Image:
    return compile_sources(bundled_sources(pack))


@dataclass
class Start:
    image: str
    mark: int = 0
    rank: int = 2
    weight: float = 1.0


def _layout(n: int, cap: int):
    """(键, 扁平长度, dtype, 视图形状)。与 Rust vec_env::buffer_sizes 一一对应。"""
    return [
        ("frame", n, np.uint32, (n,)), ("phase", n, np.uint32, (n,)),
        ("player", n * 36, np.uint8, (n, 36)),
        ("enemies", n * ENEMIES_CAP * 38, np.uint8, (n, ENEMIES_CAP, 38)), ("enemies_count", n, np.int32, (n,)),
        ("bullets", n * cap * 30, np.uint8, (n * cap, 30)), ("bullets_offsets", n + 1, np.int32, (n + 1,)),
        ("items", n * ITEMS_CAP * 18, np.uint8, (n * ITEMS_CAP, 18)), ("items_offsets", n + 1, np.int32, (n + 1,)),
        ("lasers_count", n, np.int32, (n,)), ("bullets_total", n, np.int32, (n,)), ("bullets_dropped", n, np.int32, (n,)),
        ("events", n * len(EVENT_COLUMNS), np.int32, (n, len(EVENT_COLUMNS))), ("done", n, np.uint8, (n,)),
        ("ep_frames", n, np.int32, (n,)), ("warmup_retries", n, np.int32, (n,)), ("start_index", n, np.int32, (n,)),
    ]


def alloc_buffers(num_envs: int, bullets_cap: int = 1024, backend: str = "torch", pin: bool = True) -> dict:
    """返回 {键: 形状化数组}。backend="torch" ⇒ torch 张量（pin=True 时 pinned）；"numpy" ⇒ numpy 数组。"""
    out = {}
    for key, flat, dt, shape in _layout(num_envs, bullets_cap):
        if backend == "torch":
            try:
                import torch
            except ImportError as e:
                raise ImportError("backend='torch' 需要安装 torch；测试 / CPU 调试请用 backend='numpy'") from e
            t = torch.from_numpy(np.zeros(flat, dtype=dt))
            out[key] = (t.pin_memory() if pin else t).reshape(shape)
        elif backend == "numpy":
            out[key] = np.zeros(flat, dtype=dt).reshape(shape)
        else:
            raise ValueError(f"unknown backend {backend!r}")
    return out


def _flat_numpy(buffers: dict, n: int, cap: int) -> dict:
    flat = {}
    for key, length, dt, _ in _layout(n, cap):
        if key not in buffers:
            raise ValueError(f"buffers 缺少键 {key!r}")
        a = buffers[key]
        a = a.numpy() if hasattr(a, "numpy") and not isinstance(a, np.ndarray) else a
        a = a.reshape(-1)
        if a.dtype != dt or a.size != length or not a.flags["C_CONTIGUOUS"]:
            raise ValueError(f"buffer {key!r}: 需要 {np.dtype(dt)}×{length} 连续数组，得 {a.dtype}×{a.size}")
        flat[key] = a
    return flat


class VecEnv:
    def __init__(self, num_envs: int, threads: int, images: dict, starts: list, frame_skip: int = 1,
                 max_frames: int = 3600, warmup_max: int = 120, end_on=_END_ON, bullets_cap: int = 1024,
                 seed: int = 0, buffers: dict | None = None):
        names = list(images)
        for s in starts:
            if s.image not in images:
                raise ValueError(f"Start.image {s.image!r} 不在 images 里")
        self.num_envs, self.bullets_cap = num_envs, bullets_cap
        self._native = _native.NativeVecEnv(
            num_envs, threads, [images[k] for k in names],
            [(names.index(s.image), s.mark, s.rank, float(s.weight)) for s in starts],
            frame_skip, max_frames, warmup_max, list(end_on), bullets_cap, seed)
        self.buffers = buffers if buffers is not None else alloc_buffers(num_envs, bullets_cap, backend="numpy")
        self._flat = _flat_numpy(self.buffers, num_envs, bullets_cap)

    def reset(self) -> dict:
        self._native.reset(self._flat)
        return self.buffers

    def step(self, actions) -> dict:
        a = actions.numpy() if hasattr(actions, "numpy") and not isinstance(actions, np.ndarray) else actions
        self._native.step(np.ascontiguousarray(a, dtype=np.uint32).reshape(-1), self._flat)
        return self.buffers

    def set_start_weights(self, weights) -> None:
        self._native.set_start_weights([float(w) for w in weights])
```

- [ ] **Step 3: pyproject 与冒烟测试**

`pyproject.toml`：

```toml
[build-system]
requires = ["maturin>=1.15,<2"]
build-backend = "maturin"

[project]
name = "stg-rl"
version = "0.1.0"
description = "stg-engine batched reinforcement-learning environment (proto v1 observations)"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
torch = ["torch"]
test = ["pytest>=7"]

[tool.maturin]
module-name = "stg_rl._native"
python-source = "python"
features = ["extension-module"]
```

`tests/test_smoke.py`：

```python
import json
import numpy as np
import pytest
import stg_rl


def make(n=4, threads=2, cap=256, seed=0, **kw):
    img = stg_rl.compile_bundled("game")
    return stg_rl.VecEnv(n, threads, {"game": img}, [stg_rl.Start("game", mark=15)], bullets_cap=cap, seed=seed, **kw)


def test_build_info_and_hello():
    info = stg_rl.build_info()
    assert {"version", "engine_ver", "tables_hash", "git_sha"} <= set(info)
    h = json.loads(stg_rl.hello(512))
    assert h["backend"] == f"stg-engine@{info['engine_ver']}"
    assert [t["cap"] for t in h["tables"]] == [1, 512, 256, 64, 1024]
    assert stg_rl.STRIDES == {"player": 36, "bullets": 30, "enemies": 38, "lasers": 48, "items": 18}
    assert stg_rl.EVENT_COLUMNS[0] == "died" and len(stg_rl.EVENT_COLUMNS) == 8


def test_reset_step_shapes_and_done():
    env = make()
    b = env.reset()
    assert b["player"].shape == (4, 36) and b["bullets"].shape == (4 * 256, 30)
    seen = set()
    rng = np.random.default_rng(0)
    for _ in range(600):
        env.step(rng.integers(0, 128, size=4, dtype=np.uint32))
        seen |= set(np.unique(b["done"]).tolist())
        assert b["bullets_offsets"][0] == 0 and np.all(np.diff(b["bullets_offsets"]) >= 0)
    assert seen & {1, 2, 3}, "600 步随机动作应结束过至少一局"


def test_decode_with_stgagent_matches_offsets():
    schema = pytest.importorskip("stgagent.schema")
    env = make(n=2)
    b = env.reset()
    for _ in range(120):
        env.step(np.zeros(2, dtype=np.uint32))
    hello = schema.parse_hello(stg_rl.hello(256).encode())
    t = hello.table_by_name("bullets")
    n = int(b["bullets_offsets"][-1])
    if n == 0:
        pytest.skip("此刻场上无弹")
    rows = np.frombuffer(b["bullets"][:n].tobytes(), dtype=t.dtype)
    off = stg_rl.OFFSETS["bullets"]["x"][0]
    manual = np.frombuffer(b["bullets"][:n, off:off + 4].tobytes(), dtype="<i4")
    assert np.array_equal(rows["x"], manual)


def test_errors():
    img = stg_rl.compile_bundled("game")
    with pytest.raises(ValueError):
        stg_rl.VecEnv(2, 1, {"game": img}, [stg_rl.Start("nope")])
    with pytest.raises(ValueError):
        stg_rl.VecEnv(2, 1, {"game": img}, [stg_rl.Start("game", mark=999)])
    with pytest.raises(stg_rl.CompileError) as ei:
        stg_rl.compile_sources([("bad.ecl", "sub main( {")])
    assert "bad.ecl" in str(ei.value)
    bad = stg_rl.alloc_buffers(2, 128, backend="numpy")
    with pytest.raises(ValueError):
        stg_rl.VecEnv(2, 1, {"game": img}, [stg_rl.Start("game", mark=15)], bullets_cap=256, buffers=bad)
    with pytest.raises(ValueError):
        stg_rl.hello(0)
```

Run（本机）：

```bash
cd /data/sunyunbo/www/stg-engine/crates/stg-py
python3 -m venv .venv && . .venv/bin/activate
pip install -q maturin numpy pytest && pip install -q -e /data/sunyunbo/www/stg-agent-proto
maturin build --release -o dist && pip install -q --force-reinstall dist/stg_rl-*.whl
cd /tmp && python -m pytest -q /data/sunyunbo/www/stg-engine/crates/stg-py/tests
```

（`cd /tmp` 再跑，确保导入的是已安装的 wheel 而不是源码目录。）Expected: 4 passed。先按 TDD 写好测试、构建前跑一次确认 ImportError，再实现到全绿。

- [ ] **Step 4: 分发 workflow** `.github/workflows/wheels.yml`

```yaml
name: wheels

on:
  push:
    tags: ["rl-v*"]
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-24.04, id: linux-x86_64 }
          - { os: windows-latest, id: win-x86_64 }
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.10" }
      - name: build wheel (linux, manylinux_2_28)
        if: runner.os == 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release -m crates/stg-py/Cargo.toml --out dist
          manylinux: 2_28
      - name: build wheel (windows)
        if: runner.os == 'Windows'
        shell: bash
        run: pip install "maturin>=1.15,<2" && maturin build --release -m crates/stg-py/Cargo.toml --out dist
      - name: smoke test on installed wheel
        shell: bash
        run: |
          python -m pip install numpy pytest dist/*.whl
          cd "$RUNNER_TEMP" && python -m pytest -q "$GITHUB_WORKSPACE/crates/stg-py/tests"
      - uses: actions/upload-artifact@v4
        with: { name: "wheel-${{ matrix.id }}", path: dist/*.whl }

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/rl-v')
    runs-on: ubuntu-24.04
    permissions: { contents: write }
    steps:
      - uses: actions/download-artifact@v4
        with: { path: dist, merge-multiple: true }
      - uses: softprops/action-gh-release@v2
        with: { files: dist/*.whl }
```

（`stgagent` 在 CI 里不可得 ⇒ 该测试 `importorskip` 跳过；本机 Step 3 必须实跑到它。）本地用 `act` 不做要求；Step 3 本机 wheel 通过即视为构建脚本正确，首次 tag 由用户推。

- [ ] **Step 5: 文档收口**
- spec：§3 把 `bundled_content` 改为 `bundled_sources` / `compile_bundled`，删 gym 适配那条改为「v1 不做（Ruling 4）」；§10 内置内容一句同步；文末加「## 13. 实施偏差」表列 Ruling 1–4。
- `CLAUDE.md` 仓库结构：`crates/` 下加 `stg-rl/`（RL 批量 env 核心：layout/encode/env/vec_env）与 `stg-py/`（PyO3 wheel `stg_rl`，独立 workspace）；常用命令加 `cargo run --release -p stg-harness -- rl-bench` 与 stg-py 本地构建三行；Milestone 地图 M5 行补「M5 起步：stg-rl env 刀（2026-09-15）」。
- `docs/architecture.md`：crate 表加两行；M5 接缝行「还缺」改为「训练作业包 / 特征化胶水（训练仓）」。
- `docs/follow-ups.md`：新 `D23. stg-rl env 刀的非目标`（spec §12 列表逐条 + 触发点）。
- `PROGRESS.md`：「现在」段改为 stg-rl env 刀落地 + 下一步（proto 仓推 GitHub → 首个 `rl-v0.1.0` tag → 训练仓胶水 + 训练作业包；与第 1 关内容刀并列）；里程碑史表首插一行（测试计数、rl-bench 峰值、闸门）。

- [ ] **Step 6: 全闸门**

```bash
cd /data/sunyunbo/www/stg-engine
cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace 2>&1 | grep -E "^test result|FAILED|panicked"
cargo run -p stg-harness -- verify-tables
cargo run -p stg-harness -- golden --out /tmp/sunyunbo/g.txt && md5sum /tmp/sunyunbo/g.txt   # 54b5c5a3…
(cd crates/stg-py && cargo fmt -- --check && cargo clippy -- -D warnings)
bash crates/stg-godot/smoke/run-smoke.sh && bash godot/smoke/run-smoke.sh
```

Expected: 全绿；金向量 md5 不变；stg-py pytest 4 passed（Step 3）。

- [ ] **Step 7: Commit**

```bash
git add -A crates/stg-py .github/workflows/wheels.yml .gitignore Cargo.toml docs CLAUDE.md PROGRESS.md
git commit -m "feat(stg-py): T7 stg_rl wheel（PyO3 abi3，GIL 释放批量 step、缓冲校验、内置 game 包）+ wheels.yml tag 出包挂 Release + 文档收口"
```
