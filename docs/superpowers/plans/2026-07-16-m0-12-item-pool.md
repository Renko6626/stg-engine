# M0-12 道具池 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通道具经济线——敌死掉落（世界 RNG 散布）→ 重力/终速下落 → 三源磁吸（近距/PoC/`attract_all_items`）→ 行 5 拾取 → 趟三入账（四类 + 满 power 转化 + 蜡进位），扩展性四步清单落地。

**Architecture:** `items.rs` 数据模块（`define_pool!` 第五实例 + 类型常量 + `ItemTypeCfg` v0 常量配置表 + 掉落表）；运动/磁吸住 integrate 道具趟（敌人后 fields 前）；掉落挂 settle 趟二死亡结算；拾取 = 碰撞矩阵行 5 + 趟三唯一入账函数 `credit_item`。上游 spec：`docs/superpowers/specs/2026-07-16-item-pool-design.md`。

**Tech Stack:** Rust 2024 / stg-core。复用 `atan2`/`polar_to_vec`/`len_sq`/`Pcg32::rand_range(n: u32) -> u32`。

## Global Constraints

- **类型编号（冻结，钉死测试）**：`ITEM_POWER=0 / ITEM_POINT=1 / ITEM_LIFE_PIECE=2 / ITEM_BOMB_PIECE=3`，`ITEM_TYPE_COUNT=4`；哨兵 `MAGNET_NONE=0xFF / MAGNET_PICKED=0xFE`。
- **配置表 v0**：`ITEM_CFG: [ItemTypeCfg; ITEM_TYPE_COUNT]`——数组类型使"表长==类型数"成为编译期事实（加常量忘加行编不过）。默认参数（实测调参门）：弹出初速 3.0 / 重力 `ITEM_GRAVITY = Fx::from_raw(9_830)`(≈0.15) / 终速 `Fx::from_raw(144_179)`(≈2.2) / 磁吸 8.0 / 拾取半径 16 / 磁吸圈 40；分值 POWER=10、POINT=100、蜡=50。
- **入账常数**：`POWER_MAX: u16 = 128`、`PIECES_PER_LIFE: u8 = 5`、`PIECES_PER_BOMB: u8 = 5`（items.rs，账本规则跟道具走）。**满 power 转化**：power 已满时 POWER 按 `ITEM_CFG[ITEM_POINT].score` 入 score。
- **散布公式（钉死；消耗序 = 结算序、逐颗 vx 先 vy 后）**：`vx = Fx::from_raw(rng.rand_range(131_073) as i32 - 65_536)`（±1.0）、`vy = Fx::ZERO - cfg.eject_speed + Fx::from_raw(rng.rand_range(32_769) as i32)`（-3.0 + [0,0.5] 抖动）。
- **`POC_LINE_Y: i32 = 128`**（world.rs 场界常量区）。`POOL_ITEM: usize = 5`。`ROW_ITEM_PLAYER: u8 = 5`（events.rs）、`EVT_ITEM_PICKED: u8 = 4`。
- **行 5 自机门禁与行 1/2 同款**（`life_state != LIFE_ALIVE → continue`）。
- **次帧首动**：settle（相位 7）掉落的道具当帧只过 cleanup（相位 9），下一帧 integrate（相位 5）首动——相位序天然保证，测试钉住。
- **磁吸语义**：触发判定先于本帧移动；锁定后每帧重瞄直线急追；目标非 ALIVE → 解锁（`MAGNET_NONE`、`vx = 0`、vy 保持，当帧按下落逻辑走）。
- **TDD** 每任务红绿；合入前变异检验（T7）。commit 中文 conventional + 尾签 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

---

### Task 1+2（同一实现者、两次 commit，11a 先例的耦合红绿）: `items.rs` 数据模块 + `WorldBody.items` 接入

**Files:**
- Create: `crates/stg-core/src/items.rs`
- Modify: `crates/stg-core/src/lib.rs`（模块声明区加 `pub mod items;`，字母序）
- Modify: `crates/stg-core/src/world.rs`（`POOL_ITEM`/`POC_LINE_Y` 常量 + `items` 字段）
- Modify: `crates/stg-core/src/step.rs`（`copy_into` 加行 + 快照测试）

**Interfaces:**
- Produces: 上述全部常量；`ItemPool`/`ItemHandle`/`ItemInit`（宏生成）；`ItemTypeCfg { score: u32, eject_speed: Fx, terminal_vy: Fx, magnet_speed: Fx, pickup_radius: Fx, attract_radius: Fx }`；`pub(crate) ITEM_CFG`；`pub(crate) DROP_TABLES: &[&[(u8, u8)]]`（表 0 空、表 1 = [(POWER,2),(POINT,1)]）；`WorldBody.items: ItemPool`（pub，与四实体池同级——表现层将来要读）。

- [ ] **Step 0: 开分支**

```bash
git checkout -b m0-12-items
```

- [ ] **Step 1: 写失败测试**（`items.rs` 尾部 `mod tests`）

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::checksum::Checksum;
    use crate::math::Fx;

    /// 类型编号冻结（快照/回放契约；改动=有意识确认）。
    #[test]
    fn item_type_numbering_frozen() {
        assert_eq!(
            (ITEM_POWER, ITEM_POINT, ITEM_LIFE_PIECE, ITEM_BOMB_PIECE),
            (0, 1, 2, 3)
        );
        assert_eq!(ITEM_TYPE_COUNT, 4);
        assert_eq!((MAGNET_NONE, MAGNET_PICKED), (0xFF, 0xFE));
    }

    /// 配置表逐行健全：分值/物理参数为正、拾取半径 ≤ MAX_ENTITY_RADIUS（行 5 加法证明前提）。
    #[test]
    fn item_cfg_rows_sane() {
        for (t, cfg) in ITEM_CFG.iter().enumerate() {
            assert!(cfg.score > 0, "type {t}");
            assert!(cfg.eject_speed.raw() > 0 && cfg.terminal_vy.raw() > 0);
            assert!(cfg.magnet_speed.raw() > 0 && cfg.attract_radius.raw() > 0);
            assert!(
                cfg.pickup_radius.raw() > 0
                    && cfg.pickup_radius.raw() <= crate::world::MAX_ENTITY_RADIUS.raw(),
                "type {t} 拾取半径越出行 5 加法安全域"
            );
        }
        assert_eq!(ITEM_CFG[ITEM_POINT as usize].score, 100);
    }

    /// 掉落表：表 0 恒空（enemy.drop_table 零初始化默认 = 不掉）；表 1 = 标准杂鱼。
    #[test]
    fn drop_tables_shape() {
        assert!(DROP_TABLES[0].is_empty());
        assert_eq!(DROP_TABLES[1], &[(ITEM_POWER, 2), (ITEM_POINT, 1)]);
    }

    /// 池确定性 + 校验和敏感（与其余池同款纪律）。
    #[test]
    fn item_pool_deterministic_and_checksum_sensitive() {
        let mut w = crate::step::World::new(1);
        let base = w.body.items.checksum();
        let h = w
            .body
            .items
            .alloc(ItemInit {
                x: Fx::ZERO,
                y: Fx::ZERO,
                vx: Fx::ZERO,
                vy: Fx::ZERO,
                item_type: ITEM_POWER,
                magnet_to: MAGNET_NONE,
                timer: 0,
            })
            .unwrap();
        assert_ne!(w.body.items.checksum(), base);
        let i = w.body.items.get(h).unwrap();
        assert_eq!(w.body.items.magnet_to[i], MAGNET_NONE);
    }
}
```

`step.rs` tests 加快照往返（与 xforms 孪生、含判别腿）：

```rust
    #[test]
    fn snapshot_covers_item_pool() {
        let mut w = World::new(3);
        w.body
            .items
            .alloc(crate::items::ItemInit {
                x: Fx::from_int(5),
                y: Fx::from_int(6),
                vx: Fx::ZERO,
                vy: Fx::ZERO,
                item_type: crate::items::ITEM_POINT,
                magnet_to: crate::items::MAGNET_NONE,
                timer: 0,
            })
            .unwrap();
        let ck = w.checksum();
        let mut snap = World::new(3);
        w.copy_into(&mut snap);
        assert_eq!(snap.checksum(), ck);
        let i = 0;
        snap.body.items.item_type[i] ^= 1;
        assert_ne!(snap.checksum(), ck, "items 必须真的参与校验和");
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p stg-core items:: 2>&1 | head -5`
Expected: 编译失败（模块/字段不存在）——T1+T2 共享一个 RED，两 commit 一起转绿（既定先例）。

- [ ] **Step 3: 实现 `items.rs`**

```rust
//! 道具池（D7）——`define_pool!` 第五实例 + 类型常量 + 配置表 v0 + 掉落表。
//! 行为世界侧固定（账本公平性同级，D7）；**扩展性四步清单**（spec 2026-07-16）：
//! 新增类型 = ①加类型常量（编号只增不改）②`ITEM_CFG` 加行（数组类型强制表长==类型数）
//! ③`credit_item`（world/settle.rs）加 match 臂 ④按需加掉落表行。
//! 配置表 v0 = 引擎常量（player.rs 先例）；WorldTables 建成后整表搬家、结构体不动。

use crate::define_pool;
use crate::math::Fx;

// ── 类型编号（冻结；只增不改，钉死测试押运）─────────────────────────
pub const ITEM_POWER: u8 = 0;
pub const ITEM_POINT: u8 = 1;
pub const ITEM_LIFE_PIECE: u8 = 2;
pub const ITEM_BOMB_PIECE: u8 = 3;
pub const ITEM_TYPE_COUNT: usize = 4;

/// `magnet_to` 哨兵：未锁定 / 已拾取（等待 cleanup 回收）。0..MAX_PLAYERS = 锁定目标。
pub const MAGNET_NONE: u8 = 0xFF;
pub const MAGNET_PICKED: u8 = 0xFE;

// ── 入账常数（账本规则世界侧固定，D7/D9）────────────────────────────
pub const POWER_MAX: u16 = 128;
pub const PIECES_PER_LIFE: u8 = 5;
pub const PIECES_PER_BOMB: u8 = 5;

/// 全局重力（≈0.15 px/帧²；未锁定道具 vy += 至终速钉住）。
pub(crate) const ITEM_GRAVITY: Fx = Fx::from_raw(9_830);

/// 每类型配置（v0 引擎常量；金向量实测后调参）。
pub struct ItemTypeCfg {
    pub score: u32,
    pub eject_speed: Fx,
    pub terminal_vy: Fx,
    pub magnet_speed: Fx,
    pub pickup_radius: Fx,
    pub attract_radius: Fx,
}

const STD: ItemTypeCfg = ItemTypeCfg {
    score: 0, // 各行覆写
    eject_speed: Fx::from_int(3),
    terminal_vy: Fx::from_raw(144_179), // ≈2.2
    magnet_speed: Fx::from_int(8),
    pickup_radius: Fx::from_int(16),
    attract_radius: Fx::from_int(40),
};

/// 索引 = 类型编号；数组类型使"表长 == 类型数"成为编译期事实。
pub(crate) const ITEM_CFG: [ItemTypeCfg; ITEM_TYPE_COUNT] = [
    ItemTypeCfg { score: 10, ..STD },  // POWER
    ItemTypeCfg { score: 100, ..STD }, // POINT
    ItemTypeCfg { score: 50, ..STD },  // LIFE_PIECE
    ItemTypeCfg { score: 50, ..STD },  // BOMB_PIECE
];

/// 掉落表 v0：表 id → [(类型, 数量)]。表 0 = 空（enemy.drop_table 零默认 = 不掉）。
pub(crate) const DROP_TABLES: &[&[(u8, u8)]] = &[
    &[],
    &[(ITEM_POWER, 2), (ITEM_POINT, 1)], // 表 1：标准杂鱼
];

define_pool! {
    Item, cap = 512,
    fields {
        x: Fx, y: Fx, vx: Fx, vy: Fx,
        item_type: u8, magnet_to: u8, timer: u16
    }
}
```

（若 `..STD` 展开在 const 上不被接受——`ItemTypeCfg` 需 `Copy`？struct update 语法在 const 项里
对非 `Drop` 类型可用；如编译器拒绝就逐行写全六字段，不引入运行时初始化。）
`lib.rs` 加 `pub mod items;`（字母序）。**Commit ①**：

```bash
git add crates/stg-core/src/items.rs crates/stg-core/src/lib.rs
git commit -m "feat(items): 道具池数据模块——类型编号冻结 + 配置表 v0 + 掉落表 + 扩展四步清单"
```

- [ ] **Step 4: 接入 WorldBody**

`world.rs`：常量区 `pub const POOL_ITEM: usize = 5;`；场界常量区
`pub(crate) const POC_LINE_Y: i32 = 128; // 回收线（PoC）：ALIVE 自机 y 低于此线 → 全场道具磁吸`；
`WorldBody` 字段（`fields` 与 `xforms` 之间，与实体池同级）：

```rust
    pub items: crate::items::ItemPool,
```

`step.rs::copy_into`（`s.fields.copy_into` 之后）加 `s.items.copy_into(&mut d.items);`。

- [ ] **Step 5: 全绿 + Commit ②**

Run: `cargo test --workspace` → 全绿；fmt；clippy `-D warnings` 清。

```bash
git add crates/stg-core/src/world.rs crates/stg-core/src/step.rs
git commit -m "feat(world): items 池入 WorldBody——自动入校验和 + 快照拷贝 + POOL_ITEM/POC_LINE_Y"
```

---

### Task 3: 掉落——`spawn_drop` 散布 + `drop_item` API + settle 趟二接线

**Files:**
- Modify: `crates/stg-core/src/world.rs`（写 API 区两函数）
- Modify: `crates/stg-core/src/world/settle.rs`（死亡结算接线）

**Interfaces:**
- Consumes: `ITEM_CFG`/`DROP_TABLES`/`ItemInit`/`POOL_ITEM`/`STATUS_BAD_ARGS`/`rng.rand_range`。
- Produces: `pub(crate) fn spawn_drop(&mut self, x: Fx, y: Fx, item_type: u8) -> ItemHandle`（散布+alloc+P4-a 计数）；`pub fn drop_item(&mut self, x: Fx, y: Fx, item_type: u8) -> ItemHandle`（坏类型 → NULL+BAD_ARGS，余同 spawn_drop）。

- [ ] **Step 1: 写失败测试**（`step.rs` tests）

```rust
    /// 掉落散布消耗世界 RNG：同种子两跑逐位一致；不同颗速度不同（散布真的在动）。
    #[test]
    fn drop_item_scatter_deterministic() {
        let run = || {
            let mut w = World::new(11);
            let a = w.body.drop_item(Fx::ZERO, Fx::from_int(100), crate::items::ITEM_POWER);
            let b = w.body.drop_item(Fx::ZERO, Fx::from_int(100), crate::items::ITEM_POWER);
            let (ia, ib) = (w.body.items.get(a).unwrap(), w.body.items.get(b).unwrap());
            (
                w.body.items.vx[ia],
                w.body.items.vy[ia],
                w.body.items.vx[ib],
                w.body.items.vy[ib],
            )
        };
        let (ax1, ay1, bx1, by1) = run();
        let (ax2, ay2, bx2, by2) = run();
        assert_eq!((ax1, ay1, bx1, by1), (ax2, ay2, bx2, by2), "同种子逐位一致");
        assert!(ay1.raw() < 0, "弹出初速向上");
        assert!((ax1, ay1) != (bx1, by1), "两颗散布不同（RNG 真的被消耗）");
    }

    /// P4：坏类型 → NULL + BAD_ARGS；池满 → NULL + pool_full[ITEM]（B1 道具池份额）。
    #[test]
    fn drop_item_bad_type_and_pool_full() {
        let mut w = World::new(1);
        assert_eq!(
            w.body.drop_item(Fx::ZERO, Fx::ZERO, 9),
            crate::items::ItemHandle::NULL
        );
        assert_eq!(w.body.last_status, crate::world::STATUS_BAD_ARGS);
        for _ in 0..crate::items::ItemPool::CAP {
            w.body.spawn_drop(Fx::ZERO, Fx::from_int(50), crate::items::ITEM_POINT);
        }
        assert_eq!(
            w.body.drop_item(Fx::ZERO, Fx::ZERO, crate::items::ITEM_POINT),
            crate::items::ItemHandle::NULL
        );
        assert_eq!(w.body.diag.pool_full[crate::world::POOL_ITEM], 1);
    }
```

`settle.rs` tests 加（用既有 `spawn_enemy`+杀敌套路——`test_support::spawn_enemy` 的
`EnemyInit.drop_table` 现恒 0，需给 test_support 加一个 `spawn_enemy_with_drops` 变体或
直接就地构造 EnemyInit——**选后者**，照抄夹具 + `drop_table: 1`）：

```rust
    /// 敌死按 drop_table 掉落：表 1 = 2 POWER + 1 POINT，落点 = 敌死位置（散布只改速度）。
    /// 两敌同帧死 → 掉落顺序 = 结算序（低索引敌先掉，RNG 消耗序钉死）。
    #[test]
    fn settle_death_drops_by_table_in_settlement_order() {
        let mut w = crate::step::World::new(7);
        // …就地构造两个 drop_table=1、hp=1 的敌人（横向错开），一发自机弹各命中——
        // 夹具照本文件既有杀敌测试改（略去重复：实现者复用本文件 settle_shot_kills_enemy_* 的摆位），
        // 断言：items 存活恰 6 颗；前 3 颗 x == 敌A.x、后 3 颗 x == 敌B.x（结算序）；
        // 类型序 = [POWER, POWER, POINT] × 2（表内序）。
    }
```

（此测试骨架在计划里给到断言口径，夹具摆位实现者按本文件既有杀敌测试改——该文件已有
两个完整先例可抄，属机械改造而非发明。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p stg-core drop_item 2>&1 | head -5`
Expected: 编译失败（方法不存在）。

- [ ] **Step 3: 实现**

`world.rs` 写 API 区：

```rust
    /// 掉落一颗道具（内部核；散布消耗世界 RNG——消耗序 = 调用序 = 结算序，A6）。
    /// P4-a：池满 → NULL + 计数。类型合法性由调用方保证（settle 走表、公开壳已验）。
    pub(crate) fn spawn_drop(&mut self, x: Fx, y: Fx, item_type: u8) -> crate::items::ItemHandle {
        let cfg = &crate::items::ITEM_CFG[item_type as usize];
        let vx = Fx::from_raw(self.rng.rand_range(131_073) as i32 - 65_536); // ±1.0
        let vy = Fx::ZERO - cfg.eject_speed + Fx::from_raw(self.rng.rand_range(32_769) as i32);
        match self.items.alloc(crate::items::ItemInit {
            x,
            y,
            vx,
            vy,
            item_type,
            magnet_to: crate::items::MAGNET_NONE,
            timer: 0,
        }) {
            Some(h) => h,
            None => {
                self.diag.pool_full[POOL_ITEM] = self.diag.pool_full[POOL_ITEM].wrapping_add(1);
                self.last_status = STATUS_POOL_FULL;
                crate::items::ItemHandle::NULL
            }
        }
    }

    /// 掉落一颗道具（公开写 API；将来 ECL syscall `drop_item` 直通）。
    /// P4-b：坏类型 → NULL + BAD_ARGS（散布 RNG **不**消耗——失败零副作用）。
    pub fn drop_item(&mut self, x: Fx, y: Fx, item_type: u8) -> crate::items::ItemHandle {
        if item_type as usize >= crate::items::ITEM_TYPE_COUNT {
            self.diag.contract_viol = self.diag.contract_viol.wrapping_add(1);
            self.last_status = STATUS_BAD_ARGS;
            return crate::items::ItemHandle::NULL;
        }
        self.spawn_drop(x, y, item_type)
    }
```

`settle.rs` 死亡结算函数（dying 标记之后、`EnemyDied` push **之前**——顺序任意但就此钉死）：

```rust
        // 掉落直接分配（A6/A7）：按 drop_table 查表展开；越界表 → P4-b 计数 + 视同空表。
        let table = self.enemies.drop_table[e] as usize;
        if table >= crate::items::DROP_TABLES.len() {
            self.diag.contract_viol = self.diag.contract_viol.wrapping_add(1);
        } else {
            let (ex, ey) = (self.enemies.x[e], self.enemies.y[e]);
            for &(ty, n) in crate::items::DROP_TABLES[table] {
                for _ in 0..n {
                    self.spawn_drop(ex, ey, ty);
                }
            }
        }
```

- [ ] **Step 4: 全绿 + Commit**

Run: `cargo test --workspace`（既有敌死测试不受影响——夹具 drop_table 恒 0 = 空表）。

```bash
git add crates/stg-core/src/world.rs crates/stg-core/src/world/settle.rs
git commit -m "feat(world): 掉落——spawn_drop 世界 RNG 散布 + drop_item API + settle 趟二 drop_table 接线"
```

---

### Task 4: integrate 道具趟——物理 + 三源磁吸 + `attract_all_items`

**Files:**
- Modify: `crates/stg-core/src/world/integrate.rs`（敌人趟之后、fields 之前插入道具趟 + 私有助手）
- Modify: `crates/stg-core/src/world.rs`（`attract_all_items` 公开写 API）

**Interfaces:**
- Consumes: `ITEM_CFG`/`ITEM_GRAVITY`/`MAGNET_NONE`/`MAGNET_PICKED`/`POC_LINE_Y`/`atan2`/`polar_to_vec`/`len_sq`/`LIFE_ALIVE`。
- Produces: `pub fn attract_all_items(&mut self, player: usize)`（坏索引/非 ALIVE → P4-b no-op+计数+BAD_ARGS）。

- [ ] **Step 1: 写失败测试**（`integrate.rs` tests；道具用 `w.body.drop_item` 造——但散布带随机速度，
物理测试需可控初速：直接 `items.alloc(ItemInit{..})` 摆（crate 内测试合法，先例充分））

```rust
    /// 次帧首动 + 重力到终速钉住：vy 从 -1.0 逐帧 +0.15，越过 2.2 即恒 2.2。
    #[test]
    fn item_falls_with_gravity_clamped_at_terminal() {
        let mut w = crate::step::World::new(1);
        w.body
            .items
            .alloc(crate::items::ItemInit {
                x: Fx::ZERO,
                y: Fx::from_int(100),
                vx: Fx::ZERO,
                vy: Fx::from_int(-1),
                item_type: crate::items::ITEM_POWER,
                magnet_to: crate::items::MAGNET_NONE,
                timer: 0,
            })
            .unwrap();
        let mut prev_vy = w.body.items.vy[0];
        for f in 0..40u32 {
            crate::step::step(&mut w, &InputFrame::empty(f));
            let vy = w.body.items.vy[0];
            assert!(vy.raw() >= prev_vy.raw(), "重力单调");
            assert!(
                vy.raw() <= crate::items::ITEM_CFG[0].terminal_vy.raw(),
                "永不超终速"
            );
            prev_vy = vy;
        }
        assert_eq!(prev_vy, crate::items::ITEM_CFG[0].terminal_vy, "40 帧后必达终速");
    }

    /// 近距磁吸：道具进磁吸圈（40px）即锁定并每帧重瞄直追；圈外不锁。
    #[test]
    fn item_attracts_within_radius_only() {
        let mut w = crate::step::World::new(1);
        // 自机在 (0,384)；道具 A 在 (0, 350)（距 34 < 40）、B 在 (0, 300)（距 84 > 40）
        for y in [350, 300] {
            w.body
                .items
                .alloc(crate::items::ItemInit {
                    x: Fx::ZERO,
                    y: Fx::from_int(y),
                    vx: Fx::ZERO,
                    vy: Fx::ZERO,
                    item_type: crate::items::ITEM_POINT,
                    magnet_to: crate::items::MAGNET_NONE,
                    timer: 0,
                })
                .unwrap();
        }
        crate::step::step(&mut w, &InputFrame::empty(0));
        assert_eq!(w.body.items.magnet_to[0], 0, "圈内锁定自机 0");
        assert_eq!(w.body.items.magnet_to[1], crate::items::MAGNET_NONE, "圈外不锁");
        // 锁定后向自机推进（y 增大、速率 = 磁吸速度）
        let y0 = w.body.items.y[0];
        crate::step::step(&mut w, &InputFrame::empty(1));
        assert!(w.body.items.y[0].raw() > y0.raw(), "朝自机（下方）追");
    }

    /// PoC：自机 y < 128 → 全场未锁定道具锁定；已拾取（0xFE）不受扰。
    #[test]
    fn poc_line_attracts_all_unlocked() {
        let mut w = crate::step::World::new(1);
        w.body.players[0].y = Fx::from_int(100); // 过线
        for k in 0..3 {
            w.body
                .items
                .alloc(crate::items::ItemInit {
                    x: Fx::from_int(k * 50 - 50),
                    y: Fx::from_int(200),
                    vx: Fx::ZERO,
                    vy: Fx::ZERO,
                    item_type: crate::items::ITEM_POWER,
                    magnet_to: crate::items::MAGNET_NONE,
                    timer: 0,
                })
                .unwrap();
        }
        w.body.items.magnet_to[2] = crate::items::MAGNET_PICKED; // 已拾取哨兵
        crate::step::step(&mut w, &InputFrame::empty(0));
        assert_eq!(w.body.items.magnet_to[0], 0);
        assert_eq!(w.body.items.magnet_to[1], 0);
        assert_eq!(w.body.items.magnet_to[2], crate::items::MAGNET_PICKED, "0xFE 不受扰");
    }

    /// 解锁回落：磁吸中目标死亡 → MAGNET_NONE + vx 清零，重力接管。
    #[test]
    fn magnet_unlocks_when_target_dies() {
        let mut w = crate::step::World::new(1);
        w.body
            .items
            .alloc(crate::items::ItemInit {
                x: Fx::from_int(50),
                y: Fx::from_int(200),
                vx: Fx::ZERO,
                vy: Fx::ZERO,
                item_type: crate::items::ITEM_POWER,
                magnet_to: 0, // 已锁定自机 0
                timer: 0,
            })
            .unwrap();
        w.body.players[0].life_state = crate::player::LIFE_DEATHWINDOW; // 非 ALIVE
        w.body.players[0].state_timer = 8;
        crate::step::step(&mut w, &InputFrame::empty(0));
        assert_eq!(w.body.items.magnet_to[0], crate::items::MAGNET_NONE, "解锁");
        assert_eq!(w.body.items.vx[0], Fx::ZERO, "垂直续落");
    }

    /// attract_all_items：合法目标全场上锁；坏索引/非 ALIVE → P4-b no-op + 计数。
    #[test]
    fn attract_all_items_api_contract() {
        let mut w = crate::step::World::new(1);
        w.body
            .items
            .alloc(crate::items::ItemInit {
                x: Fx::ZERO,
                y: Fx::from_int(50),
                vx: Fx::ZERO,
                vy: Fx::ZERO,
                item_type: crate::items::ITEM_POINT,
                magnet_to: crate::items::MAGNET_NONE,
                timer: 0,
            })
            .unwrap();
        w.body.attract_all_items(0);
        assert_eq!(w.body.items.magnet_to[0], 0);
        let cv0 = w.body.diag.contract_viol;
        w.body.attract_all_items(7); // 坏索引
        assert_eq!(w.body.diag.contract_viol, cv0 + 1);
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p stg-core integrate::tests::item 2>&1 | head -5`
Expected: FAIL/编译失败（道具趟与 API 不存在——道具不动、不锁）。

- [ ] **Step 3: 实现**

`integrate.rs` 敌人趟之后插入（升序掩码遍历同款骨架）：

```rust
        // 道具（D7）：触发判定先于移动；物理即状态（磁吸=magnet_to、下落=重力到终速）。
        let poc_player = (0..crate::MAX_PLAYERS).find(|&p| {
            self.players[p].life_state == crate::player::LIFE_ALIVE
                && self.players[p].y.raw() < Fx::from_int(super::POC_LINE_Y).raw()
        });
        let nw = self.items.alive.len();
        for w in 0..nw {
            let mut bits = self.items.alive[w];
            while bits != 0 {
                let i = w * 64 + bits.trailing_zeros() as usize;
                bits &= bits - 1;
                self.integrate_item(i, poc_player);
            }
        }
```

私有助手（同文件）：

```rust
    /// 单颗道具的一帧：触发（PoC / 近距）→ 磁吸或下落移动。已拾取（0xFE）静置待回收。
    fn integrate_item(&mut self, i: usize, poc_player: Option<usize>) {
        use crate::items::{ITEM_CFG, ITEM_GRAVITY, MAGNET_NONE, MAGNET_PICKED};
        let m = self.items.magnet_to[i];
        if m == MAGNET_PICKED {
            return;
        }
        let cfg = &ITEM_CFG[self.items.item_type[i] as usize];
        if m == MAGNET_NONE {
            if let Some(p) = poc_player {
                self.items.magnet_to[i] = p as u8;
            } else {
                let r2 = (cfg.attract_radius.raw() as i64) * (cfg.attract_radius.raw() as i64);
                for p in 0..crate::MAX_PLAYERS {
                    if self.players[p].life_state != crate::player::LIFE_ALIVE {
                        continue;
                    }
                    let d2 = crate::math::geom::len_sq(
                        self.players[p].x - self.items.x[i],
                        self.players[p].y - self.items.y[i],
                    );
                    if d2 <= r2 {
                        self.items.magnet_to[i] = p as u8; // 升序首个 = 低索引（I4）
                        break;
                    }
                }
            }
        }
        let m = self.items.magnet_to[i];
        if (m as usize) < crate::MAX_PLAYERS {
            let p = m as usize;
            if self.players[p].life_state != crate::player::LIFE_ALIVE {
                // 解锁回落：vx 清零、vy 保持，本帧起按下落走
                self.items.magnet_to[i] = MAGNET_NONE;
                self.items.vx[i] = Fx::ZERO;
            } else {
                // 每帧重瞄直追（磁吸速度恒定）
                let a = crate::math::cordic::atan2(
                    self.players[p].y - self.items.y[i],
                    self.players[p].x - self.items.x[i],
                );
                let (vx, vy) = crate::math::geom::polar_to_vec(cfg.magnet_speed, a);
                self.items.vx[i] = vx;
                self.items.vy[i] = vy;
                self.items.x[i] = self.items.x[i] + vx;
                self.items.y[i] = self.items.y[i] + vy;
                return;
            }
        }
        // 未锁定/刚解锁：重力到终速钉住
        let nvy = self.items.vy[i] + ITEM_GRAVITY;
        self.items.vy[i] = if nvy.raw() > cfg.terminal_vy.raw() {
            cfg.terminal_vy
        } else {
            nvy
        };
        self.items.x[i] = self.items.x[i] + self.items.vx[i];
        self.items.y[i] = self.items.y[i] + self.items.vy[i];
    }
```

`world.rs` 写 API 区：

```rust
    /// 全场磁吸（bomb / 导演 / 将来 ECL syscall 的通用入口）：全部未锁定道具锁定该自机。
    /// P4-b：坏索引或目标非 ALIVE → no-op + 计数 + BAD_ARGS。
    pub fn attract_all_items(&mut self, player: usize) {
        if player >= crate::MAX_PLAYERS
            || self.players[player].life_state != crate::player::LIFE_ALIVE
        {
            self.diag.contract_viol = self.diag.contract_viol.wrapping_add(1);
            self.last_status = STATUS_BAD_ARGS;
            return;
        }
        let nw = self.items.alive.len();
        for w in 0..nw {
            let mut bits = self.items.alive[w];
            while bits != 0 {
                let i = w * 64 + bits.trailing_zeros() as usize;
                bits &= bits - 1;
                if self.items.magnet_to[i] == crate::items::MAGNET_NONE {
                    self.items.magnet_to[i] = player as u8;
                }
            }
        }
    }
```

- [ ] **Step 4: 全绿 + Commit**

```bash
git add crates/stg-core/src/world/integrate.rs crates/stg-core/src/world.rs
git commit -m "feat(world): 道具趟——重力/终速物理 + 三源磁吸（近距/PoC/attract_all_items）+ 解锁回落"
```

---

### Task 5: 行 5 拾取 + 趟三 `credit_item` 入账 + cleanup 回收

**Files:**
- Modify: `crates/stg-core/src/events.rs`（`ROW_ITEM_PLAYER = 5`、`EVT_ITEM_PICKED = 4`）
- Modify: `crates/stg-core/src/world/collide.rs`（行 5，置于行 4 与行 6 之间——行序遍历 I4）
- Modify: `crates/stg-core/src/world/settle.rs`（趟三 `ROW_ITEM_PLAYER` 臂 + `credit_item`）
- Modify: `crates/stg-core/src/world/cleanup.rs`（道具回收：0xFE 或越界）

**Interfaces:**
- Consumes: 全部前序产物。
- Produces: `fn credit_item(&mut self, p: usize, item_type: u8)`（settle.rs 私有——唯一 per-type
  逻辑居所，扩展四步的第 ③ 步落点，函数文档写明）。

- [ ] **Step 1: 写失败测试**（`settle.rs` tests）

```rust
    /// 行 5 判别几何：拾取半径 16 + graze 16 = 32——道具距自机 30 拾、34 不拾（非圆心重合）。
    #[test]
    fn item_pickup_discriminates_radius_sum() { /* 摆两颗道具于 (0,384±30/±34)…
        断言 events 恰 1 条 EVT_ITEM_PICKED、data[0]=类型、拾中那颗 magnet_to==0xFE、另一颗仍 0xFF */ }

    /// 四类入账 + 满 power 转化：power=127 吃 POWER → 128；再吃 POWER → power 不动、score += 100。
    #[test]
    fn credit_power_caps_then_converts_to_point_score() { /* 直调 credit_item 三次断言账本 */ }

    /// 蜡进位跨界：life_pieces=4 再吃 1 → lives+1、pieces==0；bombs 同构。
    #[test]
    fn piece_carry_crosses_boundary_exactly() { /* 直调断言 */ }

    /// 同帧双拾取幂等：两自机圈都罩住一颗道具？——players[1] 恒缺席，改为同自机双 hit 不可能
    /// （行 5 每对至多一 hit）；退而测：趟三首见置 0xFE 后，二次 credit 由 0xFE 跳过（手工双推 hits）。
    #[test]
    fn item_picked_only_once() { /* 手工 push_hit 两条同道具 hit → settle → 断言账本只入一次 */ }

    /// 拾取入账事件字段：a_index/a_gen = 道具句柄位、data = [类型, 玩家号]。
    #[test]
    fn item_picked_event_shape() { /* 拾一颗断言 Event 各字段 */ }
```

（判别几何/直调/手工 push_hit 三种夹具本文件全有先例；计划给断言口径，摆位照抄邻测试。）
`cleanup.rs` tests 加：`item_recycled_after_pick_and_when_oob`（0xFE 当帧回收；越底 +64 回收）。

- [ ] **Step 2: RED**：`cargo test -p stg-core settle::tests::item 2>&1 | head -5` → 编译失败/断言红。

- [ ] **Step 3: 实现**

`events.rs`：`pub(crate) const ROW_ITEM_PLAYER: u8 = 5;`（行 4 与行 6 常量之间）、
`pub const EVT_ITEM_PICKED: u8 = 4;`。
`collide.rs` 行 4 块之后（外层自机、内层道具，门禁与行 1/2 同款）：

```rust
        // 行 5：道具 × 自机拾取圈（graze_radius 兼拾取圈，D7；主动半径查配置表）
        for p in 0..crate::MAX_PLAYERS {
            if self.players[p].life_state != crate::player::LIFE_ALIVE {
                continue;
            }
            let nw = self.items.alive.len();
            for w in 0..nw {
                let mut bits = self.items.alive[w];
                while bits != 0 {
                    let i = w * 64 + bits.trailing_zeros() as usize;
                    bits &= bits - 1;
                    let pr = crate::items::ITEM_CFG[self.items.item_type[i] as usize]
                        .pickup_radius;
                    let r = pr + self.players[p].graze_radius;
                    let d2 = crate::math::geom::len_sq(
                        self.items.x[i] - self.players[p].x,
                        self.items.y[i] - self.players[p].y,
                    );
                    if d2 <= (r.raw() as i64) * (r.raw() as i64) {
                        self.push_hit(ROW_ITEM_PLAYER, i as u16, p as u16);
                    }
                }
            }
        }
```

`settle.rs` 趟三（graze 臂旁）加：

```rust
                ROW_ITEM_PLAYER => {
                    let it = hit.active as usize;
                    if !self.items.is_alive(it)
                        || self.items.magnet_to[it] == crate::items::MAGNET_PICKED
                    {
                        continue; // 首见即标：同帧多 hit 只入账一次
                    }
                    self.items.magnet_to[it] = crate::items::MAGNET_PICKED;
                    let ty = self.items.item_type[it];
                    self.credit_item(hit.passive as usize, ty);
                    let ev = Event {
                        kind: crate::events::EVT_ITEM_PICKED,
                        a_index: it as u16,
                        a_gen: self.items.generation[it],
                        x: self.items.x[it],
                        y: self.items.y[it],
                        data: [ty as i32, hit.passive as i32],
                    };
                    self.push_event(ev);
                }
```

`credit_item`（settle.rs，函数文档写"扩展四步第 ③ 步落点：新增类型在此加臂"）：

```rust
    /// 拾取入账（D9 趟三）——**唯一** per-type 逻辑居所（扩展四步第 ③ 步）。
    /// 未知类型：P4-b 计数忽略（两机同弃）。
    fn credit_item(&mut self, p: usize, item_type: u8) {
        use crate::items::*;
        let pl = &mut self.players[p];
        match item_type {
            ITEM_POWER => {
                if pl.power < POWER_MAX {
                    pl.power += 1;
                } else {
                    pl.score += ITEM_CFG[ITEM_POINT as usize].score as u64; // 满 power 转化
                }
            }
            ITEM_POINT => pl.score += ITEM_CFG[ITEM_POINT as usize].score as u64,
            ITEM_LIFE_PIECE => {
                pl.life_pieces += 1;
                if pl.life_pieces == PIECES_PER_LIFE {
                    pl.life_pieces = 0;
                    pl.lives += 1;
                }
            }
            ITEM_BOMB_PIECE => {
                pl.bomb_pieces += 1;
                if pl.bomb_pieces == PIECES_PER_BOMB {
                    pl.bomb_pieces = 0;
                    pl.bombs += 1;
                }
            }
            _ => {
                self.diag.contract_viol = self.diag.contract_viol.wrapping_add(1);
            }
        }
    }
```

（borrow 注意：`pl` 与 `self.diag` 冲突——未知臂先 drop pl 或改写为不取 `pl` 引用的形态；
实现者按编译器指引调整，语义不变。）
`cleanup.rs` 加道具回收趟（弹回收同款骨架）：`magnet_to == MAGNET_PICKED` 或
越界（沿用弹的 OOB 判定式）→ `free_index`。

- [ ] **Step 4: 全绿 + Commit**

```bash
git add crates/stg-core/src/events.rs crates/stg-core/src/world/collide.rs crates/stg-core/src/world/settle.rs crates/stg-core/src/world/cleanup.rs
git commit -m "feat(world): 行5 拾取 + 趟三 credit_item 四类入账（满power转化/蜡进位）+ 道具回收"
```

---

### Task 6: 金向量加戏 + 双跑

**Files:**
- Modify: `crates/stg-harness/src/main.rs`

- [ ] **Step 1**：导演敌人 `EnemyInit.drop_table: 0` → `1`（敌死自动掉落入流）；新增块 ⑫：
  `if frame % 200 == 90 { b.attract_all_items(0); }`（自机非 ALIVE 时 = 确定性 P4-b 计数，
  与 spiral_h 悬垂同款哲学，注释写明）；`cmd_golden` 文档补一句（含偏移 90）。
- [ ] **Step 2**：`cargo test --workspace` + fmt + clippy + 金向量双跑 diff → IDENTICAL。
- [ ] **Step 3**：

```bash
git add crates/stg-harness/src/main.rs
git commit -m "feat(harness): 金向量 M0-12 加戏——敌死掉落自动入流 + 每200帧全场磁吸（RNG散布/入账进对拍）"
```

---

### Task 7: 变异检验（无提交物；定向反向 Edit 还原，禁 git checkout/restore/stash）

- [ ] 变异 A：`credit_item` 蜡进位 `== PIECES_PER_LIFE` 改 `== PIECES_PER_LIFE + 1` →
  `piece_carry_crosses_boundary_exactly` 必红。还原。
- [ ] 变异 B：满 power 转化臂改成溢出也 `power += 1`（删 else）→
  `credit_power_caps_then_converts_to_point_score` 必红（debug 溢出或断言）。还原。
- [ ] 变异 C：近距磁吸 `d2 <= r2` 改 `d2 >= r2` → `item_attracts_within_radius_only` 必红。还原。
- [ ] `git diff --exit-code` = 0 + `cargo test -p stg-core` 全绿；三杀记报告。

---

### Task 8: 收尾——设计回写 + 文档 + 全绿门

- [ ] `stg-world-design.md` D7 道具节回写（各注明"spec 2026-07-16 / M0-12 实现定稿"）：
  配置表 v0 = 引擎常量落点 + 磁吸圈列（attract_radius）+ `0xFE` 已拾取哨兵 + 满 power 转化 +
  扩展四步清单指针（→ items.rs 模块文档）。
- [ ] `docs/follow-ups.md` **B1 改写**：道具池份额已还（drop_item 池满已测），条目收窄为
  "create_bullet / create_player_shot / create_enemy / create_field 四个仍零覆盖"。
- [ ] `PROGRESS.md`：史表顶加
  `| 2026-07-16 | M0-12 | 道具池：掉落/三源磁吸/行5拾取/四类入账——经济线打通，扩展四步清单 |`；
  现在段重写（下一步候选：bomb · 敌人 move_to · SPAWN_PATTERN+图样表 · M1 ECL）。
- [ ] `CLAUDE.md` 结构树实体数据模块行：`src/{bullets,shots,enemy,field,player}.rs` →
  `src/{bullets,shots,enemy,field,items,player}.rs`（说明"前四个"→"前五个是 define_pool! 实例"）。
- [ ] 全绿门：fmt / clippy / test / verify-tables / 金向量双跑。单 commit `docs:` 前缀，
  然后 `superpowers:finishing-a-development-branch`。

---

## Self-Review 记录

- **Spec 覆盖**：类型/编号/哨兵/配置表/编译期表长→T1；池+快照+POC_LINE_Y→T2；散布公式/消耗序/
  drop_item/settle 接线/越界表→T3；物理/三源/解锁/attract API→T4；行5/趟三入账/转化/进位/幂等/
  事件/回收→T5；金向量→T6；变异→T7；回写+B1 更新→T8。扩展性：四步清单进 items.rs 模块文档（T1）
  + credit_item 函数文档（T5）+ D7 回写指针（T8）。无缺口。
- **占位扫描**：T3/T5 三个测试给"断言口径 + 摆位照抄本文件先例"（settle.rs 有完整杀敌/几何/
  手工 push_hit 三类夹具先例，属机械改造）；其余全实码。
- **类型一致性**：`spawn_drop`（pub(crate)）/`drop_item`（pub）/`attract_all_items`（pub）/
  `credit_item`（settle 私有）贯穿一致；`ItemInit` 七字段处处同形；`POOL_ITEM=5`、
  `ROW_ITEM_PLAYER=5`、`EVT_ITEM_PICKED=4` 各处一致。
- **已知妥协**：T1+T2 耦合红绿（先例）；`..STD` const struct-update 若编译器拒绝则逐行写全
  （计划已注明降级路径）。
