
# 弹幕 STG 引擎设计文档

**版本 v0.3** — 在 v0.2 基础上，吸收 grill 评审：明确阶段优先级；取消静态 proto，改为哑创建原语 + 脚本层 shooter；钉死 ECL 任务生命周期、级联语义与派生时机。
**定位**：Godot 前端 + Rust 确定性内核的 2D 弹幕 STG 引擎，一等公民目标是 rollback 联机、逐帧回放、headless 高速模拟。

**阶段优先级（v0.3 明确）**：五大目标并非等价。排序为 **确定性（作为回放/CI 基底）→ headless/单机手感 → rollback 联机（北极星，但非首交付）**。**Phase 1 交付目标 = 确定性内核 + lockstep 联机**；rollback 是其后建立在已完成确定性之上的附带任务。但**交付目标分阶段、不变量（§2 I1–I7）不分阶段**：从第一天起完整持有全部不变量，rollback 后续接入才近乎免费。Phase 1 的 Definition of Done 不是"引擎能跑"，而是**一条复刻真实东方符卡的病态诊断场景（金向量）在 x86_64 与 aarch64 上逐帧校验和一致**。

---

## ⚠️ 阅读须知：本文是总纲，世界层细节以 `stg-world-design.md` 为准

本文 **v0.3** 是**架构总纲**。世界层的实施蓝图 [`stg-world-design.md`](./stg-world-design.md)（v1.0，经 20 轮评审拍板）
在若干处**推翻或细化**了本文的草案。冲突时**一律以后者为准**（见 `CLAUDE.md`）。

**本文正文保持原貌**（它是历史设计的记录）。下表是**已被修订条目的索引**——读到这些地方时，
别信正文，去查右栏指向的编号。

| 本文处 | 本文的说法（**已过期**） | 以此为准 |
|---|---|---|
| **§3.1** world 布局 | `spawn_q` 字段 | **删除**（A6 创建即分配） |
| | `events` 单缓冲、"帧末必空" | 拆为 **`hits` + `frame_events`**，生命周期改为"存活至下帧 `begin`"（A5） |
| | `stage: StageState` | **`globals: [i32; 1024]` + `boss_ui`**（A2） |
| | — | 新增 `signals` / `diag` / `last_status` / **`fields: FieldPool`**（D6） |
| **§3.2** 弹幕三档 | 单一笛卡尔运动模型 | **双表示**（POLAR_FX / CART_FX）(D3) |
| | 字段含 `layer: u8` | **`layer` 删除**——改"**池即层**"（见 §3.4 条）；`delay/ax/ay` 加入；字段以 **D3** 为准 |
| | `transform_head: u8` | **u16**（D3） |
| **§3.2 vs §10.5** | 两处 `MAX_XFORM_SLOTS` 自相矛盾 | 统一为 **16**；canned op 定稿 **17 个**（D4）；§10.5 关闭 |
| **§3.3** 敌人与 Boss | **"建议加入可注册回调 `on_died`"** | **否决**（P5 无回调——回调=函数指针=破 I7 memcpy 快照）。改 **`death_script` + 相位挂钩**（A7） |
| | 敌人字段草案 | 以 **D5** 为准（**双半径** `radius`(体碰)/`hurtbox`(受击)、`move_to` 插值器） |
| **§3.4** 层（u8 枚举） | 层是运行时 `u8` 枚举字段 | **池即层**——层是**静态的池身份**：`BulletPool`=EnemyBullet、`ShotPool`=PlayerShot、`EnemyPool`=EnemyBody、`FieldPool`=Field（D2/D7） |
| | 层名 `BombField` | **`Field`**——通用圆形作用区，**bomb 只是首个租户**（符卡清弹/阶段清场/死亡脚本清弹平权）(D6) |
| **§3.4** 矩阵行 6/7 | 主动方 `BombField` | **`Field`**，按 `flags` 能力位（`CLEAR_BULLETS`/`DAMAGE`）启用（D8） |
| | 行 6 事件 `BulletCleared`（逐弹） | **`FieldCleared`（聚合，每 field 每帧一条，带 `count`）**——逐弹在全屏消弹下爆 cap 16×（弹池 8192 vs events 512）(D9) |
| **§3.4** 硬规则段 | "事件按（矩阵行序, 主动索引, 被动索引）**排序**收集" | **非严格行主序**：行 1/2 为共用一次 `len_sq` 而按 弹×自机 对交错推送。真实不变量是"**嵌套固定 → 收集序确定**；settle 按行过滤，故跨行交错无影响"(D8/D9) |
| **§3.5** step 顺序 | v0.3 的相位序 | 以 **A4 v2** 为准（spawn_q flush 删除、新增相位挂钩、"boss 阶段推进检查"移出 cleanup） |
| **§3.7** 世界向外暴露字段 | 遗留疑问 | 已解答：**`WorldView` + `globals` + `boss_ui` + `frame_events`**（A9） |
| **§3.2.1 / §4.4** | `create_bullet(...)` 带 `task_script` 参数 | 移到 **syscall 绑定层组合**（世界不认识任务，P1/P2 推论：先 `world.create_bullet` 拿句柄，再 `ecl::spawn_task(script, owner=句柄)`）；§4.4 另新增 `pulse_signal`/`last_status`/`nearest_enemy`/`drop_item`；变换 locals ABI 每槽 3 字打包（D4） |
| **`static_ecl`**（§1.x 架构图/表、§4.2、§4.5——**全文散布**） | `static_ecl` 单一概念 | 拆为 **WorldTables + EclImage**（A3，合并内容哈希语义不变）。全文凡见 `static_ecl` 皆读作这两者之和 |
| **§5.1** ActionInput | `buttons: u8` | **u16**（A8） |
| **§10** 待拍板 | #5 变换槽/op 清单未决 | **关闭**（D4）；#4 容量初值已钉（D10，留实测调参）；#6/#3 维持已决 |

> **§2.4（池、句柄与分配）已于 M0-3 就地回写，与实现一致，不在此表。**
>
> 本表的权威来源是 `stg-world-design.md` 的 **Part V「对母文档的修订清单」**（其 10 条）
> 外加 M0-7/M0-8 实施期新产生的 3 条（§3.4 的层/矩阵/收集序）。后续若再有推翻，
> **先更新 Part V，再同步此表**。

---

## 0. 设计目标与非目标

**目标**

1. **确定性**：同一份（初始状态, 静态脚本, 输入序列）在所有受支持平台上产生 **bit 级完全一致**的世界演化。
2. **性能**：60 Hz 固定步进下，常态 4000+ 活跃弹幕，单步 < 0.5 ms——为 rollback 重演预留余量。
3. **联机**：输入 lockstep + rollback，网络稳态**只交换输入**。
4. **回放**：回放文件 = 头信息 + 输入日志，是确定性的免费副产品，同时兼任 desync 复现工具与回归测试向量。
5. **沙箱**：模拟核可完全脱离 Godot 独立运行（RL 训练、CI 测试、未来可能的服务器校验）。

**非目标**：通用游戏引擎；正经3D；模拟核内做任何渲染/音频；ECL 运行时热更（脚本一律预编译）。

---

## 1. 总体架构与断层线

五个层级，一条断层线：

```
┌──────────────────────────────────────────────────┐
│  表现层（Godot 壳 / headless 消费者）             │
│  MultiMesh 弹幕绘制 · 特效演出 · 音频 · UI        │
├────────────────── 断 层 线 ──────────────────────┤  ↑ 通道A：状态内存视图
│  模拟核 world（Rust，确定性）                     │  ↑ 通道B：帧渲染请求
│   ├ 弹幕世界层：实体池·运动·碰撞·事件结算          │  ↓ 唯一入口：InputFrame
│   └ ECL 执行层：字节码 VM · 任务（协程）池         │
├──────────────────────────────────────────────────┤
│  输入层：ActionInput 抽象（本地/网络/回放/AI）     │
│  网络层：输入交换 · 预测 · 回滚调度 · 校验和       │
└──────────────────────────────────────────────────┘
```

**层之间解耦约定**：

- 向下**只有一个入口**：每帧一份 `InputFrame`（§5），代表抽象的“这一帧有哪些外部操作”，让stg世界去读，抛弃掉所有其他内容。

- 向上**只有两个出口**：(A) 实体状态的只读内存视图(对于大量弹幕批量绘制）；(B) 本帧的离散渲染请求队列（类似zun的anm，针对少量敌人、主角和其他动画）（§6）。
- 断层线以下：不 import Godot、不读系统时钟、不用浮点、不产生任何 syscall 之外的副作用，保证能完全自己考。

- 断层线以上：不持有任何权威游戏状态，Godot节点只负责接收请求并绘制特效，或做任何“stg以外”的上层游戏。

**步进形式**：

```
world[n+1] = step(world[n], static_ecl, input_frame[n])
```

- `world` 携带**全部**可变状态——包括当前子弹和敌人表、 RNG 状态、ECL 任务VM状态、帧计数。
- `static_ecl` 是预编译只读数据（比如ecl脚本，他大量被读），内容哈希写入回放头与联机握手。
- `input_frame` 是**唯一**的每帧外部变量。
- step 内部的演化如果依赖此三者之外的任何东西（时钟、Godot 随机、未量化浮点输入）都是被禁止的。

### 1.1 crate / 模块分解——让断层线成为编译期事实

断层线不靠"大家记得别 import Godot"的自律，而靠 **Cargo 依赖图在物理上不可能跨界**：

```
        stg-ecl-compiler ──(离线/加载期, 产出 static_ecl)──┐
                                                          ▼
  stg-godot(gdext) ─┐                              [static_ecl 只读]
  stg-py(PyO3)     ─┼──依赖──►  stg-core  ◄──────────────┘
  stg-harness(CLI) ─┘         (确定性内核)
                    ┌───────────────────────────────────────┐
                    │ stg-core: World+池+定点数学+ECL VM+step  │
                    │ Cargo.toml 里【没有】godot/std-time/rand │
                    └───────────────────────────────────────┘
                              (phase 2) stg-net ──依赖──► stg-core
```

- **`stg-core`**：断层线**以下全部**。零 Godot、零系统时钟、零 `rand`——这些依赖**在 Cargo.toml 层面就不存在**，于是 §2 的"不读时钟/不用浮点/不碰宿主随机"从纪律升级为**编译期不可违反**。确定性契约的唯一持有者。
- **`stg-ecl-compiler`**：独立 crate，只在离线/加载期跑，产出 `static_ecl`（字节码 + 常量 + appearance 约定表），不进任何热路径（§4.5）。
- **`stg-godot` / `stg-py` / `stg-harness`**：三个平行消费者，都只依赖 `stg-core`，各自决定拿状态视图去画（MultiMesh，§6.4）、喂观测张量（§8）、还是对拍回归（§9）。
- **`stg-net`**：phase 2 才出现的传输 crate，同样架在 `stg-core` 之上（§7 分期声明）。

### 1.2 状态所有权——谁拥有什么、什么随快照

| 东西 | 谁拥有 | 随快照回滚？ | 身份保障 |
|---|---|---|---|
| `World`（帧 / RNG / 各池 / 任务；shooter 状态寄生在 `task.locals`，§3.2.1） | `stg-core` | **是**（唯一） | 字段级校验和（§3.6） |
| `static_ecl`（字节码 / 常量 / appearance 表） | 加载期常驻只读 | 否（不变） | 内容哈希进回放头 / 握手 |
| 烘焙表（sin/cos/easing） | 引擎二进制常量 | 否 | `engine_ver` + 表哈希（§2.1） |
| 表现层（Godot 节点 / 粒子 / 独立 RNG） | 表现层 | 否（非权威） | 回滚时通道 A 自愈（§6.3） |

**唯一权威可变状态是 `World`，且只有它随快照回滚。** 其余要么只读、要么非权威、要么可确定性再生。

### 1.3 阶段 → 结构映射——每个 milestone 落在哪些 crate

- **M0** `stg-core`（数学 + 池 + step 骨架 + 快照/回放）+ `stg-harness`（金向量逐帧对拍）
- **M1** `stg-core`（ECL VM + syscall 表）+ `stg-ecl-compiler`（Rust DSL 拼字节码）
- **M2** `stg-godot`（WorldBridge + MultiMesh + 请求分发器）
- **M3** `stg-core` 环形快照 + `stg-harness` 延迟/输入扰动/校验和风暴（本地回滚）
- **M4** `stg-net`（UDP + 会话/重同步）—— **phase 2 起点**
- **M5** `stg-py`（headless 并行 env）

**总纲**：`stg-core` 是确定性的唯一真相与唯一契约持有者；其余 crate 皆为其消费者，断层线由 Cargo 依赖图在编译期焊死。Phase 1（§7 声明）= `stg-core` + `stg-ecl-compiler` + `stg-harness` 三者，本地把金向量的跨平台逐帧一致跑通。

---

## 2. 确定性契约（七条不变量）

任何提交不得违反以下任何一条：

- **I1 数值**：模拟层唯一标量是定点数 **Q16.16（i32）**；`f32/f64` 不得出现在断层线以下。
- **I2 角度**：**BAM u16**（一圈 = 65536），三角函数一律查表。
- **I3 随机**：PRNG 状态是 `world` 的字段，随快照回滚；表现层使用另一颗独立 RNG，严禁触碰模拟 RNG。
- **I4 顺序**：一切遍历按池索引升序；分配用确定性 free-list；禁止 HashMap 等无序容器参与模拟。
- **I5 协程**：任何模拟协程的完整执行状态（ip + 栈 + 局部变量）必须位于**可 memcpy 的扁平内存**中，不依赖宿主语言调用栈。GDScript `await` 与 Rust `async` 均被禁止承载模拟逻辑（前者拿不到挂起状态，后者的编译器生成状态机无法序列化）。
- **I6 时间**：逻辑固定 60 Hz，一切计时用整数帧，step 不接受 delta。
- **I7 布局**：`world` 内不含指针/引用/堆容器；实体互引一律 generation-index 句柄；**快照 = 整块字节复制**。

### 2.1 定点数学核

- 标量 `Fx = Q16.16 (i32)`：范围 ±32768，精度 ≈ 1.5e-5。对 ~384×448 的弹幕场，位置、速度、加速度全部够用。
- 乘除走 i64 中间量：`mul(a,b) = ((a as i64 * b as i64) >> 16) as i32`。溢出纪律：debug 构建下溢出 panic，release 下 wrapping 并计入帧内断言。
- **BAM 角度**：u16 天然模 65536 回绕，加减免处理周期。`sin/cos` 用查表（四分之一波对称，16384 × i32 ≈ 64 KB），跨平台逐字节一致——这是绕开"libm 超越函数不保证一致"这堵墙的正解。
- **表的生成纪律（v0.3，关键）**：**绝不在各平台构建期用浮点现生成表**——那只是把"libm 不一致"从运行期挪到构建期（x86_64/aarch64 构建机的 libm 差 1 ULP → 烘焙出不同表 → desync），地雷仍在。正解走 **(a)：生成一次、把原始字节 commit 进仓库、用 `include_bytes!` 处处按字节嵌入**；二进制在任何平台/构建机含同一份字节，表"准不准"无所谓、只要处处相同。配套：① 保留生成器，CI 里重新生成并**断言与 commit 字节完全相同**（防手滑漂移）；② 显式钉死取整规则（round-half-to-even），它决定最低位；③ 所有 easing 曲线（§4.4 `move_to`）同守此纪律。表成为确定性契约的一部分。
- **表身份进握手**：sin/cos/easing 表是引擎二进制的一部分，由 §5.3/§7.2 的 `engine_ver` 覆盖；更稳的是在握手/回放头里额外带一个**烘焙表哈希**，同版本内表若悄悄漂移也能当场抓出，而非 desync 到一半才发现。
- 补齐一个小而全的自有原语集：`sin / cos / atan2（整数 CORDIC，纯整数天然确定、免二维表）/ isqrt（整数牛顿法）`。碰撞判定按 I1 用平方距离比较，正常路径**不需要开根**。

### 2.2 RNG

PCG32（state + inc，16 B）内嵌于 `world`。消耗顺序由 I4 的固定遍历序保证，单流即可。表现层的粒子抖动、屏幕震动噪声等用godot自己的RNG，与模拟无关。

### 2.3 时间与帧

`frame: u32` 从 0 起。所有 wait、CD、无敌帧、超时都是整数帧。显示端插值（若做）只发生在表现层。

### 2.4 池、句柄与分配

- 所有实体池**固定容量**（初版：弹幕 8192 / 敌人 256 / 自机弹 1024 / 道具 512 / ECL 任务 512），SoA 布局。
- 存活位掩码 + 索引升序遍历（顺序稳定）；**掩码即分配器**（最低空位优先分配、无独立 free-list；M0-3 偏离原 LIFO，确定性不变、少一个状态面）。
- 句柄 = `{ index: u16, generation: u16 }` 打包 u32；**每次 alloc generation+1**（活槽 gen≥1，杀零句柄；M0-3 细化原"复用时+1"）；`valid = alive && gen 匹配`，悬垂/越界/零句柄的访问确定性返回"已失效"（`Option::None`）。
- 组合收益：容量固定 + 无指针 ⇒ `world` 是一个平坦内存块，**快照就是 memcpy**，且句柄跨快照恢复后依然有效。
- **硬规则（支撑 §3.6 "哈希全槽不掩码"）**：分配/复用一个池槽时，创建路径（`create_bullet` 等）**必须写满该槽的所有字段**，不得留任何字段为陈旧值。M0-3 用 **exhaustive `Init`（编译强制写满）+ 宏全覆写单测**保证之（原 debug"遗留值"断言对合法同值写入会误报，弃用）。否则新实体继承死槽陈旧字节——仍确定，但是逻辑 bug。此纪律使"死槽字节确定"与"新实体正确"同时成立。

---

## 3. 弹幕世界层

### 3.1 world 布局（草案）

```rust
#[repr(C)]
pub struct World {
    frame: u32,
    rng: Pcg32,                        // 随快照回滚
    stage: StageState,                 // 关卡推进、boss 阶段、全局变量槽
    players: [PlayerState; MAX_PLAYERS],
    bullets: BulletPool,               // SoA, cap 8192
    shots:   ShotPool,                 // 自机弹
    enemies: EnemyPool,
    items:   ItemPool,
    tasks:   TaskPool,                 // ECL 任务（协程）上下文，§4.2
    spawn_q: SpawnQueue,               // 帧内暂存，帧末必空
    events:  EventQueue,               // 帧内碰撞/死亡事件，帧末必空
    reqs:    RenderReqBuf,             // 通道 B 输出缓冲，§6
}
```

快照与校验说明：

- `reqs` 是**纯输出缓冲**——回滚重演时会被确定性地再生，不纳入校验和。
- `spawn_q`、`events` 帧末保证为空，校验和跳过。其余字段全部参与快照与校验。
- **padding 陷阱**：`repr(C)` 的填充字节是垃圾值。本地 memcpy 快照没问题（原样存原样恢复），但**跨机校验和不能直接 hash 整块字节**——解决方案见 §3.6：**字段级逐数组哈希**，天然跳过字段间 padding。

### 3.2 弹幕三档

对于不同复杂度的弹幕，我们有三个档位和方案：

1. **哑弹**：纯运动学，每帧 `vel += acc; pos += vel`。字段：`x y vx vy ax ay: Fx`、`angle: u16(BAM)`、`sprite: u16`、`radius: Fx`、`layer: u8`、`flags: u8`、`life: u16`、`transform_head: u8`（具体字段设置待定，但要尽量少和精致）。绝大多数子弹属于此档。`radius` 的默认值来自 appearance→判定表（§3.2.1），但创建时**允许显式参数覆盖**。
2. **变换弹（canned transforms）**：存在“拐弯、等待、变速、曲线”等复杂效果的子弹，实现为哑弹 + 一段预排程变换序列，序列由脚本在发射时现编（**无静态 proto**，§3.2.1），创建时拷入弹自己拥有的槽。单槽 `{ at_frame: u16, op: u8, args: [Fx; 2] }`。op 覆盖十来个固定操作：变速、转向、瞄准自机、换贴图、启用重力、反弹一次……足以覆盖"等 N 帧后拐弯/加速"等常见zun行为，这可以参考ECL的**子弹额外效果表**。**槽位上限目前取定长，取一个"够用"的引擎常量 `MAX_XFORM_SLOTS = 16`（不够再上调）**；槽本身存于一个独立的变换槽池，由弹的 `transform_head` 索引，故哑弹不为不用的槽付内存。
3. **任务弹（tasked）**：本质上是一个“套着子弹贴图的敌人”，他能挂一个 ECL 任务（owner = 该弹句柄），任意复杂逻辑（接近触发、循环变速、与 boss 联动）。任务用 ECL 编写，更不涉及 proto。

### 3.2.1 无静态 proto：哑创建原语 + 脚本层 shooter

- **引擎侧只有三样**：`create_bullet(appearance, x, y, speed, angle, [变换序列], [task_script]) -> BulletHandle`、`create_bullets_batch(...)`（批量、一条 syscall 铺一整环，见 §4.4）、以及一张 **appearance_id → 判定半径/sprite 的静态表**（进 static_ecl 内容哈希）。`create_bullet` 按 appearance 查表把 `radius` 拷入弹，**显式参数可覆盖默认**。
- **shooter 是纯 ECL 用户态模式，引擎不认识它**（§4.2 末）：一个 shooter 就是脚本层维护的一组发射参数（一次几发、样式/速度/方向、若是变换弹挂哪些 effect），配好后调引擎的批量原语落地。它的状态存于任务的 `locals` 区（§4.2 已为此扩容）或全局变量竞技场，随 world 快照。
- 代价点破：取消 proto 后，引擎唯一强制的耦合只剩 appearance↔判定这张约定表，其余全靠脚本纪律（外观/判定不匹配、变换序列写错，引擎不代拦）。对 DIY 引擎可接受。
- 敌人生成遵循同一原则：无静态 proto，appearance→展示/判定走约定表 + 显式覆盖。

### 3.3 敌人与 Boss

敌人同样是池中数据，**不能用 Godot 节点**。字段：位置/速度（Fx）、`hp: i32`、判定半径、层、sprite/anm id、无敌帧、受击闪计时、掉落表 id、`task: Handle`（主控任务）等等。

Boss = 敌人 + 阶段状态（当前符卡 id、阶段血量刻度、超时计时）。阶段推进由其主控的ecl任务驱动："血量过阈值或超时 → 清弹、结算、宣言下一张符卡"。

死亡**不就地改世界**：产出 `EnemyDied` 事件入 `events`，建议加入一个可注册的回调函数(on_died？)由结算阶段统一处理（掉落、清弹、发死亡特效请求）。

### 3.4 碰撞：分层、矩阵、事件

**层**（u8 枚举）：`PlayerHit`（判定点）/ `PlayerGraze`（擦弹圈，兼道具拾取圈）/ `PlayerShot` / `EnemyBody` / `EnemyBullet` / `Item` / `BombField`。

**碰撞矩阵**（只检测启用对，按固定行序）：

| # | 主动 | 被动 | 产出事件 |
|---|------|------|----------|
| 1 | EnemyBullet | PlayerHit  | PlayerHitByBullet |
| 2 | EnemyBullet | PlayerGraze | Graze |
| 3 | EnemyBody   | PlayerHit  | PlayerHitByBody |
| 4 | PlayerShot  | EnemyBody  | EnemyDamaged |
| 5 | Item        | PlayerGraze | ItemPicked |
| 6 | BombField   | EnemyBullet | BulletCleared |
| 7 | BombField   | EnemyBody  | EnemyDamaged |

- 全部圆判定；比较用**平方距离**（i64 中间量），不开根（I1）。
- 现阶段直接 O(N×M)：敌弹×自机是 N×1，自机弹×敌人两侧都小。未来密度膨胀再加均匀网格 broadphase——**不改变任何接口**。
- **硬规则：碰撞阶段只收集事件，不改状态。** 事件按（矩阵行序, 主动索引, 被动索引）排序**收集**。但**结算消费不沿用矩阵行序，而是按事件类别分趟**（§3.5 step 9）：清除/防护 → 伤害 → 计分。顺序仍完全确定，只是重排以匹配手感（如同帧 bomb 保命）。

### 3.5 step 固定顺序（确定性的命门）

step 内部顺序成文如下，任何改动过评审：

1. **begin**：清空 `reqs / events / spawn_q`（上帧已保证为空，此为防御性清理）。
2. **输入译码**：`InputFrame` → 各自机意图（方向、低速、射击、bomb）。
3. **ECL 阶段**：按任务池索引升序推进所有活跃任务（关卡任务与实体任务同池同序）。推进每个任务前先做两道确定性门禁：(a) **owner 存活校验**——owner 句柄失效则就地杀死该任务、不执行其任务体（§4.2 生命周期规则 a）；(b) **`born_frame == 当前帧` 跳过**——本帧新派生的任务本帧不首跑，统一次帧才第一次执行（§4.3 派生语义）。通过门禁的任务经 syscall 产出：移动指令、发弹/生成敌人（入 `spawn_q`）、渲染请求（入 `reqs`）。每任务每帧设指令预算（如 10k 条）防死循环，超限**确定性地**杀任务并记事件。
4. **自机更新**：移动（定点、场界钳制）、射击 CD 与发弹（入 `spawn_q`）、bomb 触发、无敌/复活计时。
5. **生成落地**：flush `spawn_q` → 各池（free-list 确定分配）。当帧生成的实体当帧参与后续阶段。
6. **变换槽**：对变换弹执行到期的 canned op。
7. **运动积分**：所有池按索引序 `vel += acc; pos += vel`。
8. **碰撞检测**：按 §3.4 矩阵行序收集事件。
9. **事件结算**：**按类别分三趟消费**（每趟内仍按收集序，故全程确定）——
   - **趟一 · 清除/防护**：bomb 清弹（矩阵第 6/7 行）等，给被清弹打"已清除"标记。**先于中弹**，故同帧 bomb 能救下本会命中的弹。
   - **趟二 · 伤害**：扣血、敌人死亡（掉落/清弹/特效请求）、自机中弹（跳过已清除的弹 → 死亡或进 N 帧决死窗口；决死窗口是跨帧机制，与同帧 bomb 保命互补）。
   - **趟三 · 计分/拾取**：graze 计数（**逐弹一次**：弹上置 `grazed_by` 位掩码，每颗弹对每个自机只计一次，独立于中弹，擦到即计）、道具拾取。
10. **清理**：越界（含边距）、寿命尽、被杀实体回收入 free-list；boss 阶段推进检查。
11. `frame += 1`。此刻通道 A 状态视图与通道 B 请求队列对表现层可读。

### 3.6 快照、回滚、校验和

- **快照** = 对 `World` 整块字节复制（I7 保证合法）。粗算：8192 弹 × ~32 B + 其余池 ⇒ 首版 < 1 MB。环形缓冲保留最近 ~16 帧（≈16 MB），每帧一次 1 MB memcpy 在现代内存带宽下可忽略。
- **回滚** = 恢复帧 F 快照 + 用修正后的输入重演至当前帧。预算约束：最大回滚 R = 8 ⇒ 要求 8 × step < 4 ms，这就是 §0 单步 0.5 ms 指标的来源。
- **校验和（v0.3 定机制）**：**字段级哈希，非整块**——逐个 SoA 数组按固定字段序哈希，天然跳过字段间 padding（整块哈希会把 `repr(C)` 未初始化的 padding 垃圾算进去 → 假 desync）。
  - **哈希全部槽、不用 alive 掩码**：只要 World 分配时整块清零 + 无 `MaybeUninit` + 写入全走类型字段，死槽字节也是确定的（上次确定性写入的遗留值，两机相同），照哈希即一致；掩码只增码并引入 alive-mask 自身出错的风险。**前提硬规则见 §2.4。**
  - **SoA 数组内部无 padding**（连续同类型），整条哈希；padding 只在不同类型字段之间，字段级逐数组正好跳过。`spawn_q/events`（帧末必空）、`reqs`（纯输出）跳过。
  - **防漏**：哈希器用 `#[derive(Checksum)]` 之类 proc-macro 从结构体字段**自动生成**（单一真相源）。否则加了新字段忘了加进哈希器 → 该字段静默不参与校验 → 其上 desync 永远抓不到，比 padding 危险得多（静默）。至少 CI 加"结构体大小/字段数变更即红"的守卫。
  - **两种节奏、同一函数**：CI/金向量 = **逐帧**校验和（即 DoD 机制）；联机随包 = 每 **K=20** 帧采样（省带宽）。开发构建不一致即硬停并落盘双方 world + 输入日志；发布构建走重同步（§7.4）。

### 3.7 世界向外暴露字段

为了让godot的游戏本体能获取stg世界的字段，我建议加入一些变量空间是可以被读的，单独作为一个标记？

常见的：
玩家状态、boss状态、小怪状态、当前ecl的全局变量？

### 3.8 可扩展性

---

## 4. ECL 层

### 4.1 为什么必须自建 VM

rollback 要求协程状态可保存、可恢复、可随快照整块复制。GDScript `await` 的挂起状态埋在引擎内部拿不到；Rust `async` 的状态机由编译器生成、无反射、同样不可序列化；Lua coroutine 依赖别人的 runtime 且不保证确定性。所以模拟协程的唯一合法形态是：**执行状态显式地躺在我们自己的内存里**。这就是不变量 I5，也是自建字节码 VM 的根本理由——性能只是顺带的。

### 4.2 任务（Task）形态

```rust
#[repr(C)]
struct Task {
    alive: u8,
    script: u16,              // static_ecl 中的入口索引
    pc: u32,
    wait: u16,                // 剩余等待帧
    born_frame: u32,          // 派生帧戳；== 当前帧则本帧跳过（次帧首跑，§4.3）
    owner: Handle,            // 关卡 / 敌人 / 子弹；失效则任务自动死亡
    parent: Handle,           // 派生树标签，仅供显式 kill_children 遍历（默认不级联）
    sp: u8,  csp: u8,
    stack:  [i32; 64],        // 求值栈（定长、扁平）
    calls:  [CallFrame; 8],   // sub 调用栈
    locals: [i32; 64],        // 扩容：兼作脚本层 shooter 状态区（§3.2.1）
}
```

定长栈买到的是：任务池仍是平坦数组，快照零特判。`locals` 从 16 扩到 64 是为承载脚本层 shooter 状态——同屏敌人（任务载体）不过几个到几十个，`512 × 64 × 4 B ≈ 131 KB` 可忽略。代价是每任务 ~600 B 常驻，仍可接受。栈深编译期尽量静态检查，运行期越界为确定性报错（杀任务 + 记事件），绝不 UB。

**生命周期与级联语义（v0.3 拍板，两条轴分清）**：

- **owner 轴（实体 → 其任务）**：owner 句柄失效 → 任务自动死亡（step 3 门禁 a）。弹在则任务在。
- **parent 轴（任务 → 其 `spawn_task` 子任务）**：**默认不级联**。`parent` 仅是 `kill_children` 的显式标签。决定性理由：任务**正常完成（ret）是常态**，若完成即级联，则"控制线程发完弹一 return、它布下的整环弹的任务瞬间全死"是确定性群体灾难。danmaku 要 **detached-by-default（发射后分离）**，非 structured concurrency。孤儿子任务的 `parent` 句柄天然"已失效"且安全；任务终将因 owner 失效/完成/显式 kill 而死，泄漏由定容池确定性暴露。
- **保留显式 teardown**：`kill_task` 默认 `recursive=false`（只杀自己）；`kill_children` / `kill_task(recursive=true)` 才按池索引升序确定性整树清。"整树消失"永远是脚本显式意图（如 boss 阶段结束拆控制线程），与清弹（bomb/阶段清场）是独立机制。

### 4.3 指令集与语义（草案）

- **栈机**：push / pop / dup；算术区分整数与 Q16.16 语义（`addi/addf` 风格），乘除走 i64 中间量；比较 + `jz/jnz/jmp`；`call/ret`（sub 调用）。
- **`wait n`**：写 `wait` 计时并让出；帧首 `wait > 0` 则减一并跳过本帧执行。这就是协程的全部魔法——没有任何隐藏栈。
- **`spawn_task(script, owner)`**：派生子任务（ZUN ECL 的 async 语义），返回任务句柄。**默认次帧首跑**：子任务盖 `born_frame` 戳，step 3 遍历本帧跳过它——使"当帧跑不跑"与 free-list 槽索引**解耦**、时序可预测（派生链通常很短，一帧延迟微乎其微）。此选择与"每任务独立扁平栈"契合：step 3 恒为一层扁平迭代、任何帧边界可直接 memcpy 快照，`spawn_task` 退化为"占槽 + 盖戳 + 返句柄"零递归。配套 `kill_task(recursive=false 默认) / kill_children`（§4.2）。
  - *后期可选* `spawn_task_now`（当帧内联跑子任务到首个 wait）：真实用例窄（"N 个子任务且首 tick 必须本帧"；同帧一环弹用父任务里 `for { create_bullet }` 即可，无需子任务）。若做，**用 step 3 内 worklist 迭代 drain 而非宿主递归**（避免无界爆栈），深度由池容量封顶。phase 1 不实现。
- **syscall**：一切副作用的唯一通道（§4.4）。
- 值域统一 i32，按指令语义解释为原始整数或 Q16.16；初版不做类型系统，表层语言的编译器负责别配错。

### 4.4 syscall 表（ECL 与世界的全部接口）

**读**：`player_x/y(i)`、`self_x/y`、`self_hp`、`frame`、`rand() / rand_range()`（消耗模拟 RNG）、boss 阶段变量、全局变量槽。

**写**（经安全 API，不直接摸池；**无静态 proto**，§3.2.1）：

- `create_bullet(appearance, x, y, speed, angle, xform_off, xform_cnt, task_script) -> BulletHandle` —— 哑创建原语，全定长 i32 参数（穿栈干净）。`appearance` 经约定表定默认 `radius/sprite`，显式参数可覆盖。**变换序列走 locals 区间引用**：`(xform_off, xform_cnt)` 指向**本任务 `locals` 内打包好的槽**（≤ `MAX_XFORM_SLOTS`），syscall 把该段 locals 拷入弹自己拥有的槽——shooter"建一次序列、发多颗都引用同一区间"，序列可为编译期常量也可运行时现算。`xform_cnt=0` 即哑弹。帧内断言守 `xform_off + xform_cnt*slot_size ≤ 64`，越界确定性报错。`task_script` 非空则派生任务（owner = 新弹）；
- `create_bullets_batch(appearance, base, count, per_index_delta...) -> ...` —— 批量发射原语：一条 syscall 铺一整环/一整扇（按序号施加 `angle_step` 等增量），避免在 ECL 里 `for` 循环烧指令预算。shooter（脚本层）配好参数后调它落地。**创建原语一律确定、不内建 RNG**：随机散布（随机扇/随机环）由 ECL 自己 `for i { create_bullet(angle + rand_range(...)) }` 逐发完成，RNG 消耗序 = 循环序（保 I3/I4），代价是随机弹幕退回逐发 syscall——实测性能代价不大，换来确定性边界最简单。故批量原语只服务规则弹幕；
- `spawn_enemy(appearance, ...) -> EnemyHandle` —— 同样无 proto，appearance 走约定表 + 显式覆盖；
- `set_vel / set_acc / aim_player / move_to(t, x, y, easing)`（easing 曲线同样是定点查表）；
- `emit_req(req_id, args...)` —— 渲染/音频请求，写入通道 B（§6.2）；
- `set_var / boss_set(...)` —— 全局与 boss 变量槽。

syscall 是**白名单**：ECL 拿不到指针、越不出沙箱，最坏的脚本也只能浪费指令预算，不能破坏世界不变量。

### 4.5 编译管线与静态数据

面向手感的表层语言（GDScript/Python 风格：变量、if/while、sub、async spawn）→ 编译器（独立 Rust crate，离线或加载期运行）→ 字节码 + 常量表 + 弹幕 proto 表 = `static_ecl`（只读，内容哈希写入回放头与联机握手）。

**分期建议**：M1 先用 Rust builder / 宏 DSL 直接拼字节码，把 VM 语义与 syscall 表跑通；表层语言与编译器随后再上。语言设计不应阻塞引擎验证——而且这条路你在 THTK 工具链上已经走熟了。

---

## 5. 输入层：动作输入抽象

### 5.1 ActionInput

设备无关的"动作"是进入模拟的**唯一热路径**：

```rust
#[repr(C)]
struct ActionInput {            // 4 B / 人 / 帧
    buttons: u8,                // 抽象意义上的“动作id”，默认会绑定基本的移动、射击、减速、bomb等操作
    parameters : [i8],    // 可选的参数，虽然目前的操作都是“按键开关”式的0/1操作，将来可能需传参
    _pad: u8
}

struct InputFrame { frame: u32, actions: [ActionInput; MAX_PLAYERS] }
```

键位绑定发生在**表现层**；即让Godot引擎负责“把按下z键转换成发送射击操作”，stg模拟核永远不知道"Z 键"的存在，只见到量化后的动作位。原始浮点鼠标/摇杆值必须在断层线以上完成量化。

### 5.2 来源统一

同一个类型，四种生产者：**本地设备、网络对端、回放文件、AI 策略**，这样抽象之后，所有的东西都能共用一个接口。

### 5.3 回放

```
ReplayFile = Header { engine_ver, ecl_hash, seed, player_cfg }
           + [ActionInput; frames × players]
           + 可选：每 K 帧校验和
```

体积：2 人 × 4 B × 60 Hz ≈ 28 KB/分钟。内嵌校验和使回放兼任 **desync 复现器**：CI 中同一回放跑两遍、跨平台各跑一遍，断言逐点一致（§9）。

### 5.4 带外命令

暂停、读写快照、调试生成器、慢放等控制面走独立接口，不进入 step。规则：**凡改变 world 的带外操作**（如调试发弹）要么被记录为输入流中的扩展事件（保回放完整性），要么显式标记"本会话回放/联机失效"。二选一，不允许静默注入。

---

## 6. 渲染与表现层

### 6.1 两条输出通道

模拟核对表现层的全部供给只有两样：

**通道 A —— 状态视图**（每帧常驻，拉取式）。实体池的只读内存借用：弹幕的 x/y/angle/sprite/scale/alpha、敌人、自机、道具。表现层每显示帧在 step 后读取，用什么管线画是它自己的事——Godot 走 MultiMesh，headless 环境把同一段内存当观测张量，跳过不画也行。

**通道 B —— 渲染请求**（每帧离散，推送式）。本帧发生的一次性表现事件队列：爆炸、符卡宣言演出、音效、震屏、UI 提示。帧首清空、step 期间由核与 ECL 写入、step 后被表现层读走。请求是**纯输出**：回滚重演时会被确定性地再生，不参与快照校验。

这条分界的隐藏红利：**回滚时通道 A 自愈**——被回滚掉的中间帧根本没被画出来；需要特殊处理的只剩通道 B（§6.3）。这正是"渲染读状态"优于"渲染读累积事件流"的原因。

### 6.2 请求格式

```rust
#[repr(C)]
struct RenderReq {
    id: u16,          // 注册期驻留的请求名
    seq: u16,         // 帧内自增序号，(frame, seq) 全局唯一
    args: [i32; 6],   // 按 id 约定解释；坐标以 Q16.16 原样传出
}
```

- **id**：字符串名（`"boss_explode"`、`"se_shoot"`、`"spell_declare"`）在**编译/注册期**驻留为 u16；运行时无字符串——既省包体，也避免字符串比较进入任何热路径。
- **音效走同一通道**（id 命名空间区分），不另开机制。
- 容量：每帧上限 256 条；超限**确定性丢弃**并计数——表现可以掉，确定性不能破。

### 6.3 回滚下的表现策略

- **通道 A**：只画"本显示帧最后一次 step 后"的状态；重演的中间帧天然不可见。
- **通道 B 默认策略——投机播放 + 水位去重**：表现层维护 `presented_watermark`（已呈现到的帧号）。任何 step/重演产出的请求，`frame ≤ watermark` 的丢弃（已播过），`frame > watermark` 的播放并推进水位。误预测产生的"鬼影特效/音效"接受为格斗游戏级别的标准代价。
- 大型演出（符卡宣言、boss 登场）可按 id 标记 **confirmed_only**：只在越过回滚地平线（输入已确认的帧）后播放，用 2–3 帧延迟换绝不误播。
- 表现层自身的粒子、抖动使用独立 RNG（I3），与模拟无涉。

### 6.4 Godot 侧参考实现

- **gdext**（Rust GDExtension）包一个 `WorldBridge` 节点：`step(input)`、`bullet_view()`、`take_requests()`。
- **弹幕**：每显示帧把通道 A 的位置/角度/贴图索引转 float，写入 MultiMesh 实例缓冲，`RenderingServer.multimesh_set_buffer` 一次上传。4k 弹 × 每实例 8–12 float 的转换拷贝微不足道。**定点 → 浮点的转换只发生在这条边界上。**
- **请求分发器**：表现侧一张 id → 处理器的路由表，把 RenderReq 派发到特效场景 / AudioStreamPlayer / UI 动画——这就是 ZUN anm 层思想在本架构里的落点：**核出请求，壳做演出**。
- 自机立绘、boss 显示体等低频对象可以是正常 Godot 节点，但其权威状态永远来自通道 A，节点只是显示器。

---

## 7. 网络层：lockstep + rollback

**分期声明（v0.3）**：本节描述的是 **phase 2** 的完整联机形态（预测 + 回滚 + UDP 传输 + 会话/重同步，一整包）。**Phase 1 只取其中"确定 step + InputFrame 唯一入口"这一子集，并且只在本地验证**——喂同一份输入流给两个 world 实例、断言逐帧全等（即 §9 的确定性 CI），**不含任何真实网络传输**。理由：在有 rollback（预测）之前造真实 UDP lockstep，只会得到"丢包/抖动即卡顿"的纯延迟 lockstep，上 rollback 时又得大改；phase 1 要证明的确定性与输入管线完全不需要网线。传输层（UDP/会话/重同步）本就属于 rollback 那一档。故 phase 1 的 lockstep = 本地可验证的确定性性质，是后续联网与 rollback 的**基础设施**。

### 7.1 模型

**确定性内核**（同输入 ⇒ 同状态，这是 lockstep 的前提）+ **rollback 作为延迟隐藏**：本地不等对端，用预测输入（重复其最后已知输入——对"按住方向"占主导的 STG 是很好的预测器）先行步进；真输入到达且与预测不符时，回滚到分歧帧重演。

两个常数：本地固定输入延迟 **D = 2~3 帧**（显著压低回滚频率），最大回滚窗口 **R = 8 帧**（对端落后超出 R 则暂停等待，长时间超出触发重同步）。

### 7.2 线上协议

- 稳态每包：`{ from_frame, n, inputs[n]（冗余重发所有未 ack 区段）, ack, checksum_piggyback }`。每帧每人 4 B，包极小；UDP 即可，冗余重发天然抗丢包，**不需要可靠传输层**。
- 握手：`engine_ver`、`ecl_hash`、`seed`、槽位分配；初始状态由双方从同一确定性初始化各自构造（或传一次初始快照）。
- 帧率对齐：按对端 ack 差微调本地步速（±1 帧/秒级）防时钟漂移。

### 7.3 快照在联机中的角色（澄清 v0.1）

"世界可 memcpy"服务于**本地**存档与回滚；网络稳态**不发世界**。整块快照上网只有两处：入局同步、desync 恢复。v0.1 中"发包原则上只发操作和状态快照"应修正为：发操作；快照仅作恢复/校验用途。

### 7.4 desync 处理

每 K 帧校验和随包对拍。不一致时：开发构建 → 双方硬停，落盘各自 world 快照 + 完整输入日志（配合 §5.3 即最小复现）；发布构建 → 约定一侧为权威，推送全量快照重同步，表现层做一次短淡入遮掩跳变。

### 7.5 共场 / 分场（待拍板，§10）

本文档按**共场 co-op**（双自机同场、全场确定、整世界回滚）这一**超集**设计。若走 PoFV 式分场对战：单场模型完全不变，每人一个 world 实例，跨场攻击作为输入事件注入对方场——只会更简单，不会推翻任何本文决定。

---

## 8. Headless 与 RL 沙箱

- `world` + ECL VM 是纯 Rust crate：无 godot 依赖、零系统调用、零全局状态——这不是额外功能，是断层线约定的自然推论。
- **PyO3 绑定**：`env.step(actions) -> obs`；obs 就是通道 A 的 numpy 零拷贝视图（定点原样给出或在绑定层转 f32）。通道 B 在 headless 下无人消费，忽略即零成本。
- **并行**：每 env 一个 world，线程池横向铺开；world 无全局状态保证 env 间零干扰。
- **动作空间天然 = ActionInput**——§5.2 承诺的红利在此兑现。确定性使 RL 的诡异轨迹可以一键降级为回放文件逐帧审查。

---

## 9. 测试与工程保障

- **确定性 CI**：同一回放跑两遍断言全程校验和逐点一致；x86_64 与 aarch64 双跑对拍。把跨平台确定性当作**持续验证的性质**，而不是一次性信仰。
- **回放语料库**：每个开发期 desync / 崩溃自动归档为回放文件，回归集只增不减。
- **ECL VM fuzz**：随机字节码不得越权、不得 panic，只允许确定性报错（杀任务 + 事件）。
- **基准纳入 CI 阈值**：8192 弹满池单步时延、快照 memcpy 时延、8 帧回滚重演总时延。
- **帧内断言**（debug 构建）：碰撞阶段写状态、ECL 越出 syscall、池分配顺序异常——直接 panic，让违约在离案发现场最近处爆炸。

---

## 10. 待拍板问题

1. **共场 co-op vs 分场 versus**——影响快照范围与协议细节；当前按共场超集设计（§7.5）。**（未决）**
2. 位置精度：全 Q16.16 已足够；是否为超长命慢速弹保留 Q32.32 位置项（倾向不要，M0 用实测说话）。**（未决）**
3. ~~ECL 表层语法定稿时机~~ **（v0.3 已决）**：phase 1 金向量的表层语言是次要的、可后续调整——即 **M1 先用 Rust DSL 拼字节码把 VM/syscall 跑通，表层语言随后再上**（§4.5）。
4. 容量常数：8192 弹 / 2 人 / 512 任务是否匹配目标弹幕密度与机体设计——**等金向量实测说话**。**（未决）**
5. 变换槽：**（v0.3 部分已决）**数量取"绝对够用"引擎常量 `MAX_XFORM_SLOTS = 8`（不够再上调，§3.2）；**canned op 清单仍待定稿**。
6. ~~graze 判定细则~~ **（v0.3 已决）**：**逐弹一次**——弹上 `grazed_by` 位掩码，每颗弹对每个自机只计一次 graze，语义干净、可确定、不会因久留圈内刷分（§3.5 step 9 趟三）。
7. shooter 状态载体：`locals[64]` 区 vs 全局变量竞技场——v0.3 默认前者，容量是否够待金向量验证。**（未决）**

---

## 11. 实施阶段（建议）

- **M0** 数学核 + 池 + step 骨架 + 快照/回放。CLI 下跑通"输入日志 → 逐帧校验和"回归，无任何图形。
- **M1** ECL VM + syscall 表 + Rust DSL 拼字节码，跑通一张非平凡符卡。
- **M2** gdext 桥 + MultiMesh + 请求分发器——Godot 里第一次看见弹幕。
- **M3** 本地回滚 harness：人工延迟 + 输入扰动 + 校验和风暴测试（联机的全部难点在这一步离线解决）。
- **M4** UDP 传输 + 会话/重同步，真机对战。
- **M5** PyO3 headless + 并行 env。
- 表层语言编译器与 THTK 工具链联动可与 M3+ 并行推进。

---

*v0.2 相对 v0.1 的主要修订：确定性从"尽量"改为七条硬不变量（§2）；"哑弹/脚本弹"细化为三档并统一到单一 ECL VM（§3.2）；补全敌人、碰撞、step 顺序三节；"世界 memcpy 发包"拆分为本地快照与仅发输入的协议（§7.3）；新增渲染双通道与回滚表现策略（§6）、ActionInput 输入抽象（§5）、headless 沙箱（§8）与测试保障（§9）。*

*v0.3 相对 v0.2 的主要修订（grill 评审）：明确五大目标的**阶段优先级**与 phase 1 的金向量 DoD（头部）；**取消静态弹幕 proto**，改为哑创建原语 `create_bullet/create_bullets_batch` + appearance→判定约定表（可显式覆盖）+ 脚本层 shooter 模式（§3.2.1、§4.4）；钉死 ECL **任务生命周期**（owner 失效则死）、**两条级联轴**（owner 轴自动、parent 轴默认不级联，显式 kill_children 才整树清）与**派生时机**（默认次帧首跑、born_frame 戳、spawn_task_now 为后期 worklist 例外）（§3.5、§4.2、§4.3）；`Task.locals` 扩容至 64 承载 shooter 状态；变换槽上限定为引擎常量 `MAX_XFORM_SLOTS = 8`；数学核烘焙表改为"commit 字节 + CI 再生成断言 + 握手带表哈希"、atan2 定为整数 CORDIC（§2.1）；校验和定为字段级哈希全槽 + derive 宏防漏、逐帧(CI)/K采样(联机)、配"复用槽写满"硬规则（§2.4/§3.6）；phase 1/2 分期声明进 §7；**新增全局结构层 §1.1（crate 分解，断层线编译期焊死）/§1.2（状态所有权）/§1.3（阶段→结构映射）**；更新待拍板清单（§10）。*