# Rust 构建产物
/target
**/target

# 【注意】Cargo.lock 【要提交】—— 确定性项目须锁定依赖版本以复现构建。
# 故此处不忽略 Cargo.lock。

# 调试符号 / 崩溃转储
*.pdb

# 编辑器 / IDE / 工具 sidecar
/.idea/
/.vscode/
*.swp
*~
*.tags

# Godot 编辑器本地缓存(资源导入缓存/UID索引;.uid sidecar 相反要入库,见坑档 G2)
/godot/.godot/

# 操作系统
.DS_Store
Thumbs.db

# stg-py（独立 workspace 的本地 venv / wheel / 构建产物；Cargo.lock 仍提交锁版本）
crates/stg-py/.venv/
crates/stg-py/dist/
crates/stg-py/target/

# Python 字节码 / pytest 缓存（stg-py 冒烟）
__pycache__/
*.pyc
crates/stg-py/.pytest_cache/
